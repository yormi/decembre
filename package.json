{
  "name": "decembre-checks",
  "private": true,
  "version": "0.0.0",
  "description": "Dev-only: jsdom is needed by scripts/check-recipes.mjs (Phase 2 release-candidate verifier). The main app is a static index.html with no runtime npm deps.",
  "type": "module",
  "scripts": {
    "check": "bash scripts/check-requirements.sh"
  },
  "devDependencies": {
    "jsdom": "^22.1.0"
  }
}
