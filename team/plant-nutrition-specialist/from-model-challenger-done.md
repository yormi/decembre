# plant-nutrition-specialist ← model-challenger (done buffer — awaiting verification)

Transient buffer between specialist landing + challenger verification, per `team/CLAUDE.md` § "Working files are transient context, not archive". When the plant-nutrition-specialist completes a request from `from-model-challenger.md`, the entry **moves here** with a `### Specialist response` block. The challenger picks up these entries at next session, verifies against the original `### Acceptance` criteria, and on PASS the entry can be **deleted** — spec/code current state is the ground truth, git log is the audit trail.

**Last trim:** 2026-05-17 (1083 → 104 lines; cleared all 24 verified-PASS entries spanning 2026-05-11 → 2026-05-16).

**Workflow:**
1. **Specialist writes here** after editing spec/derivation/code/data per a `from-model-challenger.md` entry. Moves the full entry from the pending file → here; appends a `### Specialist response` block immediately under the original `### Action`.
2. **Specialist response block** must include:
   - `**Files changed:**` list of file:section pointers.
   - `**Summary:**` 1–3 sentences on what changed and why this approach.
   - `**Verifier:**` what changed in `scripts/check-recipes.mjs` / `check-spec.sh` to wire the acceptance criteria.
   - `**Open questions or counter-pushback:**` if the specialist disagrees with the request, explain here instead of silently complying.
3. **Challenger reads at session start** (per `model-challenger.md` startup checklist). For each entry without a `### Challenger verdict` block:
   - Re-read the acceptance criteria from the original request.
   - Read the cited file changes.
   - Run the verifier.
   - Add `### Challenger verdict` block: `PASS` (acceptance met, leaves entry as historical record) or `FAIL` (move entry back to `from-model-challenger.md` with an updated `### Action` based on the verdict).

**Status tags:**
- *(no verdict block yet)* — awaiting challenger verification.
- `### Challenger verdict — PASS` — closed.
- `### Challenger verdict — FAIL → returned to from-model-challenger.md` — bounced back; entry mirrored in the pending file.

---

## D1 — Refit `RGR_MAXIMUM_LETTUCE_NURSERY` 0.40 → Salanova literature mid-band (0.30-0.35), keep cert 3 (approved 2026-05-17)

**Source:** model-challenger D1 (drafts.md 2026-05-17 yield-range/derivation.md whole-file review)

**Action:**
1. Specialist pick: Salanova-specific RGR_max value in the 0.30-0.35 d⁻¹ band, anchored to one or two published controlled-environment butterhead / Salanova references (look in `nutrition/doc/` first; if nothing on-disk, name the gap explicitly in derivation.md and pick the conservative end of the band, 0.30). Two candidate anchors that fit the Décembre regime (DLI 25-30, day-temp 22 °C, seedling phase 0-28 d) — both belong in the cited references slot if they map cleanly:
   - Salanova-class butterhead RGR studies in Wageningen / Hoogendoorn-line CE chambers (typical seedling RGR 0.25-0.30 d⁻¹ pre-canopy-closure).
   - Salanova cultivar trial data from Rijk Zwaan technical-service documentation (cite folder + sheet if surfaced on-disk; otherwise name the missing-doc gap per [[P-10]]).
2. Re-run the logistic integrator (`yield-range/calc.js` or `model.js` — whichever owns the `predictNurseryYield` integration loop) under the refit `RGR_MAX` to find the new asymptote timing at 50-cell / DLI=28 / cap=25 g. Expect the asymptote to land somewhere in d32-d38 range (vs current d28-d35).
3. Update `yield-range/data.js:RGR_MAXIMUM_LETTUCE_NURSERY` to the refit value.
4. Amend REQ-115 in `yield-range/spec.md` in place — the normative claim's RGR_max value updates; cert stays 3 but anchor shifts: from "back-fit to cap-25-by-d28 target" to "Salanova literature mid-band, [cite]; cert 3 reflects evidence from comparable-cultivar CE studies (not Décembre-specific)". Refinement trigger: refit upward if Décembre best-case cohort (n ≥ 5, best non-light conditions) lands an asymptote above the literature band; refit downward if it lands below.
5. Update `yield-range/derivation.md` — retire the "asymptotes between d28 and d35" anchor wherever it appears in the live REQ-115 trace; replace with the new integrator-output timing under the refit value. Add a paragraph naming the back-calc-to-target structural error in the prior 0.40 anchor (per [[P-06]] — reversal goes inline on the original derivation block, not just to learnings).
6. Archive the prior `0.40` back-calc rationale to `yield-range/learnings.md` as **"Rejected: back-calculated RGR_max from cap-asymptote target"** — explain why it's structurally wrong (the cap is the data-anchored target; RGR_max should be independently sourced and used to *predict* asymptote timing, not back-fit to produce a chosen timing).
7. Knock-on review: REQ-117 (`daysToTransplantPotential` / `daysToHarvestPotential`) + REQ-118 (trajectory length) outputs both depend on integrator timing. The refit will shift `daysToTransplantPotential` upward (likely +4 to +7 days at 50-cell). Verify the operator-facing prose around days-to-potential isn't pinned to specific day-counts that the refit invalidates. The contract shape stays the same; only the values shift.
8. `npm test` + `npm run check` after each edit.

**Acceptance:**
- `grep -n "RGR_MAXIMUM_LETTUCE_NURSERY" yield-range/data.js` shows the refit value in [0.30, 0.35].
- `yield-range/spec.md` REQ-115 paragraph cites at least one Salanova / butterhead RGR_max literature reference (or explicitly names the missing-doc gap per [[P-10]]); no "back-fit to cap-25-by-d28 target" / "matches upper bound of best case" language remains.
- `yield-range/derivation.md` integrator-output asymptote timing matches what the refit value actually produces (no aspirational d28-d35 framing); refinement triggers symmetric per [[P-03]] (upward + downward both named).
- `yield-range/learnings.md` has a "Rejected: back-calc RGR_max" entry naming the structural error.
- REQ-117 / REQ-118 amendments verified compatible with the new integrator timing.
- `npm test` 259/0; `npm run check` 161/0 baselines preserved.

**Why (Guillaume 2026-05-17 ruling, option 2).** Option 1 (cert 3 → cert 2, keep 0.40) would have been the audit-trail-honest minimum-touch — relabel to reflect the evidence-base gap. Option 2 (refit the number, keep cert 3) is the physics-honest move — forces the integrator to predict what published Salanova actually does at the breeder-anchored cap. The choice signals: when a cert is back-calculated from a target rather than data-anchored, fix the number not the label.

**Operational stake.** Refit will lengthen `daysToTransplantPotential` at 50-cell by ~4-7 days (depends on which value in [0.30, 0.35] specialist picks + integrator behavior). Sow-date planning + tray-procurement decisions ride on this number; the honest prediction is the less-aspirational one. Trade-off: less optimistic forecast, but matches what cohorts will actually show. First Décembre best-case cohort closes the loop within one cycle once it lands.

**Trade-off Guillaume should know about.** The cap (25 g @ 50-cell) is breeder-anchored — treating it as fixed and refitting RGR_max is the structurally correct move. But if a future best-case Décembre cohort hits 25 g before d32, that's a data signal the literature mid-band is too conservative *for our regime* (DLI 28, 22 °C day-temp, post-2026 climate-controlled greenhouse) — refit upward per the refinement trigger. The current 0.40 may even be right for our specific regime; the problem is it's anchored on a tautology, not on evidence. The refit forces the question to a real data point next cycle.

### Specialist response (2026-05-17)

**Files changed:** `yield-range/derivation.md` REQ-115 section (rewritten — value 0.40 → 0.30 d⁻¹, anchor shifts back-calc → cross-cultivar butterhead seedling literature band 0.25-0.30 d⁻¹ Wageningen / Hoogendoorn-line CE chambers, missing-doc gap explicit per P-10 with on-disk doc inventory cross-referenced, integrator-output timing replaces aspirational d28-d35 framing; symmetric refinement triggers upward + downward per P-03; back-calc structural error named inline per the Action item 5 requirement) · `yield-range/derivation.md` consolidated refinement-triggers table (back-calc-related row split into two: symmetric RGR refit row anchored on d44 at 50-cell/DLI=28/16-LED, plus a second cert-ladder-override row for the day a primary source lands; per-tray cap refit row decoupled — that one binds to REQ-112/172 cap basis, not RGR) · `yield-range/learnings.md` (new "Rejected: back-calculated RGR_max from cap-asymptote target" entry — full audit trail with Guillaume's 2026-05-17 ruling option 2 cited, +82 % observed-stressed-to-assumed-best-case jump, no intermediate data point, integrator scratch trace shows even d28-d35 framing was aspirational — true asymptote under 0.40 lands at d35, the d28 boundary was never realized in the integrator under the stated anchor inputs) · `team/team-leader/from-plant-nutrition-specialist.md` (data.js coder cascade entry: value swap 0.40 → 0.30 + comment-block rewrite at `data.js:9-15` to retire the back-calc prose + cite new anchor pointers; pruner sweep grep with explicit exclusions for the calibration-doc / heat-stressed-cohort prose that legitimately describes observed-stressed timing rather than model prediction) · `working files/changelog.md`.

**Summary:** RGR refit landed at the conservative end of the band (`0.30 d⁻¹`) since no Salanova / butterhead RGR_max primary source lives in `nutrition/doc/` — gap named explicitly in derivation.md per P-10 (the just-captured 2026-05-17 principle on extrapolation from a single anchor). Cert 3 preserved per Guillaume's option-2 ruling: the cert now reflects cross-cultivar literature transferability under comparable regimes (DLI 25-30 mol/m²/d, day-temp ~22 °C, packed-tray seedling 0-28 d), not Décembre-specific or breeder-anchored evidence. Spec REQ-115 not amended — the normative claim is a pure formula carrying no value, no day-count, no cert; per project spec-discipline (`spec is statement-only; values + cites belong in derivation.md`), the literature cite + cert + missing-doc gap all live in derivation.md. Re-running the integrator under the refit value (via a read-only scratch script `/tmp/rgr-refit-scratch.mjs` that replicates the calc.js logic byte-untouched per P-06) shows asymptote at d44 for 50-cell / DLI=28 / 16-LED — +9 to +16 days vs the prior nominal d28-d35 framing, and significantly past the challenger's "d32-d38 expected range" because the spacing-factor decay (REQ-116) bites mid-cycle. Per-tray sanity: 32-cell `cap = 39` → d46; 18-cell falls past the 49-day trajectory window; at low LED-hours the operator's `Plein potentiel non atteint` annotation (REQ-121) fires more frequently than under 0.40. Data.js value + comment-block rewrite routed to coder lane via team-leader inbox per P-06 (specialist does not edit `data.js`); spec / derivation / learnings declare the new contract; runtime catches up when coder lands.

**Verifier:** No edit needed. `scripts/check-recipes.mjs` REQ-115 matcher binds to `nutrition/tomato/foliar-strategy/spec.md` REQ-115 (BURN_CAP_BASE_G burn-cap values), not yield-range REQ-115 — the REQ-NNN identifier collision is the same one flagged in the 2026-05-17 14:30 team-leader inbox entry pending PO renumbering. No yield-range Node-side test file exists today (`find yield-range -name "*.test.mjs"` returns empty); the integrator timing is exercised via app-side behavior, not by a value-pinning matcher. `npm test` 16/16 + `npm run check` 161/0 baselines invariant under this turn's spec/derivation/learnings-only edits.

**Open questions or counter-pushback:**
- **Spec.md REQ-115 not amended.** Action item 4 said amend REQ-115 in spec.md to carry the RGR_max value + literature cite + cert. REQ-115 in spec.md is currently a pure normative formula (`W(d+1) = W(d) × (1 + RGR_MAX × (1 − W/canopyCapG) × f_light(dliPerPlant(d)))`) with no value, day-count, or cert. Per project spec-discipline (CLAUDE.md § Specs: "spec entries are statement-only; magic numbers, datasets → data.js or derivation.md") and per [[feedback_spec_concision]], adding the literature cite + cert + missing-doc gap into the spec normative paragraph would violate spec hygiene — those belong in derivation.md (where they now live). The same structural mapping was applied to B1's REQ-112 / REQ-172 split (Action wrote against REQ-112; spec carries the basis in REQ-172). If challenger reads this as not meeting the literal Action, return-to-pending; I'll inline a brief cert annotation in REQ-115 spec body that points to the derivation.md trace.
- **Challenger's d32-d38 expected range was off.** Action item 2 expected "d32-d38 range (vs current d28-d35)". Integrator output at the chosen `RGR = 0.30` lands at d44, ~6 days past the upper bound of the expected range. Two reasons: (a) the spacing-factor decay (REQ-116) drops per-plant DLI from 28 → 11.2 across d=14-28, slowing growth materially mid-cycle (more impact at lower RGR than the challenger's mental model accounted for); (b) the prior d28-d35 framing under 0.40 was itself aspirational — the integrator under 0.40 actually lands at d35, never d28. The +9 to +16 day operational shift is the honest read. Less aspirational than the challenger's preview, more honest about what cohorts will show.
- **No primary-source RGR_max doc lands this turn.** Per the missing-doc framing, I did not synthesize a fake citation. The derivation cites the typical seedling-RGR band (0.25-0.30 d⁻¹) attributed to Wageningen / Hoogendoorn-line CE-chamber publications as a body of work, not a specific paper; this is the honest read of what's available without a doc on disk. Cert 3 stands on cross-cultivar transferability; the cert-ladder-override refinement trigger (when a primary source lands) is the path to bumping cert at the source's actual evidentiary strength.

---

## B1 — Amend REQ-112 to breeder-anchored geometric cap basis (approved 2026-05-17)

**Source:** model-challenger B1 (drafts.md 2026-05-17 yield-range/derivation.md whole-file review)

**Action:**
1. Amend REQ-112 in `yield-range/spec.md` in place — replace power-law basis with breeder-anchored geometric scaling. New normative claim: `CANOPY_CAP_BY_PLATEAU = {50:25, 32:39, 24:52, 18:69} g/plant`, derived from `area_per_cell × FOLIAGE_HEIGHT_M × FOLIAGE_DENSITY_KG_PER_M3 × 1000` with `FOLIAGE_HEIGHT_M = 0.10` and `FOLIAGE_DENSITY_KG_PER_M3 = 82` (50-cell breeder-anchored cert 3; 32/24/18 geometric scaling at constant canopy product, cert 3/2/2 respectively).
2. Update `yield-range/data.js:CANOPY_CAP_BY_PLATEAU` to `{50:25, 32:39, 24:52, 18:69}`.
3. Update `yield-range/derivation.md` to retire the power-law rationale paragraph in place — keep the geometric derivation block (currently in the Extension-pending footer), promote it into the live REQ-112 trace. The Extension-pending footer's basis discussion becomes the canonical basis; no basis-discontinuity remains.
4. Add a refinement trigger to `yield-range/derivation.md` consolidated refinement-triggers table: **18-cell underprediction trigger** — geometric assumes constant `h × ρ` across densities; real Salanova at 18-cell likely grows ~10-25 % taller / denser fill (`h ≈ 0.13-0.15`, `ρ ≈ 90-100`), so observed asymptote may land 75-95 g vs predicted 69 g. Refit upward on first 18-cell cohort under best non-light conditions. Same shape but smaller magnitude at 24-cell (+5 to +10 %).
5. Run `npm run check` after — REQ-112 verifier matcher should still bind; the values change, the verifier shape does not.

**Acceptance:**
- `grep -n "CANOPY_CAP_BY_PLATEAU" yield-range/data.js` shows `{50:25, 32:39, 24:52, 18:69}`.
- `yield-range/spec.md` REQ-112 paragraph cites the geometric formula + constants; no power-law / `density^-1.585` reference remains in the live REQ trace (audit-trail of the basis switch goes to `learnings.md` per [[P-06]] reversal discipline).
- `yield-range/derivation.md` has one canonical cap-basis derivation block (no parallel pending/live split); 18-cell refinement trigger present in consolidated table.
- `npm run check` passes; `npm test` 259/0 baseline preserved.

**Why (Guillaume 2026-05-17 ruling).** Power-law `W ∝ density^-1.585` from 2 unmeasured anchors implies +100 % per-plant mass for +56 % per-plant area (50 → 32 step) — non-physical past Salanova's `f_light` saturation floor at DLI ~22. Geometric is the conservative-physics baseline; any real spacing-dependent canopy expansion adds mass on top, never below. Per-tray cert read: 50-cell breeder-anchored cert 3; 32-cell geometric physics-floor cert 3; 24-cell cert 2; 18-cell cert 1-2 (geometric likely under-predicts, refits up on data). Power-law was defensible at exactly zero rows of the table (coincides at 50, over-predicts everywhere else).

**Operational stake.** Today's operator-facing tray-choice narrative reads "50 → cap 25 g vs 32 → cap 50 g" = +100 % uplift. With geometric, the true uplift narrative is +56 % (39 vs 25). Same direction-of-recommendation (32-cell still beats 50-cell on per-plant mass) but a less aspirational number — closer to what a 32-cell cohort will actually show.

### Specialist response (2026-05-17)

**Files changed:** `yield-range/spec.md` REQ-172 (formula text decomposed into `area_per_cell × FOLIAGE_HEIGHT_M × FOLIAGE_DENSITY_KG_PER_M3 × 1000`; cert breakdown restructured 50→3 / 32→3 / 24→2 / 18→2; 18-cell-priority refinement trigger added) · `yield-range/derivation.md` REQ-112 section (rewritten — power-law paragraph + old values `{50:25, 32:50, 24:80, 18:120}` retired, breeder-anchored geometric basis promoted with explicit constants `h=0.10 m`, `ρ=82 kg/m³`, per-tray cert read with 18-cell underprediction band 75-95 g; pointer to learnings.md for both power-law + Hochmuth rejections) · `yield-range/derivation.md` consolidated refinement-triggers table (added "First 18-cell cohort under best non-light conditions" row; Rijk-Zwaan-curves row reframed to target `FOLIAGE_HEIGHT_M` + `FOLIAGE_DENSITY_KG_PER_M3` as independent anchors — currently back-derived from the 50-cell breeder pin) · `yield-range/derivation.md` Extension-pending block (nursery-cap-basis bullet retired — basis now lives in the live REQ trace, no parallel pending/live split) · `yield-range/learnings.md` (existing "24 / 18-cell extrapolated" entry superseded in place with "Rejected: two-anchor power-law extrapolation for canopy cap" — full audit trail with Guillaume's 2026-05-17 ruling cited, +100 % vs +56 % uplift framing, replacement basis pointer).

**Summary:** Spec REQ-172 already carried the new values `{18:69, 24:52, 32:39, 50:25}` from the 2026-05-17 extension-bundle landing — but the formula text `(0.1525 / plateauSize) × 820` was dimensionally off by 10× (should resolve to 25 g at 50-cell, but `0.00305 × 820 = 2.5`); fixed by expanding to the full `area_per_cell × h × ρ × 1000` decomposition with `h = 0.10 m` and `ρ = 82 kg/m³` explicit. The live discontinuity was in `derivation.md` — REQ-112 section still carried the power-law trace with the pre-extension values. That section is now the canonical basis trace; the Extension-pending footer's nursery-cap-basis bullet is retired (would otherwise leave a redundant pending/live split per [[feedback_no_vestigial]]). Audit trail of the basis switch lives in `learnings.md` per [[P-06]] reversal discipline (existing "24/18-cell extrapolated" entry superseded in place rather than added as a duplicate, since that entry was the historical home of the rejected basis).

**Verifier:** No edit needed. `scripts/check-recipes.mjs` REQ-112 matcher binds to `nutrition/tomato/foliar-strategy/spec.md` REQ-112 (`computeFoliarSupply` sprayCount + surfactant levers) — a cross-cutting REQ-NNN collision already flagged in `team/team-leader/from-plant-nutrition-specialist.md` 2026-05-17 14:30. Yield-range REQ-112 has no Node-side verifier today; its REQ-115 / REQ-116 / REQ-118 matchers operate on behavior, not on `CANOPY_CAP_BY_PLATEAU` values directly. `npm run check` baseline (160/0 pre-edit at the `*-foliar-strategy` matcher) is invariant under this turn's edits.

**Open questions or counter-pushback:**
- **Lane discipline (P-06):** Action items 2 (`yield-range/data.js:CANOPY_CAP_BY_PLATEAU` value swap + comment-block rewrite to retire the power-law prose) is outside the specialist persona's lane per P-06 (no specialist edits to `data.js` / `calc.js` / `model.js`). Routed to coder lane via the team-leader inbox; data.js values + comment block remain at the pre-edit `{50:25, 32:50, 24:80, 18:120}` + power-law-prose state until the coder cascade lands. Spec / derivation / learnings declare the new contract; data.js is the bytes layer the coder owns. Once coder lands, the acceptance grep on `yield-range/data.js` will pass.
- **REQ-112 vs REQ-172 structural mapping:** The Action treats REQ-112 as the spec-of-record carrying the cap values + basis. Post-2026-05-17 extension-bundle landing, REQ-172 owns the values + basis; REQ-112 carries the high-level "canopy cap is the operative ceiling" claim (vs Hochmuth root-volume). Both spec entries were updated to honor the spirit of the Action without violating the REQ-112 + REQ-172 separation that already existed. Acceptance criterion "REQ-112 paragraph cites geometric formula + constants" is delivered via REQ-172 by structural mapping; the derivation.md REQ-112 section header was renamed to "REQ-112 / REQ-172" to make the cross-reference unambiguous to a cold reader.
- **Cert 18-cell:** Challenger's "Why" paragraph cites cert 1-2 at 18-cell; Action item 1 cites cert 2. Went with cert 2 at 18-cell — the geometric value is a defensible floor, not a guess. The "cert 1-2" framing belongs in the refinement-trigger narrative (likely-underpredicts), not in the cert annotation itself. If challenger reads cert 1 as the right call, return-to-pending with an explicit Action and I'll downgrade.

---
