# Todo — nutrition/tomato/foliar-recipe

## Commander's intent

PURPOSE

Foliar-recipe took the heaviest 2026-05-17 amend cycle — burn caps Mn/Zn re-pinned 18→22 / 16→22 (E entry), Mo carve-out to fertigation (REQ-061 amendment), yucca-return trigger formally dropped (B5 + P-08), REQ-101 cert-3 cert-bump-back trigger reframed condition-based, B1 delivered-vs-demand table reconciled to live `plant-needs/data.js` math, Cu cert-2 carve-out for divergence-below-extension established, Mn/Zn cert-2 carve-out parallel for divergence-above. The subproject's spec is mature; derivation carries the most live content of any subproject (475 lines). This round: derivation-prose density check + cert-table integrity (every cert annotation in derivation matches its mirror in `data.js` comment + spec.md REQ statement) + REQ-115 algorithm step coherence walk (steps 1 + 2 + 3 currently describe per-element floor + CE-cap loop + return shape, but the luxury-cap guard at step 1 added 2026-05-16 needs to be cross-referenced from step 2 explicitly so the cold reader sees the guard before the CE-cap loop sees a recipe with already-zeroed luxury entries).

KEY TASKS

1. Cert-annotation triangulation walk: every cert annotation in derivation.md (per-element burn cap table at line 83-90, per-element min-dose table at line 144-151, coverage discount at line 56-63, channel efficiency at line 267-281) must match the cert annotation in `data.js` comment block AND in `spec.md` REQ statement. Read all three; build a triangle per (constant, cert) and confirm consistency. Note: `data.js` is forbidden to edit per P-06 — read only; if a mismatch is found, edit spec.md / derivation.md to match data.js (the live code being load-bearing), or flag the mismatch for coder-lane edit.
2. REQ-115 step coherence: spec.md REQ-115 lines 122-156 list the algorithm steps; derivation.md "Per-element min-dose floor" (lines 130-181) + "CE-cap algorithm" (lines 184-217) carry the detail. The luxury-cap guard mention at step 1 must cross-reference the worked example table in derivation.md so the cold reader follows from spec → derivation → numeric example without dead-reckoning. Add the cross-reference in spec.md if missing.
3. Yucca-return trigger residual scrub: B5 archive verdict says the trigger formally rejected and FOLIAR_COVERAGE_WITH_YUCCA kept for future-toggle-only code. Confirm derivation.md doesn't carry any residual "if yucca returns, doses go back up" prose anywhere. Grep `grep -nE "yucca.*return|yucca.*back|doses.*back" nutrition/tomato/foliar-recipe/`. Residual references in `learnings.md` (rejection record) are intentional; references in `derivation.md` should be coverage-only-flip framing or absent.
4. Zn over-luxury (136 %) hold-doc check: derivation.md lines 348-356 + refinement-trigger lines 434-441 document the hold-Zn-pending-tissue decision. The team-visible text describes the decision well; confirm one consistent place names the trigger conditions (petiole Zn > 120 ppm DW Sonneveld OR leaf-tip phytotoxicity image) without duplication elsewhere. Currently the trigger conditions appear at line 440 and the algorithmic Zn handling at 348-356 — confirm cross-reference, no duplicate.
5. Sentís-ceiling refinement-trigger walk: derivation.md lines 49-53 + 271-281 + 421-426 carry the "downward Sentís ceiling" path. Three places repeat the framing; verify they're not duplicating numbers — derivation should name the framing once + cross-reference, not re-derive the 0.30 → 0.03 collapse three times. Consolidate if duplication is structural rather than emphasis.
6. Mn cert-2 carve-out cross-check vs Cu cert-2 carve-out: derivation.md "Per-element burn cap" section (lines 75-127) names two carve-outs — Cu divergence-below (cert 2) + Mn/Zn divergence-above (cert 2) — both with Décembre-internal-observation framing. The cert-2 transferability defense for Mn/Zn (operator-timing window, post-restructure tank concentration, Ca-saturated soil context) parallels Cu's cert-2 transferability defense (Cu enzyme-damage threshold, narrow toxicity, Décembre-specific axil-pool image). Confirm both defenses carry the same shape: cert-3-within-Décembre + cert-2-for-transferability with named mechanism. If one carries cert-3-within-Décembre framing explicit and the other doesn't, normalize.
7. Cert-bump-back trigger condition-based reframe: REQ-101 cert-bump-back trigger originally tied to "2026-05-12 tissue panel" (per B2 archive 2026-05-12). The 2026-05-17 changelog batch reframed many calendar-based triggers as condition-based. Verify the foliar cert-bump-back trigger reads "when tissue panel correlates predicted vs measured within ±20 %" without a hard calendar date. If date present, reframe.

END STATE

- derivation cert annotations (burn cap, min-dose floor, coverage discount, channel efficiency) all triangulate spec.md ↔ data.js.
- REQ-115 algorithm steps cross-reference the worked example in derivation explicitly.
- Yucca-return trigger fully retired from derivation.md (residual only in learnings.md by design).
- Zn 136 % over-luxury hold-rationale + trigger conditions named in one place + cross-referenced elsewhere; no duplication.
- Sentís-ceiling framing carries the collapse-math once + cross-referenced elsewhere; no triplicate derivation.
- Mn/Zn cert-2 transferability defense parallels Cu cert-2 in shape (cert-3-within-Décembre + cert-2-transferability).
- All foliar refinement triggers condition-based (no calendar dates).
- Zero new REQs expected.

RULES OF ENGAGEMENT

- Lane: own spec.md / derivation.md / learnings.md / this todo file
- Forbidden (P-06): app/index.html, */app/logic.js, */app/page.html, dist/, calc.js, model.js, data.js, requirements.md
- Verifier scripts: may edit if REQ changes
- REQ claims: scripts/claim-req.sh <spec-path> plant-nutrition-specialist (flock race-safe)
- Changelog: one line per material change, no trimming
- Deviation from intent: only with explicit justification in report

## Items

- [x] Cert-annotation triangulation walk (burn cap / min-dose / coverage / channel efficiency). → All four constants triangulate spec.md ↔ derivation.md ↔ data.js. Cu/Mn/Zn cert-2 in all three; Fe/Mo/B cert-3 in all three; coverage 0.30 cert-3 in all three; channel-efficiency cert-3 (effective cert min) in all three. Min-dose floor cert-3 across; derivation Cu rationale "Narrow toxicity threshold" matches data.js which adds "cert 2 burn-cap also argues against forcing higher dose" — internally consistent (floor cert is scale-resolution; burn-cap cert is separate axis).
- [x] REQ-115 algorithm steps cross-reference worked example in derivation. → spec.md REQ-115 step 1 luxury-cap guard line now reads "see derivation.md § 'Per-element min-dose floor' worked-example table for the Cu fire range"; step 2 transition prefaced with "(After step 1 returns a per-element floor + cap + luxury-guard recipe.)" so the cold reader sees the luxury-cap guard fires BEFORE the CE-cap loop reads a recipe with already-zeroed luxury entries.
- [x] Yucca-return trigger residual scrub (derivation; learnings is by-design residual). → grep clean of "yucca return / back / doses back" except for line 344 ("not tracked as a refinement-trigger return path" — framing the rejection, kept) + the learnings.md rejection-record entry (by-design residual). The channel-efficiency cert trigger that previously read "Yucca returns + tissue panel ±20 % correlation → cert 3 → 4 for the with-surfactant value" reframed condition-based: "Tissue panel ±20 % correlation under surfactant-on regime → cert 3 → 4 for the with-surfactant value (independent of whether yucca specifically is the surfactant)".
- [x] Zn over-luxury (136 %) hold + trigger conditions consolidation walk. → Trigger conditions named ONCE at derivation lines 434-441 (">120 ppm Zn DW Sonneveld guideline OR leaf-tip phytotoxicity image" + downward action "cut Zn to ~16 g/15 L"); algorithmic-handling block at line 350-356 cross-references with "Refinement trigger named below". No duplication detected.
- [x] Sentís-ceiling refinement-trigger consolidation (single derivation; cross-ref elsewhere). → Full collapse math (Zn 136→14%, Mn 72→7%, Cu 25→2.5%, foliar → insurance, soil-pH gate dependency) lives ONCE at refinement-triggers § "Downward (Sentís ceiling)" lines 421-426. Coverage-derivation lines 49-53 mention "refits to ~0.03" + cross-references "refinement triggers below" + "learnings.md" — names the framing, defers the math. Channel-efficiency cert section lines 277-281 rewritten to cross-reference: "see § 'Refinement triggers' below for the full collapse math" + headline numbers retained for context (~0.03 / ~0.06). Triplicate derivation eliminated.
- [x] Cu cert-2 vs Mn/Zn cert-2 carve-out shape normalization. → Mn/Zn certainty exception now carries explicit "cert 3 within Décembre" framing parallel to Cu: "Defensible by Décembre observation (cert 3 within Décembre — multi-week no-burn track at 22 g...); transferability to other ops unproven without an independent observation period (cert 2 in general — ...)". Both carve-outs now ride the cert-3-within-Décembre + cert-2-for-transferability shape with named mechanism (Cu: narrow Cu²⁺ enzyme-damage threshold; Mn/Zn: early-AM post-fog low-leaf-temp spray window + post-restructure tank concentration + Ca-saturated soil context).
- [x] All foliar refinement triggers condition-based (no calendar dates). → spec.md REQ-101 cert paragraph reframed (no more "2026-05-12 petiole panel"). derivation.md refinement-triggers § "Tissue test reveals per-element drift" — dropped "sampled 2026-05-11" + "(B2 downgrade, 2026-05-12)" parenthetical; "Until panel lands" → "Until tissue data lands". channel-efficiency cert section — yucca-returns precondition replaced with "surfactant-on regime" condition. Historical / factual date references kept (Cu axil-pool 2026-05-06, restructure 2026-04-29, Mo carve-out 2026-05-16, learnings rejection records) — those anchor what happened, not forward triggers.
