# Nutrition — Semis laitue — app UI specs

UI invariants for the Semis laitue (lettuce nursery) Nutrition admin
subpage: per-plant + per-tray + per-cohort grid, cell-volume warning,
modal-on-click pourquoi keys.

Crop-side recipe specs live in `nutrition/nursery/spec.md`; chemistry /
cross-crop rules in `nutrition/spec.md`.

No Semis UI specs are wired today (2026-05-09). The cross-crop URL hash
routing spec (REQ-005, in `requirements.md`) covers the subpage
crop-toggle behavior.

Add new specs here with the next available REQ-NNN id from the global pool.
