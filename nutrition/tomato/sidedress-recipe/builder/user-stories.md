# Tomate — sidedress-recipe — builder

UI invariants for the sidedress builder block on the Tomato Nutrition
admin page — per-element supply vs demand from the granular soil
channel plus a stored-vs-first-principles drift sub-block. Reads the
sidedress-recipe model (`*/sidedress-recipe/model/spec.md`); page
chrome / header in `nutrition/tomato/shell/spec.md`.

No REQs allocated yet — content frames the scope for incoming claims.
