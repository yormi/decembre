# Nutrition — Salanova — app UI specs

UI invariants for the Salanova Nutrition admin subpage (5-block flow,
modal-on-click cert, compost replaces soil, click-to-modal pourquoi keys).
Crop-side recipe specs live in `nutrition/lettuce/spec.md`; chemistry /
cross-crop rules in `nutrition/spec.md`.

No Salanova UI specs are wired today (2026-05-09). The cross-crop URL
hash routing spec (REQ-005, in `requirements.md`) covers the subpage
crop-toggle behavior.

Add new specs here with the next available REQ-NNN id from the global pool.
