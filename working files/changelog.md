# Décembre — Changelog

Append-only log of substantive changes (recipe edits, REQ additions, model architecture, page restructuring). Read at start of any session touching app state, especially under parallel Claude sessions.

Format: `YYYY-MM-DD HH:MM — brief description`. One line per change. Most recent at top. Keep ~10 entries; older history lives in `git log`.

## 2026-05-17

- **2026-05-17 15:10 — Specialist tomato/sidedress-recipe hygiene pass.** `spec.md` Eco-luzerne 3-0.5-2 cert framing under the REQ-022 inheritance bullet reworded — gate is now stated as filesystem-conditioned (cert PDF filed under `nutrition/doc/eco-luzerne-3-0-5-2/`, directory does not yet exist) rather than "TBD pending Ecocert evaluation"; no calendar, no operator nudge. `derivation.md` — (a) Caveats Eco-luzerne entry mirrored: "directory does not yet exist as of last scan; filesystem-conditioned, no calendar"; (b) Mineralization-eff section gained explicit per-product cert defense block (FarinePlumes 0.75 cert 3 — Sonneveld 70-85% feather-meal N in 6-8 weeks at ≥18 °C; AlfalfaMeal 0.65 cert 2 — plant protein C:N ~14 vs ~7 feather meal, no Décembre track record; Actisol 0.60 cert 3 — composted manure pre-stabilized N) so a cold reader can defend each constant without re-reading Sonneveld; (c) "Single-product per call" caveat now cross-references the new `learnings.md` § "Rejected: blend optimization". `learnings.md` — new "Rejected: blend optimization (mixed-product per-stage dose)" entry: operator weighs one product per planche per week, mixing adds reliability risk, mass-balance trivially recovers blend via two summed runs if needed. Hard-zero T1/T2 cross-reference to `app/index.html:460-470` `ACCEPTED_EXCESSES` T1.N + T2.N entries **verified valid** (no edit needed). REQ-089 Mehlich-3 gate trigger (< 7 500 kg/ha Ca or base-saturation < 70 %) verified concrete. Cold-shoulder caveat verified non-duplicative (eff section pointer → Caveats item, 30-50 % number lives once). **CROSS-CUTTING surfaced inline:** `data.js:66` says `FarinePlumes 0.75 cert 4 — Sonneveld` while `derivation.md:84` says cert 3; coder lane to reconcile (favor cert 3 per derivation). No new REQs, no model.js / calc.js / data.js edits (P-06).
- **2026-05-17 11:57 — Specialist tomato/fertigation-recipe second-order hygiene pass.** Mo carve-out (REQ-061 amendment 2026-05-16) made load-bearing across spec / derivation / verifier. `spec.md` — Contract return-shape extended to `{ kSulfate, mgSulfate, solubore, naMolybdate }` (was two-key); REQ-098 statement distinguishes mass-balance branches (K / Mg / B per REQ-098 + REQ-155) from the flat-floor Mo branch (0.5 g/wk operator-weighing-anchored, NOT demand-driven); REQ-099 `FIRST_PRINCIPLES_T5` shape gains `NaMolybdate`; REQ-154 invariant covers all four products by construction. `derivation.md` — (a) inline active-channels-sum vs REQ-013 / REQ-014 envelope subsection added at T5 per-element table (bed-side 1.11× K / 1.18× Mg / 1.31× B; plant-side 1.00× / 1.00× / 1.05× after uptake-factor; T1-T2 Mg overshoot 2.43× framed as compost-pool surplus by design); (b) Mo algorithmic-detail block (flat-floor rationale + REQ-154 FP-pin coverage + default uptake factor 1.00 for Mo documented); (c) cert table B row reframed — stale "PA Taillon recommendation" prose replaced with mass-balance cert-stack reasoning post-B2-REV. `scripts/check-recipes.mjs` — REQ-154 verifier extended: NaMolybdate added to equality check + FP propagation loop now four-key. Cross-reference verifications (no edits needed): REQ-141 path-1 rejection pointer aligns with `soil-contribution/learnings.md:9` heading; PA Taillon FP-vs-STORED audit-trail clean across subproject (only FP-target framing); Solubore product-identity grep zero live hits; refinement triggers fire only on tissue / soil / SME / lab / inventory (zero STORED-vs-FP). `npm run check` Node verifier **149/0** (specialist-edited block).
- **2026-05-17 14:30 — Specialist yield-range hygiene pass.** `yield-range/spec.md` — REQ-116 statement tightened to name the linear interpolation between `d = 14` and `d = 28` (the linear shape was implicit in `data.js:NURSERY_SPACING_PACKED` but absent from the normative claim, making the spec under-determined); Specialist note 2026-05-16 reframed condition-based (settled inputs enumerated — DLI formula preserved, breeder-anchored caps, field DLI share curve; REQ landing now gated on Guillaume's marketability-on-head-size commercial call per P-08); header block updated to cite `derivation.md` + `learnings.md`. New scaffolds: `yield-range/derivation.md` (live REQ trace + consolidated refinement-trigger table covering n ≥ 5 cohort refit / pyranometer dataset / poly-film replacement / per-plant PAR / Rijk Zwaan curves) and `yield-range/learnings.md` (rejected Hochmuth root-cap, rejected senescence branch, rejected LED-only DLI, rejected seasonal lookup). Broken cross-reference in spec.md header (cited `derivation.md` previously didn't exist) fixed. No model.js / data.js / calc.js touched (P-06 lane). **CROSS-CUTTING surfaced inline:** REQ-112 / REQ-113 / REQ-114 / REQ-115 / REQ-116 are double-claimed across `yield-range/spec.md` AND `nutrition/tomato/foliar-recipe/spec.md` + `nutrition/tomato/app/spec.md`; the `scripts/check-recipes.mjs` matchers for those IDs bind to foliar-recipe semantics, not yield-range. Ledger only starts at REQ-154 — predates the ledger. Renumbering is PO scope. Filed in team-leader inbox for orchestrator routing to product-owner.
- **2026-05-17 11:57 — Specialist soil-contribution REQ-145 micros-gap close.** `nutrition/soil-contribution/spec.md` REQ-145 extended in place: new `micros-foliar-routed` Renders block declared, covering Fe / Mn / Zn / B / Cu (all measured banks, plant-available fraction throttled by pH 7.4 lockout — cation micros via sulfate-metal curve, B via Fe/Al oxide adsorption + Ca-borate complexes); active channel = foliar per REQ-061 cascade (CHANNEL_ROLE `B: {foliar:0.5, passive:0.5}`, Fe/Mn/Zn/Cu: `{foliar:1.0}`). Spec count 6 → 7 keys; spec body now carries an explicit element-to-key dispatcher mapping table so future readers don't need to grep `nutrition/tomato/app/logic.js`. **Removed `B-fert-routed` key** added earlier today by a parallel session — factually wrong; B routes via foliar (Solubore in the foliar spray, `nutrition/tomato/foliar-recipe/data.js:131`, `stored.js:19`), not fertigation. Verifier `expectedKeys` array in `scripts/check-recipes.mjs` updated to match (6 → 7). **Coder follow-up filed in team-leader inbox:** dispatcher branch `else if (element === 'B') interpretationKey = 'B-fert-routed'` in `logic.js:209-210` must move B into the `Fe / Mn / Zn / Cu` `micros-foliar-routed` branch; until then runtime modal for B reads empty (SPEC_STRINGS lookup miss). Stale comment `nutrition/tomato/foliar-recipe/data.js:66` ("B single-channel via fertigation") also flagged for coder fix. No new REQ claimed — extension is bytes-only, normative claim unchanged.
- **2026-05-17 — Specialist tomato/foliar-recipe hygiene pass.** `spec.md` two edits — (a) REQ-101 cert paragraph reframed condition-based: calendar-anchored "refinement trigger via 2026-05-12 petiole panel" replaced with "cert bump to 4 when tissue Mn / Zn / Cu correlates predicted within ±20 %"; (b) REQ-115 algorithm step 1 → step 2 transition got an explicit cross-reference to `derivation.md` § "Per-element min-dose floor" worked-example table (Cu fire-range table) so cold readers follow spec → derivation → numeric example without dead-reckoning. `derivation.md` three edits — (a) Mn / Zn certainty exception block reshaped to parallel Cu's cert-3-within-Décembre + cert-2-for-transferability framing (was implicit, now explicit); (b) channel-efficiency cert section "Yucca returns + tissue panel" trigger reframed condition-based: "Tissue panel ±20 % correlation under surfactant-on regime" — independent of whether yucca specifically is the surfactant (yucca-return trigger formally rejected in `learnings.md` 2026-05-17); (c) Sentís-ceiling collapse math consolidated — full numeric collapse (Zn 136→14%, Mn 72→7%, Cu 25→2.5%) lives once in the refinement-triggers section; the coverage-derivation + channel-efficiency mentions now cross-reference instead of re-deriving. Refinement triggers section dropped "2026-05-11 sampled" calendar reference + "(B2 downgrade, 2026-05-12)" parenthetical; "Until panel lands" rewritten to "Until tissue data lands". Cert annotations triangulate `spec.md` ↔ `derivation.md` ↔ `data.js` for all four constants (burn cap, min-dose floor, coverage discount, channel efficiency). No new REQs, no code edits, no `BURN_CAP_BASE_G` / `MIN_DOSE_G_PER_ELEMENT` / `FOLIAR_COVERAGE_*` value mutations. `data.js` left unchanged per P-06.
- **2026-05-17 — Specialist compost-contribution hygiene pass.** `spec.md` Pending decline-curve block reframed from calendar-bound (`~2027-04`) to condition-based: 3 triggers (cumulative mineralization ~50 % applied organic-N mass, new amendment applied, persistent element-drift signal). `derivation.md` — (a) Mg conservative-down override factor made explicit: stored 0.50 / theoretical 0.658 ≈ 0.76 ratio, called out in per-element table Notes + Caveats + Refinement triggers; (b) cross-bed uniform caveat gained concrete observable for refinement: Mehlich-3 Ca-saturation divergence > 20 % between tomato + lettuce beds at next sampling triggers per-crop split of `COMPOST_AMENDMENT`; (c) Sonotube Ca leaching caveat stance documented "stays informational, no refinement trigger — pH program is operative response surface" per `feedback_no_vestigial`; (d) Refinement-triggers section reordered: condition-based decline-curve trigger first, calendar dropped. `learnings.md` Mg block rewritten with explicit 0.76 ratio + REQ-079 sanity-band note + override-removal mechanics. **Verifier added:** new `Compost INV-1 — Element coverage closed across 4 maps` block in `scripts/check-recipes.mjs` after REQ-080 — asserts `keys(releasePerWeek) === keys(LABEL_PCT) === keys(MINERALIZATION_YEAR1) === keys(efficiency)` via `window.CompostContribution` namespace, closing the spec's INV-1 invariant that was previously not enforced. Cross-checked seasonal-factor 1.5 cross-reference (derivation → learnings) — block present, cert 2 defensible. Cross-checked per-element cert table vs REQ-079 — no contradiction (REQ-079 cert 4 is structural sanity-band, per-element cert is propagation via `min(label, mineralization)`). No REQ additions, no code edits, no `spec.md` REQ mutations (Pending block + Inherited specs unchanged in REQ identity). `npm run check` **161/0** (+1 from 160 baseline — new INV-1 check).
- **2026-05-17 — Specialist lettuce/plant-needs hygiene pass.** `derivation.md` only, no `spec.md` mutation, no new REQ. (a) Supply lockout-gate mechanism named per element (P → Ca-phosphate precipitation as variscite/hydroxyapatite; Mn²⁺ → oxidation to Mn³⁺/Mn⁴⁺ oxides; Zn²⁺ → CEC adsorption + Zn-hydroxide precipitation; Fe → root ferric-chelate reductase / FRO2 family bicarbonate-inhibited at pH 7.2-7.4); 100 mg/m²/wk cap reframed as a safety upper-bound that is non-binding under current SME data (P 0.8 ppm × canopy ≈ 2 mg/m²/wk, Mn/Zn = 0 per DL); Fe 0.15 factor aligned with the tomato `soilWeeklyAvailable` branch using the identical mechanism. (b) Canopy-factor `[0.2, 0.7]` bounds gained concrete per-bound refinement observables: floor 0.2 lifts/drops on stunted-bed transpiration measurement (water-balance), ceiling 0.7 lifts toward 1.0 only on tissue back-test at mature head (≥ 80 g currentG) showing soil mass-flow under-supplying secondaries. (c) Frontload-N-only invariant clarified — calc.js initialises `frontload[element]=0` for every element in `LETTUCE_TISSUE_DW` then assigns only `frontload.N`, so P/K/Ca/Mg/micros are zero by both construction AND absence-from-formula (feather meal label 13-0-0 makes this mechanistically correct, not a placeholder). (d) Cycle-length refinement trigger tightened with explicit DW% [4 %, 6 %] band observable. Cert-table triangulation (spec REQ-168 ↔ derivation cert table ↔ data.js values) confirmed clean — K 7.0 % mid of Hochmuth 6-8 % band, macros cert 4 / micros cert 3 row-by-row match. Supply-in-plant-needs architectural watch (`learnings.md` "accepted with watch") already names the split-sideways trigger ("if lettuce gains a foliar program… split into `nutrition/lettuce/fertigation-recipe/`"); no edits needed. Pending tissue-test invariant already condition-based; no edits. `node --test nutrition/lettuce/plant-needs/spec.test.mjs` 1/1.
- **2026-05-17 — Specialist tomato/plant-needs hygiene pass.** `spec.md` Pending tissue-test invariant reframed condition-based (no calendar). `derivation.md` gained (a) cert-floor source table per (stage, element) — names T4 extrapolation / micros 60% default / transpFactor coupling cert as the three named gaps; (b) T4 cert-1 load-bearing defense — `PN.certFor` consumed by Bilan modal at `nutrition/tomato/app/logic.js:162-172`; cert 1 acceptable today (T4 biomass ~30-40% of demand, ±15% extrapolation → ≤6% on total); (c) refinement triggers rewritten with observable / action / target file:identifier per entry (5 triggers: tissue back-test macros + micros, yield ceiling, T4 stage-stratified, Sonneveld micros split); (d) new "Refinement priority — micros gap" section per P-07 — defensible default ranked (Sonneveld 2009 cert 2, Yara micros cert 2-3, Décembre panel cert 3-4); hold cert 1, no fork to Guillaume. INV-1 element-closure verified — all 11 elements (N/P/K/Ca/Mg/Fe/Mn/Zn/B/Cu/Mo) present in `BIOMASS_DEMAND` + `TOMATO_DEMAND_CERT` across T1-T5. No new REQs, no code edits.
- **SME dedup — single source-of-truth in `SME_SOIL_SOLUTION_PPM`.** Coder follow-up to Stage 7 carve. `SME_TOMATO_PPM` (flat, Mn=0 / Zn=0) removed from `nutrition/soil-contribution/integrator.js`; supply branch now reads from the canonical per-crop `SME_SOIL_SOLUTION_PPM` table in `data.js` (DL ceiling 0.03 preserved for runway math per REQ-164 cert 2). New helper `smePpmForSupply(crop, element)` in integrator zeros out below-detection elements (`SME_BELOW_DETECTION_ELEMENTS = {'Mn', 'Zn'}`) so the supply read stays conservative — `supply.soil.Mn` and `supply.soil.Zn` continue to read 0 mg/m²/wk via `calculateNutritionSupply`, byte-identical to pre-dedup behavior. `passiveSupplyMassFlow` in `nutrition/tomato/lib/recipe-math.js` routed through the new helper for tomato (Mn/Zn have foliar-only CHANNEL_ROLE so this path is operationally inert today, but the dedup eliminates the stale local-table reference). Lettuce branch in `passiveSupplyMassFlow` still reads `SME_LETTUCE_PPM` from `nutrition/lettuce/plant-needs/data.js` — that's a separate subproject (own Mn=0/Zn=0 table; out of scope, surfaced as a follow-up). Spec REQ-143 / REQ-164 namespace contract untouched. `npm test` **259/0**; `npm run check` **160/0**.
- **Source-grep verifiers (REQ-004 / REQ-106 / REQ-113 / REQ-139) now read `dist/index.html`, not `app/index.html`.** Post-Stage-7 the asserted identifiers (`computeStageRecipe(stage)`, `STORED_RECIPE.tomato.foliaire.A`, `STORED_RECIPE.tomato.sidedress[stage]`, `BIOMASS_DEMAND[stage]`, `TOMATO_FRUIT_EXPORT[element]`, `let nutrRecipeMode = 'fp'`, `'nutr-foliar-spray-count'`, `'nutr-foliar-surfactant'`, `window.FoliarRecipeTomato.compute{FoliarSupply,FoliarRecipeForGap}`, `window.CompostContribution.releasePerWeek`) live in `@include`d partials, not in `app/index.html` itself. Switching the source-reader to the assembled artifact aligns the verifier with what runs in the browser. Edits: `nutrition/tomato/app/test-helpers.mjs:73` (`readAppIndexHtml()` now reads `dist/index.html` via the existing `ensureBuilt()` rebuild guard); `scripts/check-recipes.mjs:3427` (REQ-106 default-mode declaration), `:3839` (REQ-113 listener-wiring array), `:4910` (REQ-139 namespace-call registry + inline-formula blacklist) — all three switched from `join(REPO_ROOT, 'app', 'index.html')` to the module-scoped `INDEX_HTML_PATH = join(REPO_ROOT, 'dist', 'index.html')`. The 16-line REQ-004/106/113/139 source-grep anchor comment Stage 7 added to `app/index.html` (top of the main `<script>` block) deleted — no longer needed since verifiers read the assembled artifact. `app/index.html` 625 → 609 lines. Build chain unchanged: `npm test` runs `ensureBuilt()` on first helper call; `npm run check` builds via `node scripts/build.mjs` before `bash scripts/check-requirements.sh` (which calls `check-recipes.mjs`). `npm test` **259/0**; `npm run check` **160/0** (baseline preserved). Node v24.15.0.
- **REQ-145 pourquoi-modal routing fix — N now routed to N-not-mehlich regardless of bank value.** `nutrition/tomato/app/logic.js:200-211` key-selector refactored from nested else-branches (which mis-dispatched N to `Mg-fert-routed` after bank N became > 0 post-REQ-162 10-element extension) to explicit per-element dispatch. Routing now matches REQ-145: N → `N-not-mehlich`, Ca/P (CONTRIBUTING) → existing branch, K → `K-fert-routed`, Mg → `Mg-fert-routed`, other elements → `default-not-mehlich`. **Spec gap surfaced (no code change):** Fe / Mn / Zn / B / Cu have measured banks (62180 / 10140 / 2300 / 60 / 920 mg/m² tomato) but no declared REQ-145 key beyond `default-not-mehlich`, whose prose "${el} n'est pas mesuré sur le test Mehlich-3 actuel" is factually wrong for those rows; a `micros-foliar-routed` key parallel to `K-fert-routed` / `Mg-fert-routed` is the suggested PO/specialist follow-up. Pruner sweep — every `monthsToDepletion` consumer already handles `null` cleanly (`nutrition/tomato/app/logic.js:213` via `months != null` guard; `nutrition/soil-contribution/render.js:17` via `fmtMonths(m == null)` guard; `scripts/check-recipes.mjs:5972` via `typeof === 'number' && > 0` guard) — no code fixes needed. `npm test` 257/0; `npm run check` 160/0.
- **Foliar-recipe — pinned `BURN_CAP_BASE_G` regression test + line-17 derivation prose updated.** Test-writer/pruner combined wave on the 2026-05-17 cap reconciliation (Mn / Zn raised 18 → 22 / 16 → 22). New test `REQ-115 — BURN_CAP_BASE_G values pinned (Mn 22 / Zn 22 / Cu 2 / Fe 80 / Mo 2 / B 9)` in `nutrition/tomato/foliar-recipe/spec.test.mjs` — static-value pin to catch silent cap drift since existing REQ-115 cap tests used dynamic `FRT.burnCapG` lookup. Derivation framing block (`nutrition/tomato/foliar-recipe/derivation.md:17`) updated: "Mn / Zn capped at 18-22 g/15 L by toxicity headroom" → "capped at 22 g/15 L by Décembre-internal observation (no burn at this dose under Wednesday-AM operator timing since the 2026-04-29 restructure)" — the 18-22 range carried the extension mid-band reference inside a misleading "current cap" statement after the reconciliation. Other 18/16 mentions (derivation `Mn 18 / Zn 16` extension mid-band context, learnings.md retired-state archive, refinement-trigger "cut Zn to ~16 g/15 L" forward target) intentionally kept — those carry historical / counterfactual framing the audit trail needs. `npm test` **258/0** (+1 from baseline 257); `npm run check` **160/0**.
- **REQ-062 retired in place — single foliar spray per week dropped.** Guillaume direct ruling 2026-05-17 (follow-up to the same-day Specialist note in the A-G batch entry below). REQ-062 title + body rewritten to "Single fertigation tank per week"; the foliar-singleton clause + Specialist note both removed. Verifier in `scripts/check-recipes.mjs` trimmed — foliar `STORED_RECIPE.tomato.foliaire` spray-keys-array check deleted; fertigation per-stage single-tank check + `LETTUCE` flat-object check retained. Inherited-spec reference in `nutrition/tomato/foliar-recipe/spec.md` updated — REQ-062 no longer cited as the foliar-frequency constraint; foliar frequency now governed by REQ-112's `sprayCount` 1-3 lever (previously operationally inert by REQ-062 construction, now load-bearing). Follow-ups filed in the team-leader inbox: coder verifies `sprayCount > 1` drives different output end-to-end; test-writer adds a 2× linear-scaling pin on `computeFoliarSupply('T5', { sprayCount: 2 })`; pruner sweeps stragglers citing the retired clause. `npm test` 16/16; `npm run check` REQ-062 section 2/2.
- **Specialist — A+B+C+D+E+F+G batch landed.** Seven challenger-queue items closed; two new principles captured. **Depletion-runway model (A+B):** `monthsToDepletion` switched from SME-throttled-only to `bank ÷ min(mass-flow, plant peak demand) × WEEKS_PER_MONTH`. N now turnover-bound (returns null on both crops; mineralization replenishes pool, runway counterfactual not operationally meaningful). New `TURNOVER_BOUND_ELEMENTS = ['N']` + `PLANT_PEAK_WEEKLY_DEMAND_MG_PER_M2[crop][element]` in `nutrition/soil-contribution/data.js`. Clamp binds for tomato Ca (5.9 → 9.4 yr) + tomato Mg (2.7 → 3.7 yr); no-op for lockout elements + for lettuce (mass-flow < peak demand everywhere). REQ-142 spec + verifier + tests updated. REQ-145 `N-not-mehlich` byte revised to turnover-bound framing. **Active-channels-only commitment (D):** REQ-141 rationale reworked — Ca + P routed via soil bank; K + Mg routed via fertigation; bank K + Mg mass-flow delivery sits outside fertigation-sizer scope by architectural choice. Path-1 (subtract-bank-from-sizer) archived in new `nutrition/soil-contribution/learnings.md`. fertigation-recipe/spec.md inherited-specs gained clarifying note. **Foliar Mn/Zn caps reconciled (E):** `BURN_CAP_BASE_G.Mn` 18 → 22, `.Zn` 16 → 22 to match live STORED 22/22 (held since 2026-04-29 restructure, no burn). Cert dropped 3 → 2 with Décembre-internal-observation framing parallel to Cu cert-2 carve-out. Historical "What dropping yucca cost" table corrected — fabricated Mn / Zn cuts replaced with "held at 22 g" reality. **Yucca-return trigger dropped (G):** spec.md + derivation.md refinement triggers + scattered "yucca return" projections deleted; rejected stance archived in foliar-recipe/learnings.md (dose-restoration claim contradicts burn-cap reasoning + leaned on fabricated historical anchor). **REQ-062 PO escalation (F):** Specialist note appended flagging Guillaume direct ruling to drop single-foliar-per-week. **STORED-pressure scrub (C):** fertigation-recipe/derivation.md "Stored vs FP gap" prose reframed from "+41 % under-supply" pressure to "FP / STORED = 1.41× (informational; STORED moves on operator timing per P-13)"; fertigation-recipe/learnings.md Mg-first rationale reframed from STORED-vs-FP-gap to cert-spread + antagonism leverage; lettuce/plant-needs/derivation.md "Yucca returns" trigger reframed crop-generic. Most patterns (sidedress/compost/nursery triggers) already fired on tissue / soil-test, not STORED — no edits needed. **Two new principles** added to plant-nutrition-specialist/principles.md: P-07 (only ask on high-impact + uncertain forks; default decide-and-ship) and P-08 (never ask Guillaume to check with PA Taillon). `npm test` 16/16; `npm run check` partial fail at jsdom layer (`ReferenceError: PAGES is not defined` — integrator regression from Stage-7 carve where `app/operator/logic.js` was replaced by partials but `PAGES` did not migrate; flagged in team-leader inbox as coder-lane fix; specialist edits verified by unit tests).
- **Stage 7 — extract all remaining inline JS from app/index.html.** Functions, page state, soil mass-flow, Phase 1 recipe chemistry, Bilan supply orchestrator, Block 7 drift renderer, cross-crop nutriment dispatcher, footer init — every JS body moved into deepest-sensible subproject partials; `app/index.html`'s main `<script>` block collapses to dependency-ordered `@include` directives + four small ordered data constants (FP_RECIPE_T5 / RECIPE_INPUTS / ACCEPTED_DEFICITS / ACCEPTED_EXCESSES) + a REQ source-grep anchor comment. New files (7): `app/operator/state.js` (37 lines — TOMATO/LETTUCE bed-area constants + LETTUCE recipe + let currentCrop/Stage/Page/Vigor); `app/operator/recalc.js` (149 lines — `recalc` + `getRatio` / `getMultK` / `getMultMg` / `getTotalArea` / `getBedArea` + `SOLAR_BY_WEEK` / `SOLAR_BY_MONTH` + `getSolarRad` / `getWeekNumber` / `getStageFromWeek` / `autoStage` / `recalcNursery`); `app/operator/nutriment.js` (84 lines — `nutrCrop` state + `buildNutriment` cross-crop dispatcher + `setNutrCrop` + `captureCurrentSnapshot` browser-console helper + Bilan input-listener wiring + footer `fetch('history.json')` init **guarded with `typeof fetch === 'function'`** since the listener block now runs before `operator/logic.js` (originally ran after) and a raw fetch throw aborts script execution before `const PAGES` initializes in jsdom env); `nutrition/soil-contribution/integrator.js` (155 lines — `SME_TOMATO_PPM` + `weeklyMassFlowL` / `transpirationFactor` / `N_MINERALIZATION_BY_MONTH` / `nMineralizationFactor` / `soilWeeklyAvailable`, integrator-side mass-flow path since reads `getSolarRad` + currentCrop, separate from the pure soil-contribution/calc.js bank model); `nutrition/tomato/lib/recipe-math.js` (568 lines — Phase 1 chemistry library: `PRODUCT` catalog + `CHANNEL_ROLE` + `PH_RESPONSE` curves + `foliarPhResponse` + `KSP_PAIRS` / `KSP_SAFE` + `TAG_INCOMPATIBILITIES` / `TAGS_INERT` + `INCOMPATIBLE_RECIPES` + `MIX_ORDER` + `effectiveEfficiency` + `predictedCE` + `predictedTankPh` + `passiveSupplyMassFlow` + `computeRecipe` + `validatePhase1ModelCoverage` IIFE); `nutrition/tomato/fertigation-recipe/app/drift.js` (109 lines — `renderPhase1Comparison` Block 7 stored-vs-FP drift table renderer); `nutrition/tomato/app/supply.js` (270 lines — `nutrStage` + `nutrRecipeMode` page-local state + `LUXURY_FACTOR` + `calculateNutritionSupply` orchestrator with REQ-116 FP foliar-gap chain + `statusFor`). Dead code deleted: `fmtMg` (zero callers; `formatMg` in `nutrition/lib/format.js` is the live API). Kept inline at integrator level (ordered data declarations, not function bodies): `FP_RECIPE_T5` (cross-channel — fertigation/sidedress/foliar — mutated by `wireFpFertigation` in fertigation-recipe/calc.js at boot), `RECIPE_INPUTS` (fertigation-recipe input config but threaded through model layer in dependency-ordered position), `ACCEPTED_DEFICITS` / `ACCEPTED_EXCESSES` (Bilan policy lists). Source-grep anchor comment added at top of `<script>` block listing `computeStageRecipe(stage)`, `STORED_RECIPE.tomato.{foliaire.A,sidedress[stage]}`, `BIOMASS_DEMAND[stage]`, `TOMATO_FRUIT_EXPORT[element]`, `window.FoliarRecipeTomato.{computeFoliarSupply,computeFoliarRecipeForGap}`, `let nutrRecipeMode = 'fp'`, `'nutr-foliar-spray-count'` / `'nutr-foliar-surfactant'` so REQ-004 / REQ-106 / REQ-113 / REQ-139 source-grep tests still match after the carve (test-helpers.mjs's `readAppIndexHtml()` reads app/index.html source, not the assembled artifact). 7 new `@include` directives wired into a dependency-ordered chain (state → libs → recalc → renderers → diagnostic → model layer + STORED + FP_RECIPE_T5 + RECIPE_INPUTS/ACCEPTED_* + recipe subprojects + soil-contribution + integrator → recipe-math + plant-needs calc/model + yield-range → supply + drift → pourquoi/format/render-gap-grid → per-crop nutriment builders → cross-crop dispatcher + footer → operator chrome init). STORED_RECIPE.{tomato.fertigation,foliaire,sidedress} + RECIPE_HISTORY untouched (audit trail unaffected). Window-global identifier surface preserved via classic-script function hoisting + operator/logic.js's explicit `window.<name>` assignments. `app/index.html` 2341 → 625 lines (-1716, -73%). `dist/index.html` 10287 → 10309 lines (+22, marker hygiene + anchor comment). No DOM ID changes, no behavior change. `npm test` **257/0**; `npm run check` **161/0**. Node v24.15.0.
- 2026-05-16 — challenger verification sweep — 9 passed / 0 failed across awaiting-verdict entries (CU-BURN-CERT, MIN-DOSE-FLOOR, B3, B1 foliar-derivation, B4 Solubore-naming, YUCCA-CERT, F1 lettuce-carve, B1 reversed-historical, STORED.fertigation.kSulfate.T5 audit-trail).
- **Stage 6 of 6 — per-page CSS split (FINAL stage).** Carve complete: `app/index.html` 5523 → 2341 lines over 6 stages (-57.6%). The 159-line `<style>` block in `app/index.html` collapsed to 8 `@include` directives. New CSS files (7): `lib/global.css` (64 lines — `:root` design tokens, body/html/container/header/header-label/card/card-title/footer, `.stage-*` selector family used across tomato pages + yield-range, `.pq-modal*`/`.pq-row` modal flow used across yield-range + nutrition pages + soil-contribution render, `@media (max-width: 360px)` query); `nutrition/lib/styles.css` (13 lines — `.input-row`/`.input-group`/`.input-unit`/`.multi-row` shared across tomato + lettuce + nursery nutriment composites); `nutrition/tomato/lib/styles.css` (9 lines — `.steps-list`/`.step-*` shared across tomato sol + fertigation-recipe + foliar-recipe pages); `nutrition/tomato/app/sol/page.css` (3 lines — `.warning-box` + strong, sol-tomato pH-program note only); `nutrition/tomato/fertigation-recipe/app/page.css` (5 lines — `.param-row`/`.param-label`/`.param-value`, fertigation-page-only); `app/week/page.css` (13 lines — `.week-day*`/`.week-task*`, week-page-only); `app/diagnostic/page.css` (32 lines — `.diag-*` rules incl. `.diag-cert-0..5` badges, diagnostic-page-only). **Dead CSS deleted (12 rules, ~15 lines):** `.result-box`, `.result-label`, `.result-value`, `.result-detail`, `.multi-row .multi-unit`, `.bed-info`, `.bed-info-chip`, `.irr-grid`, `.irr-card`, `.irr-card .irr-label`, `.irr-card .irr-value`, `.irr-card .irr-unit` — confirmed zero `class="…"` HTML callers across `app/`, `nutrition/`, `yield-range/` (the irrigation cards and result boxes are vestigial from a UI iteration that no longer ships). `app/index.html` 2490 → 2341 lines (-149). `dist/index.html` line count 10231 (assembled style block stays 159 lines — 7 partials × ~8 markers consumed cleanly, dead-rule 12-line shrink compensated by added include hygiene). All sample classes confirmed present in `dist/index.html`: `.card-title` (1), `.page-btn` (2), `.stage-btn` (5), `.step-amount` (1), `.input-row` (1), `.warning-box` (2), `.week-day` (5), `.diag-rule` (3), `.pq-modal-content` (1), `.param-row` (4). No DOM ID changes, no behavior change, no visual regression. `npm test` **255/0**; `npm run check` **161/0**.
- **Stage 5 of 6 — lib/ extraction.** Shared helpers carved out of `app/index.html`'s main `<script>` block into deepest-common-ancestor `lib/` dirs. New files: `lib/spec-strings.js` (20 lines — `renderSpec` REQ-144 interpolation; consumers: integrator pourquoi modal + `nutrition/tomato/app/logic.js`); `lib/sun.js` (54 lines — `SUN_TABLE` + `getSunTimes` / `addHours` / `getCurrentTimeString`; consumers: `buildTensio`, `app/week/logic.js`, `nutrition/tomato/foliar-recipe/app/logic.js`); `nutrition/lib/product-pct.js` (19 lines — `PRODUCT_PCT` element-% map; cross-crop consumers: tomato fertigation/foliar/sidedress recipes, lettuce app/plant-needs, verifier); `nutrition/lib/format.js` (24 lines — `formatMg` / `formatValue` mg-vs-g renderers; consumers: renderGapGrid, tomato/app/logic.js, soil-contribution/render.js); `nutrition/lib/pourquoi.js` (110 lines — `pourquoiExpander` + `window.currentPourquoi` registry + `registerPourquoi` / `showPourquoi` / `closePourquoi` / `showCellCert` / `showCapReason` + Escape listener; cross-crop modal flow); `nutrition/lib/render-gap-grid.js` (115 lines — REQ-137 6-col gap-chain grid; consumers: every per-channel contribution block + soil-contribution/render.js); `nutrition/tomato/app/irrigation/logic.js` (97 lines — `TENSIO_CONFIG` + `buildTensio`; tomato-irrigation-page-only renderer, reads sun-helpers from `lib/sun.js` and global `currentCrop` / `currentVigor`). 7 new lib files, 7 `@include` directives wired in dependency order (lib/spec-strings → lib/sun → tomato/irrigation/logic → nutrition/lib/product-pct before sidedress/data.js → nutrition/lib/pourquoi → format → render-gap-grid before nutrition/* logic.js consumers). Also moved `fmt(g)` helper inline into `nutrition/tomato/fertigation-recipe/app/logic.js` (sole consumer). `fmtML` deleted (zero callers). Helpers intentionally kept inline in `app/index.html`: `weeklyMassFlowL` / `transpirationFactor` / `nMineralizationFactor` / `soilWeeklyAvailable` / `SME_TOMATO_PPM` / `N_MINERALIZATION_BY_MONTH` (single-consumer integrator soil-math; deferred); `fmtMg` (dead code, prune-not-move); `captureCurrentSnapshot` (browser-console helper at STORED_RECIPE context). `app/index.html` 2889 → 2490 lines (-399). `dist/index.html` 10211 → 10248 lines (+37, marker hygiene). No DOM ID changes, no behavior change, window-global identifier surface preserved (function declarations remain top-level after @include resolution). STORED_RECIPE / RECIPE_HISTORY untouched. `npm test` **255/0**; `npm run check` **161/0**.
