# Décembre — Changelog

Append-only log of substantive changes (recipe edits, REQ additions, model architecture, page restructuring). Read at start of any session touching app state, especially under parallel Claude sessions.

Format: `YYYY-MM-DD HH:MM — brief description`. One line per change. Most recent at top. Keep ~10 entries; older history lives in `git log`.

## 2026-06-16
- **2026-06-18 — Stratégie laitue : recette fertigation — base corrigée (PDF = hebdo) + section « sans apport du sol ».** `strategy/fertigation-recipe.md` : table de comparaison repositionnée sur la **vraie pratique actuelle** (les grammes du PDF sont l'hebdo ; le lot physique = PDF × 5 sur 5 semaines → K₂SO₄ 910 g/sem), constat −78 % de sel total vs recette corrigée + preuve « 910 g K → tissu K au plancher = limite d'absorption (Na), pas d'apport ». Nouvelle section « sans apport du sol » : si le sol ne fournissait rien, la fertigation couvrirait 100 % du prélèvement (K ×3,5, et Ca/P/N exigeraient 3 nouveaux intrants peu solubles en bio + retour du Na) → le sol est la banque principale, la fertigation un appoint. Docs-only.
- **2026-06-17 — Stratégie laitue : table de comparaison recette fertigation (tissu-led).** Nouveau `nutrition/lettuce/strategy/fertigation-recipe.md` + pointeur dans `strategy/CLAUDE.md`. Compare la recette demande-modèle (K 590 g) à la version corrigée par le test tissulaire `LAIT #1` (K 200 g, Mg → foliaire, Cu gardé, Mo + citrique retirés) + colonne « runway » d'épuisement du sol depuis Mehlich-3 (K ~17 sem, Mg ~8 ans, Ca ~18 ans, P ~3,4 ans). Constat : l'épuisement n'est pas la contrainte — c'est le verrouillage (pH 7,48 + Na). Docs-only. **Diverge de `LETTUCE_FERTIGATION_RECIPE` opérateur (K 590) shippé le même jour — à réconcilier côté opérateur, décision Guillaume.**
- **2026-06-17 — Fertigation Laitue : carte config Dosatron + bloc « Vendredi matin » (recette 6 produits).** Sur la page Fertigation, sous-onglet de culture (Tomates / Laitue / Semis laitue) conservé. Vue **Laitue** = carte Dosatron 2 % (sans titre) + bloc « 🌅 Vendredi matin » (même gabarit que tomate : Laver → Mélanger → Brasser → ligne dosatron). Recette **fixe** (nouvelle constante `LETTUCE_FERTIGATION_RECIPE` dans `app/operator/state.js`) : stock ~24 L injecté à 2 %, tuiles Eau 24 L · Potassium 200 g · Magnésium 50 g · Fer 15 g · Manganèse 5 g · Zinc 2,7 g · Bore 6 g · Cuivre 1 g (valeurs corrigées tissu `LAIT #1` + cuivre ajouté le 18 — l'opérateur s'aligne maintenant sur la recette stratégie, divergence du 17 résolue). Render : `buildLettuceSteps()` dans `nutrition/tomato/fertigation-recipe/operator/logic.js` (pas de math solaire/stade) ; `setCrop` montre `#fert-steps-card` pour tomate ET laitue. Branches laitue mortes retirées de `getNutrients`/`buildSteps` + helper `fmt`. Bouton de culture **Laitue public** (gating admin retiré dans `applyAdminMode`). **À réconcilier (lane spécialiste) : ces grammes opérateur restent indépendants de la constante `LETTUCE` par 100 m² (2996/467/7,5) qui alimente le bilan Nutrition.** Vérifié (DOM, vue opérateur). check-spec : bash 12/0, node 145/2 (pré-existants, zéro ajout).
- **2026-06-16 — Carte AMF (Fertilisation) : protocole baril 170 L + tuiles par étape.** `nutrition/tomato/amf-drench/data.js` + `operator/logic.js` : 4 étapes — (1) remplir un baril (tuiles 170 L d'eau + 14 c. à soupe AGTIV REACH P) → (2) bien mélanger avant chaque arrosoir → (3) arroser de chaque côté des plants (tuiles 1 arrosoir · 🌱 · 1 arrosoir) → (4) répéter les étapes 2-3 pour chaque planche. Render généralisé : tuile(s) **par étape** (`step.tiles[]`, grille N colonnes `repeat(n,1fr)`, `amount`/`unit` vides masqués) au lieu d'un bloc `mix` global (champ `mix` retiré, helper `renderTiles`). Étapes retirées : diluer 2 tasses, gratter, irriguer 5 min. 2 avertissements conservés (goutte-à-goutte reformulé + fongicide). **Dose : 14 c. à soupe pour les 7 planches = cible protocole ~37 g/passe (montée depuis 6 c. à soupe ≈ 43 %). À confirmer en pesant 37 g une fois pour verrouiller le compte de cuillères.** Doc stratégie `strategy/protocol/amf-inoculation.md` non mise à jour (lane spécialiste). check-spec : node 145/2 (pré-existants, zéro ajout).

## 2026-06-13
- **2026-06-13 — Lectures substrat transplant archivées (/document) + caveat Pulse multicellule.** Capture (3" pot 5 sem. vs plateau 50 cellules 4 sem. : pH, EC leach, EC pic Pulse, temp terreau) → `nutrition/nursery/doc/transplant-substrate-readings-2026-06-13.png` + entrée d'index. `protocol/pulse-media-ec.md` : bloc « Applies to » ajouté — Pulse inopérant en plateau 50 cellules (prongs ne s'enfoncent pas), utiliser pour-through. Docs-only.
- **2026-06-13 — Grille de lectures par planche archivée (/document) + apprentissage Pulse-vs-1:1.** Capture du tableau (6 planches A–F : date transplant, EC 1:1, EC sol Pulse, pH 1:1, temp sol) → `nutrition/lettuce/doc/bed-grid-2026-06-13/` (image + index). Nouvel apprentissage `learnings/pulse-in-soil-tracks-moisture-not-salt.md` : Pulse enfoncé dans le sol suit l'humidité, pas le sel (les deux EC anti-corrèlent, r ≈ −0,36) → pas d'offset constant vers le 1:1 ; calibrer seulement à humidité fixe (capacité au champ). `setpoint-band-provenance.md` + `prefer-true-root-zone-bands.md` amendés (action « offset apparié » nuancée). Docs-only.
- **2026-06-13 — Transmission double-poly 0,55 → 0,65 + domaine racine (climat lumineux du site).** `GH_LIGHT_TRANSMISSION_DOUBLE_POLY` relevé 0,55 → 0,65 (haut de la bande fraîche 0,50-0,65 du `derivation` cert 3 ; film neuf/propre) dans `yield-range/data.js` + `yield-range/derivation.md` (constante + table déclencheurs) + `greenhouses/domain.md` (arête ×film). DLI banc soleil 16,5 → 19,5 mol/m²/j (~+18 %) → décale les rendements salanova calculés. 0,68 (article web générique) refusé : au-dessus du plafond frais 0,65. Nouveau `domain.md` racine : profil DLI extérieur mensuel (max ciel clair, Québec ~46,8°N) + moyenne annuelle ≈ 30 (valeur consommée par yield-range) ; profil = vérité de référence, **pas** un axe du modèle (lookup saisonnier déjà rejeté). check-spec : bash 12/0, node 145/2 (pré-existants, zéro ajout).
- **2026-06-13 — Visionneuse diagnostic : badge d'identifiant `#N` par photo.** Chaque carte porte un badge `#N` (coin haut-gauche de l'image) + l'id repris dans la légende du zoom. Numérotation 1-based par lot, dans l'ordre d'affichage, **indépendante des filtres** (id = position fixe dans `activeBatch.photos`) → un `#N` désigne toujours la même photo. Les numéros recommencent à 1 par lot (référer avec lot + numéro). Page autonome.
- **2026-06-13 — Visionneuse diagnostic : libellés de lots renommés, titre retiré, photos agrandies.** Lots renommés (`12 juin - Planche & Plateau`, `Suivi semis`) dans `diagnostic-data-2026-06-12.js` / `-06-06.js` ; titre « Diagnostic photos » retiré de la barre latérale ; grille élargie (colonnes minmax 300→430 px) → cartes/photos plus grandes. Page autonome.
- **2026-06-13 — Visionneuse diagnostic : menu fixe à gauche, encadré d'aide retiré.** `nutrition/strategy/lettuce/diagnostic-photos.html` : les trois contrôles (Lot de photos, Problème, Sévérité) déplacés dans une barre latérale fixe à gauche (268 px, défilement propre, puces empilées verticalement comptes alignés à droite, sections à en-têtes) ; la grille de photos occupe toute la largeur restante. Encadré d'aide (Confiance/n5/sévérité/zoom) supprimé. CSS morte retirée (`.help*`, `.batches`, `.flabel`). Responsive < 720 px : barre repasse en haut, puces en ligne. Page autonome, aucun fichier de données touché.
- **2026-06-13 — Visionneuse diagnostic : filtre par sévérité ajouté.** `nutrition/strategy/lettuce/diagnostic-photos.html` : 2ᵉ rangée de filtres « Sévérité » (Toutes / sévère / modérée / légère, comptes par photo, ordre pire→moindre) sous la rangée « Problème » (catégories, désormais étiquetée). Les deux filtres se combinent en ET ; reset au changement de lot. Pastilles couleur = couleurs des badges sévérité. Vérifié : sévère → 2 photos. Page autonome, aucun fichier versionné de données touché.
- **2026-06-13 — Lot 12 juin passé au skill /document (sources archivées).** 33 HEIC originaux (IMG_4663–4695) déplacés de `~/Downloads` vers `nutrition/lettuce/doc/diagnostic-2026-06-12/` (gitignorés, `*.HEIC` couvert) → la source vit avec son index. Index `CLAUDE.md` + `diagnosis.md` vérifiés conformes à la convention du dossier voisin 06-06. Aucun fichier versionné modifié hors ce changelog.

## 2026-06-12
- **2026-06-12 — 2ᵉ lot diagnostic salanova (33 photos champ/plateaux) + visionneuse multi-lots.** Nouveau `nutrition/lettuce/doc/diagnostic-2026-06-12/` : 33 photos IMG_4663–4695 (HEIC → JPG via `heif-convert`, redimensionnées ~1400 px, **gitignorées**) + `CLAUDE.md` index + `.gitignore`. Diagnostics générés par le workflow `lettuce-photo-diagnosis` (33 agents, un par image, sortie FR structurée, **120 diagnostics**, symptômes = observations visuelles seules ; garde-fou ajouté : pigment rouge variétal de la salanova rouge ≠ stress, signalé seulement si excessif/terne/nécrosé). Données : `nutrition/strategy/lettuce/diagnostic-data-2026-06-12.js`. **Visionneuse généralisée en registre de lots** : `diagnostic-data.js` renommé → `diagnostic-data-2026-06-06.js`, les deux fichiers `push` dans `window.LETTUCE_DIAGNOSIS_BATCHES` ; `diagnostic-photos.html` gagne un sélecteur de lot (caché si un seul) + compteurs catégories par lot. Catégories du lot 12 juin : désordre-physio (tip-burn) 34, sain 34, stress-salin 16, qualité-plant 14, carence 12, stress-thermique 5, fongique 4, ravageur 1. Docs-only, aucun spec/code app/STORED.

_Older entries (≤ 2026-06-06) live in `git log`._
