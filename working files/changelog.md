# Décembre — Changelog

Append-only log of substantive changes (recipe edits, REQ additions, model architecture, page restructuring). Read at start of any session touching app state, especially under parallel Claude sessions.

Format: `YYYY-MM-DD HH:MM — brief description`. One line per change. Most recent at top. Keep ~10 entries; older history lives in `git log`.

## 2026-06-06
- **2026-06-06 — Stratégie salanova : diagnostic (kernel Rumelt, partie 1/3) écrit.** Nouveau sous-dossier `nutrition/strategy/lettuce/` : `problem.md` (diagnostic — crux « culture de saison fraîche menée trop chaud et nourrie à l'océan » ; sodium = fil conducteur, chaleur = accélérateur ; critique vs bruit ; supply-vs-uptake : Ca/K/Mg bloqués par Na, N suffisant, P ok ; deux horloges nursery-rapide / sol-lent ; section **mauvaise stratégie** = pièges : ajouter chaleur/CO₂/lumière, pousser le poisson, chasser le jaune au N, traiter la laitue comme la tomate, confondre cible et stratégie ; 60 kg = brut récolte → têtes petites, défaut de grossissement) + `CLAUDE.md` index. `nutrition/strategy/CLAUDE.md` : pointeur sous-dossier lettuce/ (racine = stratégie tomate). `diagnosis-2026-06-06` : question brut-vs-paré résolue (brut). Docs-only. goal.md (politique directrice + actions) à venir.
- **2026-06-06 — Diagnostic rendement salanova documenté (/document).** `nutrition/lettuce/doc/diagnostic-2026-06-06/` : `diagnosis.md` (artefact versionné) + `CLAUDE.md` index + `.gitignore` + 8 photos PNG (séries plateau-50 15 avr→8 mai, racines serre froide vs planche chauffée/éclairée, transplant lavé monté en fleur) **gitignorées** (issues du Google Doc de suivi de Catherine ; durable = diagnosis.md). Plus lecture pH sol planche chauffée 7.3 vs non-chauffée 6.9. Diagnostic : crux = culture de saison fraîche menée trop chaud (propagation + champ), co-faute = carence cationique pilotée par Na élevé (Ca/Mg/K bas, N suffisant — « feuilles jaunes » ≠ N). Tissu LAIT #1/#2 référencé depuis `nutrition/doc/tissus - 2026-05-22.pdf` ; entrée tissus de `nutrition/doc/CLAUDE.md` reliée au diagnostic. Cible 200 kg/planche (60 best) = 60→200 g/tête. Docs-only.
- **2026-06-06 16:14 — Page operator « 🍄 Racines » (drench AMF) ajoutée.** Nouveau sous-projet `nutrition/tomato/amf-drench/` : `data.js` (`AMF_DRENCH` = produit AGTIV REACH P, schedule {start 2026-06-08, intervalDays 10, passes 5}, mix en tuiles {Eau 20 L, REACH P 3 c. à thé, 2 seaux/planche}, 4 étapes dont la 1ère porte les tuiles, 2 garde-fous ; `computeAmfPasses()` pur start+interval→liste), `operator/page.html` (3 cartes : **5 passages** [calendrier 5 lignes, prochaine passe surlignée], **Application** [recette en tuiles intégrée à l'étape 1, façon page fertigation], avertissements) + `operator/logic.js` (`buildAmf()` — rendu déterministe depuis `AMF_DRENCH`, lecture « aujourd'hui » au bord pour surligner la prochaine passe, dates FR, tuiles emoji/label/dose mono/unité). Cadence 10 j (max-flush, bas de la fenêtre 10-14 j du protocole) → passes : lun 8, jeu 18, dim 28 juin, mer 8, sam 18 juillet. Sous-toggle culture 🍅 Tomates (tomato-only, parité foliaire/sol ; `amf-crop-tomato` câblé dans `setCrop` + redirection tomato dans `setPage`). Câblage : bouton nav `🍄 Racines` dans `app/operator/page.html`, 3 `@include` dans `app/index.html`, `amf` ajouté à `PAGES` + toggles display/active + dispatch `buildAmf()` dans `app/operator/logic.js` (page opérationnelle publique, non admin, sans crop dans le hash). check-spec : bash 12/0, node 145/2 (Block 1 / Foliar réactivité, pré-existants/instables, zéro ajout).
- **2026-06-06 — Protocole AMF rendu agressif (flush-timing + 5 passes).** `nutrition/strategy/protocol/amf-inoculation.md` : nouvelle section **Flush-timing** — pulse Ocean (hydrolysat) 3-5 j avant chaque passe (lève une poussée de racines blanches, +21 % DW racinaire → plus de sites de colonisation) + cultivation légère au front d'humectation (poussée + canaux). Cadence relevée **2-3 passes/plafond 3 → 5 passes, tous les 10-14 j** (fenêtre racinaire végétative restante ; chaque passe attrape une poussée). Dose inchangée (~37 g/passe, ~185 g/¼ pail sur 5 passes — l'agressivité va dans contact + fréquence, pas la dose : la colonisation sature, le surplus de spores est gaspillé sans danger). Fulvique/humique écarté (choix Guillaume). Placement multi-points/coring en profondeur NON retenu cette passe. Odds ~45 % → ~55-65 % (cert 2 ; plafond structurel, l'écart se ferme au prochain transplant). Docs-only.
- **2026-06-06 — Sous-page « Entraînement au diagnostic » (galerie de 89 photos) sur la page Diagnostic.** Nouvelle page admin `diagnostic-practice` (la page Diagnostic est déjà admin-gated). `app/diagnostic/page.html` : carte-bouton « 🔬 Entraînement au diagnostic » → `setPage('diagnostic-practice')`. `app/diagnostic/practice-page.html` (filtre catégories + grille + retour). `app/diagnostic/practice-logic.js` : `buildDiagnosticPractice()` / `setDiagPracticeCat()` — 89 cartes regroupées par catégorie (7 présentes ; bacterial-disease + pest à 0 omises), confiance ★/5, sévérité **en toutes lettres** (mild→légère, etc.), partie du plant, blocs cause→indices visuels (bordure gauche colorée par catégorie). `app/diagnostic/page.css` : styles `.diag-practice-*` sur le thème clair de l'app (pas le thème sombre du prototype). `app/diagnostic/practice-data.js` (window.DIAGNOSIS_PRACTICE_DATA, 76 Ko, généré depuis `diagnosis-data.json` canonique commité) inliné via @include — pas de fetch. **Photos (~400 Mo) gitignorées à la source** ; `scripts/build.mjs` les copie image-only (filtre .jpg) `nutrition/tomato/doc/symptoms-2026-06-03/` → `dist/diagnostic/practice-images/` (dist gitignoré → 0 octet de photo dans le repo ; source absente → skip silencieux). Routing : `diagnostic-practice` ajouté à `PAGES` + `ADMIN_PAGES` + dispatch `setPage` dans `app/operator/logic.js`. Indices = observations visuelles seules (aucun chiffre de test tissulaire). check-spec : bash 12/0, node 145/2 (Block 1 / Foliar réactivité, pré-existants/instables, zéro ajout).
- **2026-06-06 — Protocole AMF (AGTIV REACH P) écrit pour le produit acheté.** `nutrition/strategy/protocol/amf-inoculation.md` réécrit : la comparaison pré-achat (DYNOMYCO vs AGTIV, deux modes) devenue vestigiale → protocole opérateur ciblé sur REACH P (peat powder, acheté 2026-06-06) en **drench manuel mid-crop** sur la culture actuelle (~45 %). Dose ~37 g/passe pour le bloc (~1070 plants = 382 m² × 2,8/m² ; ~2 c. à thé/seau 20 L, ~250 ml/plant, cert 2 — pas de taux mid-crop éditeur, 5× le taux de placement) ; placement **au front d'humectation du goutteur** (pas contre la tige) ; 2-3 passes 2-3 sem, plafond 3 sur T5 ; sorties anticipées définies. Garde-fous (pas de drip, pas de P soluble lourd, Ocean maintenu, pas de fongicide, sol >15 °C). Réserve pail pour le prochain transplant (~75 %, ~17 g, une passe). Gate cert : soumettre la fiche Pro-Cert/OMRI Canada à l'inspecteur Ecocert avant usage sol. `sg1-free-p.md` : statut + reco mises à jour (DYNOMYCO shelved). Docs-only, aucun spec/code/STORED.
- **2026-06-06 — Diagnostic photos symptômes tomate (3 juin) documenté.** `nutrition/tomato/doc/symptoms-2026-06-03/` : `diagnosis.md` (89 photos analysées un agent/photo → synthèse) + `CLAUDE.md` index (convention dir `symptoms-<date>/`) + 89 `.jpg` déplacées dans le repo mais **gitignorées** (~400 Mo, `.gitignore` local ; diagnosis.md = artefact versionné). Lectures classées : complexe taches fongiques bas du couvert (conf 4/5, espèce non séparable), chlorose internervaire vieilles feuilles (Mg probable, 3/5), désordres Ca fruits/BER (3/5). Docs-only.

## 2026-06-05
- **2026-06-05 — Fertigation operator : tuiles produit à 0 g masquées.** `fertigation-recipe/operator/logic.js` `buildSteps()` : les tuiles nutriment (Potassium, Magnésium, Solubore, Molybdène) sont filtrées avant rendu — toute dose arrondissant à exactement 0 g (`Math.round(value) !== 0`, sur la valeur brute en grammes, pas la chaîne formatée) disparaît. K₂SO₄ + MgSO₄ (coupés à 0 le 2026-06-05) n'apparaissent plus. Filtre sur 0 exact uniquement (les petites doses non nulles restent). Tuile 💧 Eau ajoutée après le filtre → jamais masquée. Aucun spec/STORED/matcher touché. check-spec : bash 12/0, node 145/2 (Block 1 / Foliar réactivité, pré-existants/instables, zéro ajout).
- **2026-06-05 — Pages operator alignées sur les coupes STORED (lane-codeur).** Foliaire `foliar-strategy/operator/logic.js` : `buildFoliar()` déjà data-driven sur `STORED_RECIPE.tomato.foliaire.A` → la tuile Cu disparaît automatiquement ; bloc de commentaire de rationale opérationnel Cu (4 g/67 ppm, fenêtre étroite, « black spots → drop CuSO₄ ») devenu vestigial après le retrait Cu → supprimé (note de retrait conservée). Aucune réf Cu résiduelle dans le layer operator (page.html sans Cu depuis le retrait « Signes à surveiller » du 2026-06-04 ; SAMPLE_GAP Cu dans `operator/spec.test.mjs` = input gap modèle, légitime). Fertigation `scripts/check-recipes.mjs` `stored-vs-computed-drift-block` : fixture repointée K₂SO₄→Solubore (B, borax 10 g) — K/Mg stockés = 0 depuis le 2026-06-05 divisaient par zéro ; B reste non-nul, teste la même direction FP ÷ Stored. Le rendu Block 8 (`fertigation-recipe/operator/drift.js`) gardait déjà zéro (gris/rouge/∞), aucun crash réel — seul le précondition fixture était rouge. check-spec : bash 12/0, node 145/2 (`stored-vs-computed-drift-block` reverdi ; Block 1 / Foliar réactivité = 2 rouges pré-existants/instables, zéro ajout).
- **2026-06-05 — CuSO₄ retiré du foliaire tomate (/retire-recipe).** `STORED_RECIPE.tomato.foliaire.A` : ligne CuSO₄ (2 g) supprimée (Mn 18 g + Zn 22 g + Fe 90 g seuls ; spray B CaCl₂ inchangé). Le Cu était un feed nutritif, pas un fongicide. Tissu Cu élevé = résidu de pulvérisation probable (Cu se lie à la cuticule, résiste au lavage labo) ; Cu sol normal → aucune carence à nourrir. Fenêtre suffisance→toxicité la plus étroite des micros + charge sol permanente (cert-tracked CAN/CGSB-32.311) → ROI négatif. Sol (Cu normal) couvre l'absorption racinaire de base ; arrêt sans risque de carence court terme (toute carence apparaîtrait lentement sur nouvelle croissance, correction par dose ponctuelle). Snapshot complet 3 canaux dans RECIPE_HISTORY. Surveiller nouvelle croissance + ré-évaluer prochain test tissulaire. **check-spec : bash 12/0, node 144/3 — zéro rouge ajouté par cette coupe (3 rouges pré-existants : `stored-vs-computed-drift-block` du K/Mg=0 routé lane-codeur, Block 1 / Foliar réactivité instables).**

_Older entries (≤ 2026-06-04) live in `git log`._
