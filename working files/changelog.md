# Décembre — Changelog

Append-only log of substantive changes (recipe edits, REQ additions, model architecture, page restructuring). Read at start of any session touching app state, especially under parallel Claude sessions.

Format: `YYYY-MM-DD HH:MM — brief description`. One line per change. Most recent at top. Keep ~10 entries; older history lives in `git log`.

## 2026-05-16

- **Team-leader closed sub-wave D + sub-wave E end-to-end.** Sub-wave D (PO REQ-159/160/161/162) — Wave 1 added 5 matchers in `scripts/check-recipes.mjs` (REQ-159 / 160 / 161 designed-to-fail; REQ-162 / 163 pass-with-TODO awaiting specialist data); Wave 2 coder shipped the visible cleanup (Manque entrant / Apport ici / Manque sortant now carry `(mg)` in their headers, cells render bare numbers via new `formatMg` helper, `(couvert)` parenthetical removed — bare `0` with REQ-016 green color carries the covered meaning; mirror edits on the soil-bank renderer). Sub-wave E (specialist Mo move, REQ-061 / REQ-099 / REQ-103) — Wave 2 coder landed the model-side cascade: fertigation now actually routes molybdenum (`computeFertigationSupply` reads `naMolybdate_g` × `NaMoO4_Mo` × 1000 / area instead of returning hardcoded 0); `computeStageRecipe` gains a flat 0.5 g/wk Mo branch across T1-T5; `FIRST_PRINCIPLES_T5_FERTIGATION` gains the `NaMolybdate: 0.5` slot pinned by `wireFpFertigation` invariant. Foliar side: `computeFoliarSupply` hard-codes Mo=0 regardless of recipe; `computeFoliarRecipeForGap` PRODUCTS list dropped Mo (5 elements now). REQ-115 test fixture cascaded. **STORED edits across both channels remain gated on `/retire-recipe`** — drift gauge will read fertigation Mo `0g stored vs 0.5g target` and foliar Mo `<stored>g vs 0g target` until Guillaume invokes it. Three mailbox entries archived (1 PO, 2 specialist); REQ-163 stays in PO inbox pass-with-TODO awaiting specialist's surfactant-aware foliar efficiency surface. `npm test` 189/189/0 · `npm run check` **153/0** (REQs wired 101/104, +5 from this wave). Node v24.15.0.
- **Root CLAUDE.md gains `Doc dirs — tree-level context` section.** New 3-sentence rule between "App invariants:" and "## Specs": before editing, read `<ancestor>/doc/CLAUDE.md` at every level from repo root down to the file's directory (any that exist), with `nutrition/tomato/fertigation-recipe/calc.js` as the worked example. Closes the gap that Claude Code only auto-loads sibling `CLAUDE.md` (lazy on file-touch), not `<ancestor>/doc/CLAUDE.md` at intermediate tree levels — so per-level tree context (e.g. nutrition-wide soil chemistry primer, tomato-wide channel-ownership notes) was previously invisible to agents editing deep files. `npm run check` 153/0 unchanged.
- **CLAUDE.md concision pass — root + nested.** Every paragraph in root `CLAUDE.md`, `team-coordination/CLAUDE.md`, `app/CLAUDE.md`, and the meta-rule paragraph in `requirements.md` rewritten to match PO-spec density: subject+verb+complement, scaffolding removed, redundant restatements collapsed inline. Root 55 → 51 lines, ~4.4 KB. No load-bearing rule dropped — same Specs section semantics, same parallel-session protocol, same Guillaume's-lane guardrails, same mailbox/principles conventions, same `app/` build-artifact note. `npm run check` 153/0 unchanged.
- 2026-05-16 — challenger autonomous triage of older PENDING tail — closed 17 findings (11 rejected per P-08 / supersession, 0 approved-and-routed-to-specialist, 6 left PENDING with triage notes for foreground walk).
- **Project CLAUDE.md second-pass trim; `app/CLAUDE.md` added.** Six edits inside the surviving root file. (A) Specs § Where — collapsed 3 file-path bullets to one line pointing at `requirements.md` § Domain organization (which already enumerates the 11 spec files in a table). (B) Specs § Operating rules — REQ-NNN allocation paragraph trimmed from "Acquires `flock`, scans tree + ledger tail, appends row, releases lock, echoes id … two REQ-145 collisions on 2026-05-12" mechanism+history to the one-line API: "claim via `scripts/claim-req.sh`; never invent from memory grep." (C) Specs § Operating rules — verifier-mechanics bullet deleted entirely; `requirements.md` § REQ-coverage tally is the source of truth. (D) Procedures-app paragraph — dropped "Trace lives in `<subproject>/derivation.md` … not in code comments" (already in memory `feedback_no_trace_comments.md`). (E) Parallel-session step 4 — dropped the trailing "`index.html` is the build artifact …" sentence; same content now lives in new `app/CLAUDE.md` (lazy-loads when a session touches the app surface, which is exactly when it matters). (F) Three historical parentheticals removed: "(Terminology replaced 'requirement' on 2026-05-09…)", "(Codified after REQ-003/056/057 removed 2026-05-08.)", and "(`derivation.md` is the convention)" (already said one bullet earlier). Net effect: root CLAUDE.md ≈ -10 logical lines of cruft (some inline, some structural); same load-bearing rules preserved. `npm run check` 153/0 unchanged. Note: another parallel session added a "Guillaume's lane — don't prompt him on calls he owns" section to CLAUDE.md mid-trim; left intact (well-formed, addresses a real persona pattern).
- **Project CLAUDE.md trimmed 109 → 49 lines; team-coordination/CLAUDE.md added.** Four sections moved or deleted, all redundant with other auto-loaded sources. (1) "REQ reference style" — deleted, already codified in user memory `feedback_req_expand.md`. (2) "Retiring a recipe" — deleted, already in `/retire-recipe` skill description + memory `feedback_audit_trigger_vs_snapshot.md`. (3) "team-coordination/ layout" — moved to new `team-coordination/CLAUDE.md` (lazy-loads when a session reads/edits mailbox files; correct scope for dir-local conventions). (4) "Conventions inherited by every page" — deleted; the meta-rule "domain/page specs do NOT restate cross-app REQs" added as a 3-line paragraph in `requirements.md` (after the domain-organization table, before the first `---`); the REQ-001/005/006/007 bullets were just restating entries already in `requirements.md`. Also deleted the stale "Don't spare me. Right now I need to fix the pH crisis…" sentence (season no longer in crisis-fix posture). **Kept at root** intentionally: Specs section + Parallel-session staleness mitigation (the latter mandates "read changelog at session start" — a root-only rule because nested CLAUDE.md files only load lazily on first traversal of their directory, which is too late). No verifier or test impact.
- **Mo move implementation cascade (coder, fertigation + foliar subprojects).** Closes the model-side gap left by the specialist's REQ-061 carve-out. `computeFertigationSupply` (`nutrition/tomato/fertigation-recipe/calc.js`) now routes Mo: reads `naMolybdate_g` from the canonical recipe shape (or `stored.naMolybdate` in the default-reshape branch), returns `Mo = naMolybdate_g × PRODUCT_PCT.NaMoO4_Mo × 1000 / area` (was hardcoded 0). `computeStageRecipe` gains a Mo branch — flat `naMolybdate = 0.5` g/wk across stages T1-T5 (smallest reliable barrel weight ≈ 7× peak demand, within Mo's wide tolerance band; from specialist derivation note). Return shape grew to `{ kSulfate, mgSulfate, solubore, naMolybdate }`. `FIRST_PRINCIPLES_T5_FERTIGATION` (`data.js`) gains a `NaMolybdate: 0.5` slot; `wireFpFertigation` IIFE propagates it into `FP_RECIPE_T5.fertigation` at boot. `computeFoliarSupply` (`nutrition/tomato/foliar-recipe/calc.js`) hard-coded to return `Mo: 0` regardless of recipe contents (the STORED foliaire entry still has `1 g NaMolybdate` per spray — gated on `/retire-recipe`). `computeFoliarRecipeForGap` PRODUCTS list dropped from `Mn/Zn/Cu/Fe/Mo/B` to `Mn/Zn/Cu/Fe/B` (five elements); `NaMoO4_g: 0` stays in return shape so `FP_RECIPE_T5.foliar.NaMolybdate` consumer keeps reading numerically. Two cascading test edits in `nutrition/tomato/foliar-recipe/spec.test.mjs` REQ-115 block: "huge gap → every element clipped at burnCapG(el)" PAIRS list dropped Mo; "surfactant=true reduces ideal_g" midGap shrunk to `{ Mn:1, Zn:1, Cu:0.5, Fe:1, B:1 }` so a non-Mo element shrinks under coverage (Mn / Cu drop). `npm test` 189/189/0 · `npm run check` 141/0. Node v24.15.0. **STORED edits gated on `/retire-recipe`**: STORED fertigation per-stage `naMolybdate: 1` and STORED foliaire `Molybdate de sodium 1g` both untouched — drift gauges will read fert Mo `1g stored vs 0.5g target` and foliar Mo `1g stored vs 0g target` until Guillaume runs `/retire-recipe`.
- **Wave 2 coder — REQ-159 / REQ-160 / REQ-161 wiring.** `renderGapGrid` (`app/index.html:4977`) elemental-mass column headers now carry `(mg)` (`Manque entrant (mg)` / `Apport ici (mg)` / `Manque sortant (mg)`); new `formatMg(mg)` helper added next to `formatValue` (line ~4958) renders bare numeric (no g-promotion) for the gap-grid cells; `(couvert)` annotation dropped from the `Manque sortant` cell (bare `0` carries coverage per REQ-016 color coding). Soil-bank renderer (`nutrition/soil-contribution/render.js`) mirrored — 3 elemental-mass headers gained `(mg)`, 3 cells switched to `formatMg`, `(couvert)` dropped. `formatValue` (with unit suffix) preserved for the Block 7/8 drift gauge + cap-modal delta cells (REQ-159 carve-out: drift gauge stays g/kg). Cascading sync: `REQ127_128_HEADER_ORDER` (line 3630) and `REQ137_HEADER_ORDER` (line 3888) in `scripts/check-recipes.mjs` updated to expect the new `(mg)` header strings — structural shape matchers needed to track the header rename per the same spec change. `npm run check` **153 passed / 0 failed** (was 151/2 with REQ-159 + REQ-161 designed-to-fail); `npm test` 189/189/0. Node v24.15.0.
- **Wave 1 test-writer — REQ-159 / REQ-160 / REQ-161 / REQ-162 / REQ-163 matchers added in `scripts/check-recipes.mjs`** (5 new `header()` blocks appended after REQ-158 / before final summary). REQ-159 walks every nutrition-table column header on tomato + nursery + soil-bank pages and fails when an elemental-mass column (Manque entrant / Apport ici / Manque sortant) lacks an `mg` / `(mg)` declaration — designed-to-fail (21 hits across 6 contribution blocks + soil-bank; recipe-product `Quantité` column carved out). REQ-160 fails when a cell text duplicates a header-declared unit — passes today (no header declares a unit yet) but registered so Wave 2's combined REQ-159 + REQ-160 fix lands covered. REQ-161 fails on any `(couvert)` substring in a contribution-block row — designed-to-fail (9 hits: compost P/Ca, sidedress P, fertigation Mg, foliaire Zn/Mo, soil-bank P/Ca, nursery fertigation P). REQ-162 + REQ-163 stay pass-with-TODO matching the Salanova/Semis pattern — REQ-162 awaits specialist's Mehlich-3 ÷ SME-weekly-uptake math; REQ-163 awaits specialist's surfactant-aware foliar efficiency surface AND coder-side Block 5 toggle wiring. `npm run check` **151 passed / 2 failed** (was 141/0; +10 passes from Salanova-TODO carve-outs + the 2 designed-to-fail entries). Node v24.15.0.
- **Mo move: foliar → fertigation (specialist, model-side shipped; STORED gated on `/retire-recipe`).** Per Guillaume's ship-it call. REQ-061 cascade rule carved out for Mo on anion-chemistry grounds — molybdate is fully plant-available at our soil pH 7.4 (`PH_RESPONSE['molybdate'](7.4) = 1.0`), so the foliar-bypass argument that keeps the cation micros (Mn / Zn / Cu / Fe) on foliar doesn't apply. Channel-side delivery efficiency maps updated: `FERTIGATION_EFFICIENCY_AT_CURRENT_SOIL` gains `Mo: 1.00`; `FOLIAR_EFFICIENCY_AT_CURRENT_CONDITIONS` drops `Mo: 0.27`. Verifier fixtures updated. Derivation notes on both subprojects document the carve-out + the team's smallest reliable barrel weight (0.5 g/week sodium molybdate ≈ 0.5 mg Mo/m²/sem ≈ 7× peak demand, within Mo's wide tolerance band). **Implementation cascade pending** (filed in team-leader inbox, two entries): fertigation calc to actually route Mo (currently returns hardcoded `Mo: 0`), foliar gap-fill to drop Mo from the iterated element set, STORED edits on both channels gated on `/retire-recipe`. `npm run check` 141/0 unchanged.
- **Efficacité column flipped from per-call-realized to channel-capability view.** Guillaume ruled: show the channel's efficiency for every element it CAN deliver, regardless of whether anything is being dosed this call. REQ-156 + REQ-157 amended in place (P-08); verifier loosened to drop the `mg=0 → no key` invariant; foliar block now reads its capability map directly from `window.FoliarRecipeTomato.efficiency` (Mo's 27 % cell now renders at every stage including FP T5 where the soil bank covers actual Mo). Fertigation + sidedress also benefit retroactively against the analogous stored-mode-B-routing risk. `npm test` 189/189/0 · `npm run check` 141/0.
- **Sub-wave A Wave 2 coder follow-up — REQ-157 efficiency: replace inline derivation with namespace read on 2 of 3 channels.** Per team-leader brief, fertigation + sidedress channels in `app/index.html` `calculateNutritionSupply` now assign `supply.fert.efficiency = window.FertigationRecipeTomato.efficiency` and `supply.sidedress.efficiency = window.SidedressRecipeTomato.efficiency` — single-line reads instead of inline `effectiveEfficiency × PH_RESPONSE` (fertigation) and mass-weighted Actisol/FarinePlumes blend (sidedress). Foliar substitution attempted then **reverted**: `window.FoliarRecipeTomato.efficiency` contains `Mo: 0.27`, but at fp T5 the gap-derived foliar recipe produces `NaMolybdate=0` → `supply.foliar.Mo=0` → REQ-157 verifier rejected `efficiency.Mo present (=0.27) for non-routed element (mg=0)`. Foliar channel kept its conditional-key inline derivation. `npm run check` **141 passed / 0 failed**; `npm test` 189/189/0; Node v24.15.0.
- **Wave 2 coder — REQ-158 no-abbreviation refactor (cross-app sweep).** Renamed identifiers across 39 JS/HTML/MJS files in app source + test scaffolding to make the verifier matcher pass (`scripts/check-recipes.mjs` REQ-158 block). Public-API renames cascaded through `app/index.html` consumers, `scripts/check-recipes.mjs` `exposeNames`, every `*.test.mjs` + `test-helpers.mjs`: `calcNutrSupply` → `calculateNutritionSupply`; `calcNutrDemand` → `calculateNutritionDemand`; `calcLettuceNutr{Supply,Demand}` → `calculateLettuceNutrition{Supply,Demand}`; `calcNurseryDemand` → `calculateNurseryDemand`; `effectiveEff` → `effectiveEfficiency`; `maxStableHours` → `maximumStableHours`; `minApplicationsPerWeek` → `minimumApplicationsPerWeek`; `SIDEDRESS_MIN_EFF` → `SIDEDRESS_MINIMUM_EFFICIENCY`; `MIN_EFF` (sidedress namespace key) → `MINIMUM_EFFICIENCY`; `TOMATO_NUM_BEDS`/`LETTUCE_NUM_BEDS` → `TOMATO_NUMBER_BEDS`/`LETTUCE_NUMBER_BEDS`; `RGR_MAX_LETTUCE_NURSERY` → `RGR_MAXIMUM_LETTUCE_NURSERY`; `TRAJECTORY_MAX_DAYS` → `TRAJECTORY_MAXIMUM_DAYS`. Object-literal keys: `eff` → `efficiency` on `SIDEDRESS_PRODUCTS.{Actisol,FarinePlumes,AlfalfaMeal}`; `req` (REQ-145 interpretation contract between `nutrition/tomato/app/logic.js` and `app/index.html` showPourquoi modal renderer) → `requirementId` (verifier indirect-call regex updated in tandem). Mechanical local-scope sweeps (`el → element`, `idx → index`, `cur → current`, `prev → previous`, `num → number`, `fn → handler`, `cfg → config`, `init → initialize`, `tmp → temporary` and composites like `blockEl → blockElement`, `headlineEl → headlineElement`, `totalMin → totalMinutes`, `dayNum → dayNumber`, etc.) applied via word-boundary regex per file. `npm run check` **141 passed / 0 failed** — REQ-158 now `scanned 36 files · 0 denylist hits`. `npm test` 189/189/0 unchanged. Markdown sweep (backticked tokens in `*.md`) deferred per Wave 2 brief.
- **PO-157 extended (specialist) — REQ-157 efficiency maps on the three remaining tomato channels (fertigation, foliar, sidedress).** Per Guillaume's call extending PO-157 beyond the original compost + nursery substrate scope. **`window.FertigationRecipeTomato.efficiency`** (K 0.94 / Mg 0.94 from `PH_RESPONSE['soluble-cation'](7.4)`, B 1.0 from `PH_RESPONSE['borate']`; cert 4) — channel-product → bed delivery axis distinct from REQ-155's bed → plant uptake axis. **`window.FoliarRecipeTomato.efficiency`** (Mn / Zn / Cu / Fe / Mo all 0.27 = `FOLIAR_COVERAGE_DEFAULT × foliarPhResponse(5.0)` = 0.30 × 0.9; B absent per REQ-061 single-channel; cert 3 — coverage cert dominates). **`window.SidedressRecipeTomato.efficiency`** (N-only at 0.75 from `FarinePlumes.efficiency` Sonneveld; cert 3; K/P absent because REQ-089 Ca-gate locks Actisol out on current Ca-saturated soil). New constants `FERTIGATION_EFFICIENCY_AT_CURRENT_SOIL`, `FOLIAR_EFFICIENCY_AT_CURRENT_CONDITIONS`, `SIDEDRESS_EFFICIENCY_AT_CURRENT_PRODUCTS` (full-word names per REQ-158). Per-subproject `spec.md` namespace tables (REQ-099 / REQ-103 / REQ-088) extended with `efficiency` row + rationale. Per-subproject `derivation.md` gained brief "Channel efficiency map (REQ-157)" sections covering value source + per-element cert + refinement triggers (Berger pH re-read for fertigation; yucca-return + Sentís paths for foliar; REQ-089 gate release + product-swap for sidedress). Verifier `scripts/check-recipes.mjs` extended with three new `validateEfficiencyMap` blocks under the existing REQ-157 specialist section. `npm run check` **139 passed / 2 failed** — both remaining failures outside specialist scope (REQ-145 pourquoi modal coder lane; REQ-158 cross-app abbreviation cleanup queued for coder wave). Three team-leader inbox entries filed (one per subproject).
- **Project-wide .md concision pass (~58 files touched).** Aggressive tightening across persona files (`.claude/agents/*.md`), `CLAUDE.md` + sub-`CLAUDE.md` index docs, every `*/derivation.md`, every `*/learnings.md`, every `*/spec.md` + `requirements.md`, and `working files/`. **Net: ~10,400 → ~7,200 lines (~30 % cut).** Headline reductions: `nutrition/spec.md` 827 → 564; `requirements.md` 413 → 183; `nutrition/tomato/fertigation-recipe/spec.md` 370 → 281, derivation 491 → 216, learnings 354 → 208; `nutrition/tomato/foliar-recipe/spec.md` 316 → 210, derivation 308 → 217, learnings 115 → 47; `working files/coherence-audit.md` 606 → 97, `nutrient-model-reference.md` 551 → 179, `unified-backlog.md` 453 → 211, `requirements-challenges.md` 354 → 70 (most challenges absorbed into spec tree). **Constraints preserved:** every `## REQ-NNN — <title>` header verbatim (both `check-requirements.sh` grep and `build.mjs` regex pass); every ` ```render <key>` fenced block byte-for-byte (build SPEC_STRINGS unaffected); every numeric value, formula, REQ pointer, cert annotation, source citation. PO-scope specs collapsed to statement-only per `feedback_spec_concision.md` (Rationale/Verification/Cert/Supersedes dropped from spec body — verifier IS the verification record). Model-layer specs kept richer shape at specialist discretion. New `learnings.md` files created for 6 subprojects (compost-contribution, nursery plant-needs, nursery fertigation, nursery substrate-contribution, sidedress-recipe, tomato plant-needs) holding rejected-alternative content extracted from their siblings. `npm run check` **141/0**, `npm test` 12/12 file-level (189/189/0 individual), unchanged from pre-pass.

## 2026-05-15

- **PO-158 Wave 1 — REQ-158 matcher (no-abbreviation rule, JS source) added in `scripts/check-recipes.mjs`** — designed-to-fail. Regex extracts function/const/let/var/class/object-key/param/destructure identifiers from owned-surface JS + `app/index.html`'s `<script>` blocks. Denylist 35 tokens (`eff`, `idx`, `req`, `temp`, `calc`, …); whitelist exempts chemistry/units + STORED_RECIPE / RECIPE_HISTORY keys + brand names. Result: 137/1 check (was 137/0); 186 hits across 17 files. Top: `el` (41), `maxStableHours` (17), `num` (9), `eff` (8). Wave 2 coder owns refactor.
- **Wave 3 pruner — REQ-137 amendment (6-col gap-grid) stale-prose sweep.** 3 surgical edits: `nutrition/render.js:4`, `app/index.html:5890`, `scripts/check-recipes.mjs:3895-96`. 2 borderline items surfaced for foreign owners (soil-contribution 6-col grid; helper context block). `npm test` 189/189/0 · check 137/0.
- **PO-157 cleared (specialist) — REQ-157 efficiency maps shipped on compost-contribution + nursery substrate-contribution channels.** `window.CompostContribution.efficiency` (N 0.30 / P 0.05 pH-locked / K 0.65 / Ca 0.60 / Mg 0.30 cert 1). `window.SubstrateContributionNursery.efficiency` + `cycleAverageReleasePerTray()` returns `{perTray_mg, details, efficiency}` (N 0.75, P/K/Ca/Mg 1.0). Both reference the same constant — single canonical source. Verifier REQ-157 stubs promoted. check 136/1; remaining failure is coder-lane in `app/index.html`. REQs 95/99 wired.
- **Wave 2 coder — REQ-127/128/137/156/157 wiring landed.** `renderGapGrid` grew 5→6 columns (added Efficacité at index 2). Signature gains `efficiency` arg. 5 channels wired: fert (K/Mg from PH_RESPONSE soluble-cation, B from borate); foliar (Mn/Zn/B/Cu/Mo/Fe ≈ 0.27 = foliarPhResponse(5) × 0.30); sidedress (N-blend Actisol+FarinePlumes, K from Actisol, P with phLockoutFactor); frontload (lettuce N-only); nursery (1.0 — bypasses soil pH). All 4 tomato + nursery + lettuce callers updated. **check 137/0 (was 132/5)**, `npm test` 189/189/0.
- **Wave 1 test-writer — REQ-137 amended (5→6 col) + REQ-156/REQ-157 matchers** in `scripts/check-recipes.mjs`. Header strip asserted `length === 6`, text `['Él.', 'Manque entrant', 'Efficacité', 'Apport ici', 'Manque sortant', '']`. REQ-156 walks 4 tomato Bilan blocks, asserts `cells[2]` matches `/\d+\s*%/` or `'—'`. REQ-157 walks runtime channel returns, asserts routed elements ∈ [0,1] and non-routed absent. **5 designed-to-fail matchers**: REQ-127/128/137/156/157. check 132/5 (was 128/0).
- **Wave 3 pruner — REQ-155 stale-prose sweep.** 2 edits (`fertigation-recipe/data.js` header, `model.js` namespace) updating `{kSulfate, mgSulfate}` → `+ solubore`. 4 borderline items surfaced for foreign owners (spec.md REQ-099 contract block, derivation.md line 248, two verifier strings). STORED-side prose correctly left alone (STORED structurally 2-key by design — solubore is FP-only per REQ-061). check 128/0 (no regression).
- **Wave 1 extension test-writer — REQ-013 anti-stub recompute updated for B2-REV uptake factor.** `nutrition/tomato/spec.test.mjs` REQ-013 now divides K + Mg demand by `G.PH_UPTAKE_FACTOR_AT_CURRENT_SOIL[el]` before subtracting sidedress + compost (matches calc.js post-REQ-155). Adds `t5.solubore` numeric guard. `test-helpers.mjs` EXPOSE_NAMES gains `PH_UPTAKE_FACTOR_AT_CURRENT_SOIL`. `npm test` 189/189/0.
