# Décembre — Changelog

Append-only log of substantive changes (recipe edits, REQ additions, model architecture, page restructuring). Read at start of any session touching app state, especially under parallel Claude sessions.

Format: `YYYY-MM-DD HH:MM — brief description`. One line per change. Most recent at top. Keep ~10 entries; older history lives in `git log`.

## 2026-05-17

- **2026-05-17 — Team-leader sub-wave K: yield-range RGR refit close.** Specialist's REQ-115 RGR_max refit entry (filed mid-sub-wave-J) closed inline. Value swap `RGR_MAXIMUM_LETTUCE_NURSERY 0.40 → 0.30` already shipped in W2 yield-range (sub-wave J, commit `e8728f9`); this turn added `yield-range/data.js:8` anchor comment pointing at `derivation.md` REQ-115 + `learnings.md` "Rejected: back-calculated RGR_max from cap-asymptote target", plus integrator pin in `yield-range/spec.test.mjs:464` asserting `daysToTransplantPotential = 44` at 50-cell / ledHours=16 / RGR=0.30 / cap=25g (matches specialist's scratch trace). Pruner sweep for `RGR.*0\.40` / `d28.*d35` stragglers: all hits in audit-trail (learnings.md historical block, derivation.md 0.40-back-fit-rejected paragraph, doc/yield-range-calibration-2026-spring.md cohort observations, team-coordination challenger drafts/principles/done files, mailbox entry itself) — KEEP per specialist's explicit exclusions. `npm test` 316 → 317 / 0 (+1); `npm run check` 161/0.
- **2026-05-17 — Team-leader sub-wave J: Salanova modal trim + boron routing + yield-range two-regime extension.** Three-pass test-writer + coder + pruner across 6 subprojects from the dual mailbox (1 PO + 10 specialist entries archived, mid-session yield-range RGR refit entry from specialist preserved for next-turn wave). W1 test-writers: created `nutrition/lettuce/app/spec.test.mjs` (5 REQs, +helpers) + `yield-range/spec.test.mjs` (32 tests covering REQ-117/118/171..175 + inherited, +helpers) + extended `nutrition/soil-contribution/spec.test.mjs` (B-routing pin + 7-key set pin). W2 coders: lettuce front-load DOM input retired + modal interpretation key stripped (REQ-176 / REQ-179); soil-contribution dispatcher fix for B → micros-foliar-routed; yield-range model layer rewritten (data.js / calc.js / model.js — two-regime integrator + throughput-bound annual yield + cap-basis switch); fertigation Block 7 drift renderer extended with NaMolybdate. W3 pruners: yield-range stragglers + sidedress cert mismatch (data.js:66 cert 4 → cert 3) + calendar-anchor sweeps tree-wide. `npm test` 259 → 316 / 0 (+57 green); `npm run check` 161/0 maintained at every gate. Two commits: W1+W2 (`e8728f9`) + W3-and-archive (this commit). **Owned-surface expansion (P-10, awaiting Guillaume ratification):** `nutrition/lettuce/app/page.html` + `app/operator/nutriment.js` touched — parallel to the 2026-05-15 ratification when `app/index.html` / `nutrition/render.js` / `scripts/check-recipes.mjs` were folded in; suggests folding `**/page.html` + `app/operator/**/*.js` into the persona's owned-glob list. **Spec-gap for PO:** `yield-range/app/spec.md` REQ-119 "Two inputs" diverges from the new 7-input model; page kept functional with hardcoded defaults; REQ-119 needs PO amendment to expose operator levers for `nurseryDays` / `fieldDays` / density / areas and surfaces for `annualYieldKg` + `bottleneckStage`. Detail entries from sub-agents follow below.
- **2026-05-17 — Pruner yield-range W2 straggler sweep.** Owned-surface comment-only edits across `yield-range/app/logic.js` + `yield-range/app/page.html` to retire legacy singular `daysToPotential` references in code comments + local variable + function parameter — renamed to `daysToTransplantPotential` per REQ-117 split (specialist 2026-05-17). 5 edits in `logic.js`: local-variable rename + function-parameter rename + docstring rewrite (`renderYieldRangeChart` contract) + `xMaximum` comment "// 49" → "// nurseryDays + fieldDays" + xTicks range comment update; 2 edits in `page.html` comment blocks (REQ-120 + REQ-121). **NOT touched** (intentional): the two operator-facing rendered French strings `· pic non atteint dans la fenêtre de 49 jours` + `Plein potentiel non atteint dans la fenêtre de 49 jours` — these are mandated verbatim by `yield-range/app/spec.md` REQ-121 + REQ-133, which still carry the legacy 49-jours framing. Editing the rendered string without spec-side rewrite would drift from spec — surfaced for specialist lane. **Surfaced for specialist (out of pruner lane):** (1) `yield-range/app/spec.md:41, 42, 69` still cite `daysToPotential` singular + "49 jours" rendered-string mandates — needs rename to `daysToTransplantPotential` + decision on whether "49 jours" stays (literal cap window from `TRAJECTORY_MAXIMUM_DAYS=49` repurposed-as-sanity-ceiling per REQ-118) or generalizes to the dynamic `nurseryDays + fieldDays` framing. (2) `yield-range/spec.md:126` REQ-118 prose carries "(coder lane)" parenthetical — coder cascade has landed, parenthetical now vestigial. (3) `yield-range/derivation.md:127` + `derivation.md:193` carry "49-day `TRAJECTORY_MAXIMUM_DAYS` window" + "50 entries from day 0 to day 49 inclusive" framing — both pre-date the two-regime extension; trajectory is now length `nurseryDays + fieldDays + 1` dynamic per REQ-118 (no longer fixed 50). Confirmed clean — code/HTML layer carries zero residual `daysToPotential` / `density^-1.585` / old cap-value framing outside test-pin + audit-trail pointers. `data.js:32` "rejected power-law extrapolation" line is a pointer to learnings.md (KEEP — load-bearing for future readers); `derivation.md:33-34` likewise pointer-only. `npm test` 316/0; `npm run check` 161/0 preserved.
- **2026-05-17 — Pruner sidedress / foliar / compost calendar straggler sweep.** `nutrition/tomato/sidedress-recipe/data.js:66-68` cert mismatch reconciled — FarinePlumes 0.75 comment `cert 4 — Sonneveld` → `cert 3 — Sonneveld 70-85 % feather-meal mid-band`, matching `derivation.md:84` cert 3 defense block. Foliar-recipe + compost-contribution calendar sweeps (`2026-05-12` / `petiole panel` / `2027-04` / `April 2027`) returned **zero** live hits in any pruner-owned code file (data.js / model.js / calc.js / app/logic.js across all three subprojects); all grep hits are audit-trail (learnings.md / done-folder / changelog / principles.md / spec.md historical amendment notes). **Surfaced (out of pruner lane):** (1) `app/index.html:484-489` `ACCEPTED_EXCESSES` T4.Mg / T5.Mg rationale prose cites "REQ-098 amended 2026-05-12 to drop compost subtraction" + "Audit-trail acceptance pending PA Taillon ack" — operator-facing string referencing a REVERTED amendment (REQ-098 restored 2026-05-15 per fertigation-recipe/learnings.md B1-REV cycle); specialist/PO lane to reframe. (2) `nutrition/tomato/plant-needs/data.js:321` carries calendar straggler `Tissue-test data lands ~2026-05-12 → push affected entries to cert 3` — out of my 3-subproject scope, plant-needs specialist lane. (3) `scripts/check-recipes.mjs:1955` and `nutrition/tomato/fertigation-recipe/data.js:43` both reference "2026-05-12 amendment" as historical calibration context — neither stale (both point at audit-trail-relevant cycle history), but neither in pruner-owned surface. `npm test` **316/0**; `npm run check` **161/0** maintained.
- **2026-05-17 — Coder tomato/fertigation-recipe Block 7 drift gauge 4-key extension.** `nutrition/tomato/fertigation-recipe/app/drift.js` Block 7 stored-vs-FP drift table fertigation section was enumerating only `K2SO4` + `MgSO4-7H2O`; extended to include `Solubore` (sourced from `STORED_RECIPE.tomato.fertigation[stage].borax` on the stored side, from `FP_RECIPE_T5.fertigation['Solubore']` / `computeStageRecipe(stage).solubore` on the FP side) and `NaMolybdate` (`storedFert.naMolybdate` / `FP_RECIPE_T5.fertigation['NaMolybdate']` / `fpStage.naMolybdate`) — matching the 4-key return shape of `computeStageRecipe` (REQ-098 / REQ-099 / REQ-154 specialist extension 2026-05-17 11:57) and the REQ-061 Mo carve-out routing Mo through fertigation. `calc.js` was already 4-key-aligned (computeStageRecipe returns `naMolybdate: 0.5`, wireFpFertigation propagates all four into FIRST_PRINCIPLES_T5_FERTIGATION + FP_RECIPE_T5.fertigation); no edits there. `npm test` 316/316; `npm run check` 161/0; REQ-098 / REQ-099 / REQ-154 all green. **Surfaces flagged for follow-up (no edit, outside owned surface):** (1) `nutrition/tomato/lib/recipe-math.js:504` MIX_ORDER `fertigation.tomato` is `['K2SO4', 'MgSO4-7H2O']` only — missing Solubore + NaMolybdate; (2) `nutrition/tomato/lib/recipe-math.js:635, 641-644` `computeRecipe` mapping still routes `B` / `Mo` through `foliar` instead of `fertigation` (pre-REQ-061-carve-out routing); (3) `nutrition/tomato/app/logic.js:421-428` Block 5 operator-facing fertigation table renders `K2SO4 + MgSO4 + conditional Solubore` — no NaMolybdate row in either mode; (4) `nutrition/tomato/app/logic.js:469-471` `fertSkipEls` still includes `'Mo'` in the "not in fertigation" list when `sb_fert_g > 0` — Mo IS fertigation-routed now (0.5 g/wk flat-floor); (5) `app/index.html:308-312` `FP_RECIPE_T5.fertigation` literal lists only 3 keys (`K2SO4`, `MgSO4-7H2O`, `Solubore`); runtime is 4-key after wireFpFertigation, but the source-of-truth declaration is stale; (6) `nutrition/tomato/fertigation-recipe/data.js:8` docstring says return shape is `{ kSulfate, mgSulfate, solubore }` (stale, missing naMolybdate).
- **2026-05-17 — Coder yield-range Wave-2 two-regime extension.** `yield-range/data.js`: `CANOPY_CAP_BY_PLATEAU` switched to geometric `{18:69, 24:52, 32:39, 50:25}`; `RGR_MAXIMUM_LETTUCE_NURSERY` 0.40 → 0.30; added geometry constants `FOLIAGE_HEIGHT_M=0.10`, `FOLIAGE_DENSITY_KG_PER_M3=82`, `FIELD_CANOPY_HEIGHT_M=0.18`, `FIELD_FOLIAGE_DENSITY_KG_PER_M3=55`, `LEAF_PROJECTED_AREA_M2_PER_G=0.00035`, `TRAY_FRAME_M2=0.1525`; `TRAJECTORY_MAXIMUM_DAYS=49` kept (now sanity ceiling on sum). `yield-range/calc.js`: `predictNurseryYield` extended to seven required inputs (`plateauSize, ledHours, nurseryDays, fieldDays, fieldDensityHeadsPerM2, nurseryAreaM2, fieldAreaM2`) — throws loudly on missing, no silent defaults (Karpathy SRP / surface assumptions). Two-regime integrator: day 1..nurseryDays+fieldDays; cap + DLI-share switch at boundary (`spacing_factor(d)` → `perPlantDliShareField(W, d)`); RGR/`f_light`/logistic step unchanged across boundary. New return shape: `nurseryCanopyCapG, fieldCanopyCapG, transplantWeightG, harvestWeightG, daysToTransplantPotential, daysToHarvestPotential, trajectory[{day,weight_g,regime}], annualYieldKg, bottleneckStage`; legacy `daysToPotential` + `canopyCapG` retired. Added pure helpers `fieldCanopyCapByDensity(d)` (REQ-173: `(1/d)×0.18×55×1000`) and `perPlantDliShareField(w,d)` (REQ-174: `clamp(0.40, 1.0, 1/(0.00035×w×d))`). `yield-range/model.js`: namespace gained the two new functions + five geometry constants. `yield-range/app/logic.js`: minimal patch — caller threads page-default placeholders for the five new inputs (`nurseryDays=28, fieldDays=21, fieldDensityHeadsPerM2=43, nurseryAreaM2=1, fieldAreaM2=1`) and reads `nurseryCanopyCapG` / `daysToTransplantPotential` from the new return shape; legacy chart-render path otherwise untouched. **Flag for PO follow-up:** `yield-range/app/spec.md` REQ-119 still pins exactly two operator inputs (plateau + LED-hours); the five new model inputs are wired with hardcoded placeholders at the app boundary until the spec extension lands. `annualYieldKg` + `bottleneckStage` are computed but not rendered (no spec surface yet). `npm test` 316/316 pass (was 284/316); `npm run check` 161/0 preserved.
- **2026-05-17 — Specialist yield-range REQ-115 RGR_max refit (challenger D1).** `RGR_MAXIMUM_LETTUCE_NURSERY = 0.40 → 0.30 d⁻¹` (conservative end of cross-cultivar butterhead seedling-RGR band 0.25-0.30 d⁻¹ from Wageningen / Hoogendoorn-line CE-chamber publications). Cert stays 3 per Guillaume's option-2 ruling but anchor shifts from back-calculated-to-d28-d35-target (rejected as circular: cap is the data anchor, RGR_max should predict timing under cap, not be tuned to hit a chosen timing) to cross-cultivar literature transferability under comparable regimes. Missing-doc gap named explicit per P-10 — no Salanova / butterhead RGR_max primary source in `nutrition/doc/` (inventory cross-checked: only fertigation oligo PDFs + lab analyses + product datasheets, no growth-physiology refs). `yield-range/derivation.md` REQ-115 section rewritten: integrator-output timing replaces "asymptotes between d28 and d35" aspirational framing; scratch trace at 50-cell / DLI = 28 / 16-LED / RGR = 0.30 → `daysToTransplantPotential = d44` (W = 0.95 × 25 = 23.75 g at d44; spacing-factor REQ-116 + f_light saturation REQ-115 drive the mid-cycle slow-down); per-tray: 32-cell `cap = 39` → d46, 24-cell ~d49, 18-cell past 49-day `TRAJECTORY_MAXIMUM_DAYS` window. Symmetric refinement triggers per P-03 — refit upward if Décembre best-case cohort lands < d44, refit downward if > d44; cert-ladder-override trigger for the day a Salanova / Rijk Zwaan primary source lands. `yield-range/learnings.md` gained "Rejected: back-calculated RGR_max from cap-asymptote target" — full audit trail (+82 % observed-stressed-to-assumed-best-case jump under 0.40 with no intermediate data point; re-integration under the rejected value shows even d28 boundary was aspirational — actual integrator-output lands d35 under 0.40, never d28). Consolidated refinement-triggers table restructured: back-calc-anchored cohort row split into two (symmetric RGR refit + cert-ladder override) + decoupled from REQ-112/172 cap refit. **REQ-115 spec body NOT amended** — pure normative formula carries no value, no day-count, no cert per project spec-discipline; literature cite + cert + missing-doc gap live in `derivation.md` (same structural mapping as B1's REQ-112 → REQ-172 carve). Operator-side knock-on (no code edits this turn): `Plein potentiel non atteint dans la fenêtre de 49 jours` annotation (REQ-121, `app/spec.md:42` / `app/logic.js`) fires more frequently — at 18-cell always, at low-LED 50-cell / 32-cell, at 24-cell on the boundary. **Coder cascade filed in team-leader inbox** (P-06): `yield-range/data.js:RGR_MAXIMUM_LETTUCE_NURSERY` value swap `0.40 → 0.30` + comment-block rewrite at `data.js:9-15` to retire the back-calc prose (specialist did not touch data.js). Re-running the integrator was done via read-only scratch script `/tmp/rgr-refit-scratch.mjs` that replicates calc.js logic byte-untouched. No verifier shape change — yield-range REQ-115 has no Node-side matcher (the `scripts/check-recipes.mjs` REQ-115 matcher binds to foliar-recipe BURN_CAP_BASE_G semantics, the same cross-cutting REQ-NNN collision pending PO renumbering). `npm run check` 161/0 baseline preserved. `npm test` — pre-existing test-writer-ahead-of-coder lag (NOT a D1 regression — spec/derivation/learnings-only turn cannot break calc/data): `yield-range/spec.test.mjs` (newly-landed REQ-117 split test expects `daysToTransplantPotential` / `daysToHarvestPotential` outputs from `predictNurseryYield`; `yield-range/calc.js` still exposes singular `daysToPotential`); `nutrition/lettuce/app/spec.test.mjs` (REQ-176 expects `nutr-l-frontload` retired from header card; `app/index.html` still has the input). Both pending coder cascade — already filed in earlier team-leader inbox entries (REQ-117/118 from 2026-05-17, lettuce app REQ-176 from PO's Block 2 bundle 2026-05-17).
- **2026-05-17 — Specialist yield-range REQ-112 / REQ-172 cap-basis switch (challenger B1).** `yield-range/spec.md` REQ-172 formula expanded: previously `cap_g = (0.1525 / plateauSize) × 820` (dimensionally off by 10× — `0.00305 × 820 = 2.5 g` at 50-cell, not the listed 25 g); now `area_per_cell × FOLIAGE_HEIGHT_M × FOLIAGE_DENSITY_KG_PER_M3 × 1000` with `FOLIAGE_HEIGHT_M = 0.10`, `FOLIAGE_DENSITY_KG_PER_M3 = 82` explicit (canopy mass-loading `h × ρ ≈ 8.2 kg/m²`). Cert breakdown restructured 50 → 3 / 32 → 3 (was 32 → 2; bumped because geometric is the conservative-physics floor one density-step from the breeder anchor) / 24 → 2 / 18 → 2. Refinement trigger gained 18-cell priority. `yield-range/derivation.md` REQ-112 section rewritten: power-law trace with stale values `{50:25, 32:50, 24:80, 18:120}` retired; breeder-anchored geometric basis promoted as canonical with explicit constants + per-tray cert read + 18-cell underprediction band (75-95 g vs predicted 69 g if `h ≈ 0.13-0.15 m`, `ρ ≈ 90-100 kg/m³`). Consolidated refinement-triggers table gained "First 18-cell cohort under best non-light conditions" row; Rijk-Zwaan-curves row reframed to target `h` / `ρ` as independent anchors. Extension-pending nursery-cap-basis bullet retired (now redundant with the live REQ trace; pointer added). `yield-range/learnings.md` existing "24 / 18-cell extrapolated" entry superseded in place with "Rejected: two-anchor power-law extrapolation for canopy cap" — full audit trail of Guillaume's 2026-05-17 ruling (+100 % vs +56 % uplift framing, non-physical past `f_light` saturation floor at DLI ≈ 22, fit coincides at one anchor and over-predicts elsewhere). **Coder cascade filed in team-leader inbox** (P-06): `yield-range/data.js:CANOPY_CAP_BY_PLATEAU` value swap `{50:25, 32:50, 24:80, 18:120}` → `{50:25, 32:39, 24:52, 18:69}` + comment block rewrite to retire the power-law prose (specialist did not touch data.js). No verifier shape change — `scripts/check-recipes.mjs` REQ-112 matcher binds to foliar-recipe semantics; yield-range REQ-112 / REQ-172 have no Node-side identifier-keyed matcher today. `npm test` 16/16; `npm run check` 161/0 baselines preserved.
- **2026-05-17 15:10 — Specialist tomato/sidedress-recipe hygiene pass.** `spec.md` Eco-luzerne 3-0.5-2 cert framing under the REQ-022 inheritance bullet reworded — gate is now stated as filesystem-conditioned (cert PDF filed under `nutrition/doc/eco-luzerne-3-0-5-2/`, directory does not yet exist) rather than "TBD pending Ecocert evaluation"; no calendar, no operator nudge. `derivation.md` — (a) Caveats Eco-luzerne entry mirrored: "directory does not yet exist as of last scan; filesystem-conditioned, no calendar"; (b) Mineralization-eff section gained explicit per-product cert defense block (FarinePlumes 0.75 cert 3 — Sonneveld 70-85% feather-meal N in 6-8 weeks at ≥18 °C; AlfalfaMeal 0.65 cert 2 — plant protein C:N ~14 vs ~7 feather meal, no Décembre track record; Actisol 0.60 cert 3 — composted manure pre-stabilized N) so a cold reader can defend each constant without re-reading Sonneveld; (c) "Single-product per call" caveat now cross-references the new `learnings.md` § "Rejected: blend optimization". `learnings.md` — new "Rejected: blend optimization (mixed-product per-stage dose)" entry: operator weighs one product per planche per week, mixing adds reliability risk, mass-balance trivially recovers blend via two summed runs if needed. Hard-zero T1/T2 cross-reference to `app/index.html:460-470` `ACCEPTED_EXCESSES` T1.N + T2.N entries **verified valid** (no edit needed). REQ-089 Mehlich-3 gate trigger (< 7 500 kg/ha Ca or base-saturation < 70 %) verified concrete. Cold-shoulder caveat verified non-duplicative (eff section pointer → Caveats item, 30-50 % number lives once). **CROSS-CUTTING surfaced inline:** `data.js:66` says `FarinePlumes 0.75 cert 4 — Sonneveld` while `derivation.md:84` says cert 3; coder lane to reconcile (favor cert 3 per derivation). No new REQs, no model.js / calc.js / data.js edits (P-06).
- **2026-05-17 11:57 — Specialist tomato/fertigation-recipe second-order hygiene pass.** Mo carve-out (REQ-061 amendment 2026-05-16) made load-bearing across spec / derivation / verifier. `spec.md` — Contract return-shape extended to `{ kSulfate, mgSulfate, solubore, naMolybdate }` (was two-key); REQ-098 statement distinguishes mass-balance branches (K / Mg / B per REQ-098 + REQ-155) from the flat-floor Mo branch (0.5 g/wk operator-weighing-anchored, NOT demand-driven); REQ-099 `FIRST_PRINCIPLES_T5` shape gains `NaMolybdate`; REQ-154 invariant covers all four products by construction. `derivation.md` — (a) inline active-channels-sum vs REQ-013 / REQ-014 envelope subsection added at T5 per-element table (bed-side 1.11× K / 1.18× Mg / 1.31× B; plant-side 1.00× / 1.00× / 1.05× after uptake-factor; T1-T2 Mg overshoot 2.43× framed as compost-pool surplus by design); (b) Mo algorithmic-detail block (flat-floor rationale + REQ-154 FP-pin coverage + default uptake factor 1.00 for Mo documented); (c) cert table B row reframed — stale "PA Taillon recommendation" prose replaced with mass-balance cert-stack reasoning post-B2-REV. `scripts/check-recipes.mjs` — REQ-154 verifier extended: NaMolybdate added to equality check + FP propagation loop now four-key. Cross-reference verifications (no edits needed): REQ-141 path-1 rejection pointer aligns with `soil-contribution/learnings.md:9` heading; PA Taillon FP-vs-STORED audit-trail clean across subproject (only FP-target framing); Solubore product-identity grep zero live hits; refinement triggers fire only on tissue / soil / SME / lab / inventory (zero STORED-vs-FP). `npm run check` Node verifier **149/0** (specialist-edited block).
- **2026-05-17 14:30 — Specialist yield-range hygiene pass.** `yield-range/spec.md` — REQ-116 statement tightened to name the linear interpolation between `d = 14` and `d = 28` (the linear shape was implicit in `data.js:NURSERY_SPACING_PACKED` but absent from the normative claim, making the spec under-determined); Specialist note 2026-05-16 reframed condition-based (settled inputs enumerated — DLI formula preserved, breeder-anchored caps, field DLI share curve; REQ landing now gated on Guillaume's marketability-on-head-size commercial call per P-08); header block updated to cite `derivation.md` + `learnings.md`. New scaffolds: `yield-range/derivation.md` (live REQ trace + consolidated refinement-trigger table covering n ≥ 5 cohort refit / pyranometer dataset / poly-film replacement / per-plant PAR / Rijk Zwaan curves) and `yield-range/learnings.md` (rejected Hochmuth root-cap, rejected senescence branch, rejected LED-only DLI, rejected seasonal lookup). Broken cross-reference in spec.md header (cited `derivation.md` previously didn't exist) fixed. No model.js / data.js / calc.js touched (P-06 lane). **CROSS-CUTTING surfaced inline:** REQ-112 / REQ-113 / REQ-114 / REQ-115 / REQ-116 are double-claimed across `yield-range/spec.md` AND `nutrition/tomato/foliar-recipe/spec.md` + `nutrition/tomato/app/spec.md`; the `scripts/check-recipes.mjs` matchers for those IDs bind to foliar-recipe semantics, not yield-range. Ledger only starts at REQ-154 — predates the ledger. Renumbering is PO scope. Filed in team-leader inbox for orchestrator routing to product-owner.
- **2026-05-17 11:57 — Specialist soil-contribution REQ-145 micros-gap close.** `nutrition/soil-contribution/spec.md` REQ-145 extended in place: new `micros-foliar-routed` Renders block declared, covering Fe / Mn / Zn / B / Cu (all measured banks, plant-available fraction throttled by pH 7.4 lockout — cation micros via sulfate-metal curve, B via Fe/Al oxide adsorption + Ca-borate complexes); active channel = foliar per REQ-061 cascade (CHANNEL_ROLE `B: {foliar:0.5, passive:0.5}`, Fe/Mn/Zn/Cu: `{foliar:1.0}`). Spec count 6 → 7 keys; spec body now carries an explicit element-to-key dispatcher mapping table so future readers don't need to grep `nutrition/tomato/app/logic.js`. **Removed `B-fert-routed` key** added earlier today by a parallel session — factually wrong; B routes via foliar (Solubore in the foliar spray, `nutrition/tomato/foliar-recipe/data.js:131`, `stored.js:19`), not fertigation. Verifier `expectedKeys` array in `scripts/check-recipes.mjs` updated to match (6 → 7). **Coder follow-up filed in team-leader inbox:** dispatcher branch `else if (element === 'B') interpretationKey = 'B-fert-routed'` in `logic.js:209-210` must move B into the `Fe / Mn / Zn / Cu` `micros-foliar-routed` branch; until then runtime modal for B reads empty (SPEC_STRINGS lookup miss). Stale comment `nutrition/tomato/foliar-recipe/data.js:66` ("B single-channel via fertigation") also flagged for coder fix. No new REQ claimed — extension is bytes-only, normative claim unchanged.
- **Stage 7 — extract all remaining inline JS from app/index.html.** Functions, page state, soil mass-flow, Phase 1 recipe chemistry, Bilan supply orchestrator, Block 7 drift renderer, cross-crop nutriment dispatcher, footer init — every JS body moved into deepest-sensible subproject partials; `app/index.html`'s main `<script>` block collapses to dependency-ordered `@include` directives + four small ordered data constants (FP_RECIPE_T5 / RECIPE_INPUTS / ACCEPTED_DEFICITS / ACCEPTED_EXCESSES) + a REQ source-grep anchor comment. New files (7): `app/operator/state.js` (37 lines — TOMATO/LETTUCE bed-area constants + LETTUCE recipe + let currentCrop/Stage/Page/Vigor); `app/operator/recalc.js` (149 lines — `recalc` + `getRatio` / `getMultK` / `getMultMg` / `getTotalArea` / `getBedArea` + `SOLAR_BY_WEEK` / `SOLAR_BY_MONTH` + `getSolarRad` / `getWeekNumber` / `getStageFromWeek` / `autoStage` / `recalcNursery`); `app/operator/nutriment.js` (84 lines — `nutrCrop` state + `buildNutriment` cross-crop dispatcher + `setNutrCrop` + `captureCurrentSnapshot` browser-console helper + Bilan input-listener wiring + footer `fetch('history.json')` init **guarded with `typeof fetch === 'function'`** since the listener block now runs before `operator/logic.js` (originally ran after) and a raw fetch throw aborts script execution before `const PAGES` initializes in jsdom env); `nutrition/soil-contribution/integrator.js` (155 lines — `SME_TOMATO_PPM` + `weeklyMassFlowL` / `transpirationFactor` / `N_MINERALIZATION_BY_MONTH` / `nMineralizationFactor` / `soilWeeklyAvailable`, integrator-side mass-flow path since reads `getSolarRad` + currentCrop, separate from the pure soil-contribution/calc.js bank model); `nutrition/tomato/lib/recipe-math.js` (568 lines — Phase 1 chemistry library: `PRODUCT` catalog + `CHANNEL_ROLE` + `PH_RESPONSE` curves + `foliarPhResponse` + `KSP_PAIRS` / `KSP_SAFE` + `TAG_INCOMPATIBILITIES` / `TAGS_INERT` + `INCOMPATIBLE_RECIPES` + `MIX_ORDER` + `effectiveEfficiency` + `predictedCE` + `predictedTankPh` + `passiveSupplyMassFlow` + `computeRecipe` + `validatePhase1ModelCoverage` IIFE); `nutrition/tomato/fertigation-recipe/app/drift.js` (109 lines — `renderPhase1Comparison` Block 7 stored-vs-FP drift table renderer); `nutrition/tomato/app/supply.js` (270 lines — `nutrStage` + `nutrRecipeMode` page-local state + `LUXURY_FACTOR` + `calculateNutritionSupply` orchestrator with REQ-116 FP foliar-gap chain + `statusFor`). Dead code deleted: `fmtMg` (zero callers; `formatMg` in `nutrition/lib/format.js` is the live API). Kept inline at integrator level (ordered data declarations, not function bodies): `FP_RECIPE_T5` (cross-channel — fertigation/sidedress/foliar — mutated by `wireFpFertigation` in fertigation-recipe/calc.js at boot), `RECIPE_INPUTS` (fertigation-recipe input config but threaded through model layer in dependency-ordered position), `ACCEPTED_DEFICITS` / `ACCEPTED_EXCESSES` (Bilan policy lists). Source-grep anchor comment added at top of `<script>` block listing `computeStageRecipe(stage)`, `STORED_RECIPE.tomato.{foliaire.A,sidedress[stage]}`, `BIOMASS_DEMAND[stage]`, `TOMATO_FRUIT_EXPORT[element]`, `window.FoliarRecipeTomato.{computeFoliarSupply,computeFoliarRecipeForGap}`, `let nutrRecipeMode = 'fp'`, `'nutr-foliar-spray-count'` / `'nutr-foliar-surfactant'` so REQ-004 / REQ-106 / REQ-113 / REQ-139 source-grep tests still match after the carve (test-helpers.mjs's `readAppIndexHtml()` reads app/index.html source, not the assembled artifact). 7 new `@include` directives wired into a dependency-ordered chain (state → libs → recalc → renderers → diagnostic → model layer + STORED + FP_RECIPE_T5 + RECIPE_INPUTS/ACCEPTED_* + recipe subprojects + soil-contribution + integrator → recipe-math + plant-needs calc/model + yield-range → supply + drift → pourquoi/format/render-gap-grid → per-crop nutriment builders → cross-crop dispatcher + footer → operator chrome init). STORED_RECIPE.{tomato.fertigation,foliaire,sidedress} + RECIPE_HISTORY untouched (audit trail unaffected). Window-global identifier surface preserved via classic-script function hoisting + operator/logic.js's explicit `window.<name>` assignments. `app/index.html` 2341 → 625 lines (-1716, -73%). `dist/index.html` 10287 → 10309 lines (+22, marker hygiene + anchor comment). No DOM ID changes, no behavior change. `npm test` **257/0**; `npm run check` **161/0**. Node v24.15.0.
- **2026-05-17 — Coder REQ-145 soil.B dispatcher fix.** `nutrition/tomato/app/logic.js:209-211` — `else if (element === 'B') interpretationKey = 'B-fert-routed'` branch deleted; B folded into the `Fe / Mn / Zn / Cu` `micros-foliar-routed` branch (now 5-element OR). Stale comment `nutrition/tomato/foliar-recipe/data.js:66` rewritten — "B single-channel via fertigation" replaced with two-channel framing (foliar Solubore + passive compost/sidedress) per CHANNEL_ROLE `B: {foliar:0.5, passive:0.5}`; comment-only, no code touched on that line. `B-fert-routed` grep across `*.js`/`*.md` returned no live consumer outside the deleted dispatcher branch — only historical references in changelog narrative + team-coordination inbox. REQ-145 soil.B test green (`spec.test.mjs:430`); REQ-145 7-key spec ↔ verifier agreement test green. Baseline test failures 45 → 42 (3 closed by this edit, no regressions; remaining 42 are pre-existing lettuce-demand + yield-range coder-cascade lag from specialist edits earlier today). `npm run check` **161/0** (verifier untouched).
