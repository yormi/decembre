# Décembre — Changelog

Append-only log of substantive changes (recipe edits, REQ additions, model architecture, page restructuring). Read at start of any session touching app state, especially under parallel Claude sessions.

Format: `YYYY-MM-DD HH:MM — brief description`. One line per change. Most recent at top. Keep ~10 entries; older history lives in `git log`.

## 2026-07-12
- **2026-07-12 — Laitue fertigation : nouveau canal STORED audité + Bore 0,7 → 1,3 g (/retire-recipe).** La recette fertigation laitue devient un canal d'audit de première classe, à parité avec les canaux tomate/semis. Nouveau `nutrition/lettuce/protocol/fertigation/stored.js` = `STORED_RECIPE.lettuce.fertigation` (miroir plat de `LETTUCE_FERTIGATION_RECIPE`, `app/core/state.js` — K₂SO₄ 400 g + Solubore 1,3 g / cuve hebdo, bloc 136,8 m²). Boot stub `window.STORED_RECIPE` + `@include` ajoutés. Rendu Historique : `CHANNEL_META.lettuce` (🥬), `diffFlatChannel` paramétré unité (`/L` semis, `g` laitue), carte + `liveStored` laitue. Deux entrées `RECIPE_HISTORY` : **2026-07-12** snapshot `{Potassium:400,Bore:0.7}` → diff Bore 0,7→1,3 (export B au prorata du rendement ~180 kg/sem ; K plafonné salin) ; **2026-07-11** snapshot 7-produits `{K:200,Mg:50,Fe:15,Mn:5,Zn:2.7,Bore:6,Cu:1}` → diff réduction B+K (Mg/Fe/Mn/Zn/Cu retirés, K 200→400, Bore Borax 6 g→Solubore, N front-load). Skill `/retire-recipe` étendu (canal laitue, formes de snapshot, step 1/3). Miroir tenu à la main comme le semis (pas de vérificateur de sync). Solubore Ecocert CAN/CGSB-32.311. `npm run check` : **159/0 ✓**.
- **2026-07-12 — `strategy/` remonté au niveau racine, sous-dossier par culture.** `nutrition/tomato/strategy/` → **`strategy/tomato/`** ; `nutrition/lettuce/strategy/` → **`strategy/lettuce/`** (git mv). Nouveau `strategy/CLAUDE.md` (index, calqué sur `domain/CLAUDE.md`) + en-têtes des deux `CLAUDE.md` culture recâblés. **Réfs sortantes repointées en chemins absolus repo-root** — au passage, dette pré-existante corrigée : les liens tomate assumaient encore la base `nutrition/strategy/` (cassés d'un cran, `../tomato/…`) et pointaient vers l'ancienne arbo pré-réorg (`soil-ph/model/`, `sidedress-recipe/model/`) → tous redirigés vers `nutrition/tomato/domain/…` ; côté laitue `../domain.md`/`../learnings/`/`../doc/` (cassés depuis que domain/ a un niveau) → `nutrition/lettuce/domain/…`, SME pdf → `nutrition/doc/`, protocole salinité partagé → `nutrition/protocol/`. `imageBase` runtime des 2 `diagnostic-data-*.js` repointé en relatif depuis la nouvelle position. Réfs internes (`../goal.md`, `../problem.md`) intactes. 13 fichiers déplacés. Zéro changement de comportement (docs .md hors-vérificateur). `npm run check` : **159/0 ✓**. `team/` non touché.
- **2026-07-12 — Semis re-logé sous laitue : n'est plus un pair de culture.** Décision Guillaume (option B) : le semis est le **stade plantule de la laitue**, pas une culture sœur — le spec le dit déjà (`plant-needs` = « lettuce nursery only », tout est clé `LETTUCE_NURSERY_*`). `nutrition/nursery/` **supprimé**. Répartition selon la coupe science/geste/présentation déjà en place : **modèle** (`plant-needs/`, `substrate-contribution/`, `fertigation/{data,calc,model,spec,derivation,learnings}`, `doc/`, `learnings/`) → `nutrition/lettuce/domain/nursery/` ; **protocole** (`fertigation/stored.js`, pour-through/pulse/salt-flush `.md`, `learnings/`) → `nutrition/lettuce/protocol/nursery/` ; **prose physique crop-agnostique** (`domain.md` : conditions de germination + coût substrat) → **racine `domain/propagation.md`**, aux côtés de `greenhouse.md`/`spinach.md`. Recâblage : 9 `@include` + commentaire de `app/index.html`, ~12 `readFileSync`/`// Spec:`/`// Derivation:` du vérificateur + code déplacé (sed préfixe), en-tête `// Derivation:` de `app/core/recalc.js`, index specs (`spec.md`, `nutrition/spec.md`), `soil-contribution/spec.md`, `nutrition/doc/CLAUDE.md`, skill `/retire-recipe`, persona `spec-pruner`, réfs relatives (`../protocol/`, `../domain.md`, glob `.../`) réécrites en absolu, index `domain/CLAUDE.md` (ajout propagation/greenhouse/spinach). Note : `plant-needs/spec.md` était supprimé de l'arbre de travail (pré-existant, non commité) → restauré depuis HEAD avant le `git mv`. Zéro changement de comportement. `npm run check` : **node 147/0, bash 12/0 — 159/0 ✓** (identique au pré-réorg). `team/` (logs datés) non touché.
- **2026-07-12 — `greenhouses/domain.md` + `spinach/domain.md` → racine `domain/`.** Deux domaines mono-fichier collabés dans `domain/` (`greenhouse.md`, `spinach.md`) ; dossiers vides retirés ; 2 pointeurs croisés (`domain.md`, `yield-range/domain/light/domain.md`) repointés.

## 2026-07-11
- **2026-07-11 — Fertigation opérateur : niveau `/tomato/` retiré → `app/operator/nutrition/fertigation/` à plat.** La page fertigation est **une seule page multi-culture** (un toggle, un dispatcher `buildSteps` qui branche vers `buildLettuceSteps` / le bloc semis, un conteneur DOM) — pas trois pages. Décision Guillaume : plutôt qu'un split 4 voies artificiel (nav/tomato/lettuce/seedling — les cultures ne sont pas des modules montables indépendamment), le dossier tombe à plat (`logic.js`, `drift.js`, `page.html`, `page.css`, `user-stories.md` directement sous `fertigation/`). Déplacement pur, zéro changement de code. `app/CLAUDE.md` : convention « page multi-culture partagée → pas de niveau `<crop>` ». `npm run check` : 159/0.
- **2026-07-11 — `lib/` racine déplacé sous `app/lib/`.** Les 4 primitives transversales (`global.css`, `admin.css`, `spec-strings.js`, `sun.js`) rejoignent `app/lib/` (à côté de `tomato.css`) ; plus de `lib/` à la racine. `nutrition/lib/` (helpers nutrition) reste en place. Recâblage : 5 `@include` + 1 commentaire ; `app/CLAUDE.md` MAJ. `npm run check` : 159/0.
- **2026-07-11 — UI regroupée sous un seul `app/` à la racine, calquée sur la navigation (operator vs admin).** Chaque `app/` de sous-projet (`nutrition/{tomato,lettuce,nursery}/app/`, `yield-range/app/`) remonte dans l'`app/` racine, réorganisé par `rôle / section / fonction / culture`. **`app/core/`** = chrome partagé (routeur `setPage`/`setCrop`/`toggleAdmin`, état de session + constantes serre, recalc, dispatch Bilan, barre d'outils) — anciennement `app/operator/`, renommé pour libérer `operator/`. **`app/operator/`** = pages opérateur (`nutrition/fertigation/tomato`, `nutrition/foliar/tomato`, `nutrition/sol/tomato` + carte AMF, `effeuillage/lettuce`, `lumiere/lettuce`). **`app/admin/`** = pages admin (`nutrition/bilan/{tomato,lettuce,nursery}`, `nutrition/builder/{fertigation,foliar,sidedress,plant-needs}/tomato`, `nutrition/historique`, `irrigation/{tomato,lettuce}`, `diagnostic`, `week`, `rendement`). Le `shell/` tomate se **dissout** : bilan→admin, `sol/`→operator, `irrigation/`→admin. `app/lib/tomato.css` (ex `tomato/app/lib/styles.css`). Les sous-projets gardent `domain/`+`protocol/`+`strategy/` — plus de dossier `app/`. **Recâblage** : ~40 `@include`, chemins tableau du vérificateur (`bilan/tomato/logic.js`), profondeur repo-root de 2 `test-helpers.mjs` (bilan tomato 4→5, bilan laitue 3→5), imports croisés des 5 tests app (builders → `../../../bilan/tomato/`, foliar operator → `../../../../../nutrition/tomato/domain/…`). `app/CLAUDE.md` réécrit. ~53 fichiers déplacés. Zéro changement de comportement. `npm run check` : **159/0**. `node --test` app : 2 rouges pré-existants sémantiques (`STORED.foliaire` vide, pH prédit « — » non livré), zéro `MODULE_NOT_FOUND`. `team/` non touché.
- **2026-07-11 — Yield-range : réorganisation en domain / app (même patron).** `yield-range/` : la science racine remonte sous **domain/** (`calc.js`, `data.js`, `model.js`, `seedling-thinning.js`, `derivation.md`, `domain.md`, `spec.md`, `spec.test.mjs`, `test-helpers.mjs`, `learnings/`, `doc/`) ; le sous-sujet `light/` se scinde comme une recette — `light/domain.md` → **domain/light/**, `light/operator/` → **app/light/operator/**. **app/** existait déjà (inchangé). **Ni protocol/ ni strategy/** — yield-range est un modèle de prédiction, sans geste-ferme ni cap-saison. Cas simple : triplet `data/calc/model` conservé, `test-helpers.mjs` ne lit que des fichiers frères (déplacés ensemble → zéro recâblage de profondeur), vérificateur parcourt `yield-range/` récursivement (auto-adapté). 25 fichiers déplacés (git mv). Recâblage : 6 `@include` de `app/index.html` + en-têtes commentaires de `light/operator/logic.js`. Zéro changement de comportement. `npm run check` : **159/0** ; `spec.test.mjs` passe. `team/` non touché.
- **2026-07-11 — Semis : réorganisation en domain / protocol / app (même patron que laitue/tomate).** `nutrition/nursery/` : la science remonte sous un nouveau **domain/** (`domain.md`, `plant-needs/`, `substrate-contribution/`, le triplet modèle `fertigation/{data,calc,model,derivation,spec,learnings}`, `learnings/`, `doc/`), et `fertigation/procedure/stored.js` passe en **protocol/fertigation/stored.js**. **app/** et **protocol/** existaient déjà (inchangés à part l'ajout). **Pas de strategy/** — aucun contenu goal/problem/sg côté semis. Plus simple que la tomate : semis garde le triplet `data/calc/model` (pas de renommage `recipe.js`/`contribution.js`), aucun fichier de test `.mjs`, donc zéro recâblage de profondeur test-helper. 35 fichiers déplacés (git mv). Recâblage : 9 `@include` de `app/index.html`, 6 `readFileSync(join(…))` du vérificateur (plant-needs + substrate-contribution), en-tête `// Derivation:` de `app/operator/recalc.js`, 2 pointeurs croisés em-dash (`nutrition/spec.md`, `soil-contribution/spec.md`). Zéro changement de comportement. `npm run check` : **node 147/0, bash 12/0 — 159/0 ✓** (identique au pré-réorg, du premier coup). `team/` non touché.
- **2026-07-11 — Tomate : réorganisation en domain / protocol / strategy / app (pilote après laitue).** `nutrition/tomato/` éclaté en quatre dossiers racine, mêmes préoccupations que la laitue (commit `2c9ae9a`) mais appliquées à du code, pas du markdown. **domain/** = science : `domain.md`, `spec.md`, `channel-role.js`, `plant-needs/`, `soil-ph/` (aplatis), `doc/`, `learnings/`, + le `model/` de chaque recette aplati en `domain/<recette>/`. **protocol/** = gestes ferme : le `procedure/` (stored.js/steps.js) de chaque recette + les `.md` de `strategy/protocol/` remontés. **strategy/** = cap saison : goal/problem/sg1-3/CLAUDE. **app/** = UI : `shell/`, `lib/`, `operator/`+`builder/` de chaque recette. Recettes renommées court (`fertigation-recipe`→`fertigation`, `foliar-strategy`→`foliar`, `sidedress-recipe`→`sidedress`, `amf-drench`→`amf`). **Écart assumé** : `foliar` garde un niveau `model/` sous `domain/foliar/` (unique à avoir un spec d'agrégat au-dessus du modèle → évite une collision `spec.md`) ; les deux autres aplatissent. ~126 fichiers déplacés (git mv, historique préservé). Recâblage : ~40 lignes `@include` de `app/index.html`, imports des 13 `.mjs` de test, profondeur repo-root de 3 `test-helpers.mjs` (`app/shell` +1, `domain/foliar/model` +1, `domain/` +1), 4 chemins tableau `join(…,'shell','logic.js')` du vérificateur → `app/shell`, pointeurs croisés em-dash (specs nutrition/soil-contribution, en-têtes `// Derivation:` du code déplacé). Zéro changement de comportement. `npm run check` : **node 147/0, bash 12/0 — 159/0 ✓** (identique au pré-réorg). `npm test` (hors-porte) garde ses 6 rouges pré-existants — tous sémantiques (`structuredClone` absent, `STORED.foliaire` vide depuis le retrait des sprays, DOM Wave 2 non livré), zéro `MODULE_NOT_FOUND`. Restent volontairement inchangés : journaux datés `team/` (trace d'audit) + tableaux « implementation map » pré-périmés (`calc.js`/`model.js` d'un carve antérieur).

_Older entries (≤ 2026-06-13) live in `git log`._
