# Décembre — Changelog

Append-only log of substantive changes (recipe edits, REQ additions, model architecture, page restructuring). Read at the start of any session that will answer questions about app state, especially when running parallel Claude sessions.

Format: `YYYY-MM-DD HH:MM — brief description`. One line per change. Most recent at the top.

## 2026-05-13

- 09:57 — **Wave 3 pruner — REQ-016 ghost-reference sweep.** Four sites edited autonomously per P-02 (vestigial/ghost references → delete, no surfacing): (1) `nutrition/tomato/app/page.html:127` Block 8 container comment `(REQ-016)` → `(REQ-153)` (drift gauge is now governed by REQ-153); (2) `nutrition/tomato/app/logic.js:695` `Phase 1 model — drift detection (REQ-016)` → `(REQ-153)`; (3) `app/index.html:4241` misattributed `// REQ-016 — Compute the recipe a channel SHOULD use…` → `// Compute the recipe a channel SHOULD use…` (REQ-016 was the drift gauge, not the FP recipe-compute function); (4) `yield-range/app/logic.js:83-86` `(per REQ-016 in the tomato Block 7 drift gauge): ≤5%, ≤30%, and >30%.` collapsed to `Tier thresholds: ≤5% (green), ≤30% (yellow), >30% (red) — same green/yellow/red palette signals "near optimum / partial / off-band".` per PO-153 suggested rewrite. **Left alone (audit trail / out of scope):** `scripts/check-recipes.mjs:1495` (verifier retirement record), `app/index.html:3381` (RECIPE_HISTORY REASON string), `yield-range/app/spec.md:137` (PO-scope, held for Guillaume), all `team-coordination/` + `working files/` mentions. Post-sweep `grep -rnE "REQ-016\b"` returns 3 sites only, all intentional. `npm run build && npm test`: 178/178/0; `npm run check`: 114/0 (89/94 wired).
- 09:54 — **Wave 2 coder — REQ-153 drift ratio flipped to FP ÷ Stockée.** `renderPhase1Comparison` in `app/index.html` (lines 4447-4473) — `colorOf(stored, fp)` now computes `fp / stored` (was `stored / fp`); `row(...)` renders `(fp / stored * 100).toFixed(0) + ' %'` (was `(stored / fp).toFixed(2) + '×'`); sentinels mirrored: `stored === 0 && fp === 0 → '—'` (unchanged), `stored === 0 && fp > 0 → '∞'` (was `fp === 0` branch), `stored > 0 && fp === 0 → '0 %'` (new — FP target zero, stored non-zero). 100 % now means parity per REQ-153. `npm run build && npm test`: 178/178/0; `npm run check`: 114/0 (89/94 wired — REQ-153 still deferred pending Wave 3 / team-leader verifier wiring). Scope-confined to `app/index.html`; no spec / test / STORED_RECIPE touch.
- 08:19 — **REQ-153 filed in team-leader inbox.** Missed on the original PO turn; without it no test-writer / coder / pruner wave would auto-start for the new REQ. Entry in `team-coordination/team-leader/inbox.md` names `nutrition/tomato/app` as the touched subproject, suggests test-writer + coder + pruner waves. Surfaces a process gap: every PO spec change needs both `requests.md` (when domain-specialist work is implied) AND `team-leader/inbox.md` (always — drives test/code/prune scheduling). Originally only the specialist queue was updated; the dual-queue pattern is now documented in CLAUDE.md per today's earlier `team-coordination/` layout entry.
- **PO-153 hand-off refreshed.** Original brief pointed the implementer at `nutrition/tomato/app/logic.js:701` for the ratio flip; that's only the call site — `renderPhase1Comparison` body lives at `app/index.html:4394`. Refresh corrects file:line targets (4450, 4466, 4449 sentinel, 4465 row guard), drops the speculative "express bands as `|ratio − 1|`" caveat (already done at line 4451), and expands the retired-REQ-016 cleanup list from 2 sites to 4: adds `app/index.html:4241` (misattributed `// REQ-016` on a recipe-compute fn) and `yield-range/app/logic.js:83` (3-tier-convention pointer). Two audit-trail mentions (`app/index.html:3381`, `scripts/check-recipes.mjs:1495`) explicitly stay. Fifth ghost — `yield-range/app/spec.md:137 — Mirrors the project's 3-tier convention (REQ-016)` — flagged as PO-scope, held for Guillaume's call.
- **Block 8 drift ratio FP / Stockée pinned (REQ-153).** New PO-scope spec entry committed to `nutrition/tomato/app/spec.md`: the « Recette stockée vs calculée (drift) » block on the Tomato Nutrition admin page (FP mode, T5) must render `recette premiers principes ÷ recette stockée` for every element (currently the inverse). 100 % = parity, > 100 % = stored under-supplies vs FP, < 100 % = over-supplies. Replaces the orphan'd direction contract that previously rode on retired REQ-016. Implementation + verifier wiring + P-02 cleanup of the stale `(REQ-016)` code comments queued to specialist as PO-153. No code/data/model edit this turn.
- **team-coordination/ layout documented in project `CLAUDE.md`.** New section surfacing the two recurring file patterns: (a) **producer ↔ consumer queue** — `<work>.md` ↔ `<work>-done.md` paired files where producer appends, consumer cuts to archive and appends an outcome/verdict block; live instances tabled (`requests.md` ↔ `requests-done.md` for challenger ↔ specialist, `team-leader/inbox.md` ↔ `team-leader/inbox-done.md` for PO+specialist → team-leader). (b) **persona-local principles** — one `<persona>/principles.md` per persona, monotonic `P-NN` entries. Makes the cross-persona handshake convention discoverable from CLAUDE.md instead of buried in individual persona files.
- **REQ reference style codified for all personas.** New `## REQ reference style` section in project `CLAUDE.md`: in chat with Guillaume, in coordination files, in commit messages, and in `derivation.md` / `learnings.md` prose, always lead with a concise description and put the REQ number in parens — `<description> (REQ-NNN)`, never bare. Carve-outs: `spec.md` headings keep `## REQ-NNN — <statement>` (structural); verifier matchers (`header('REQ-NNN ...')`) and code pointers (`// REQ-082`) stay bare. All 8 persona files (`coder`, `context-coherence`, `model-challenger`, `plant-nutrition-specialist`, `product-owner`, `spec-pruner`, `team-leader`, `test-writer`) gained a Style-section reinforcement of the rule. PO's prior "no REQ-NNN talk in the question" split into (a) operator-vocab question phase = no numbers, (b) post-proposal references = description + parens. Codifies memory `feedback_req_expand.md` as persona-explicit.
- 07:35 — **Vestigial / ghost-reference sweep per spec-pruner P-02 (just captured).** Four items pruned in one pass: (1) `demandTotal` removed from `nutrition/tomato/plant-needs/model.js` + REQ-083 public-API table + 3 tests in `spec.test.mjs` + verifier shape-check in `scripts/check-recipes.mjs` — zero production callers, only tests + verifier were pinning it; (2) `_surfactant` parameter dropped from `burnCapG` signature in `nutrition/tomato/foliar-recipe/data.js`, REQ-115 spec body updated to `burnCapG(el)`, call site in `calc.js` updated, surfactant-invariance test in `spec.test.mjs` simplified to single equality (param was inert per C3); (3) `FOLIAR_AREA_M2` ghost reference in `nutrition/tomato/foliar-recipe/derivation.md:250` replaced with the live `BURN_CAP_BASE_G` / `burnCapG(el)` symbols + an explicit "area computed inline in calc.js" note; (4) 14 `// stable —` annotation comments stripped from `nutrition/tomato/app/logic.js` (the markers were inert audit tags from the retired REQ-060 pattern; underlying rendered strings kept pending future auto-derive specification). `npm test` 177/177/0 (4 fewer assertions following the demandTotal + surfactant-invariance test removals; all targeted by the pruning); `npm run check` 114/0 (89/93 wired). Net: −1 function, −1 param, −1 ghost ref, −14 inert comments.
- 07:25 — **Wave 3 tomato/app pruner — Hypothèses du calcul card retired.** `nutrition/tomato/app/page.html` lines 144-159 deleted: the "Hypothèses du calcul (réf.)" card was 11 prose bullets describing the mass-balance framework, fruit-export derivation, biomass model, compost residual, sidedress program, fertigation formula, foliar coverage, surface area, recipe sources — exactly the "intro prose / hypothèses bullets / escape hatches" forbidden by `feedback_no_unspecced_narrative.md`. No spec tied to it, zero test reaches in `spec.test.mjs` or `scripts/check-recipes.mjs`, zero callers in app/index.html or nutrition/. `npm run build && npm test && npm run check` all green (181/0 + 114/0). No spec/REQ change; no calc/data/model edit; no `STORED_RECIPE` / `RECIPE_HISTORY` touch.
- 07:20 — **Fertigation derivation.md refreshed post-REQ-098 amendment.** Wave 3 fertigation pruner findings cleared: mass-balance derivation + JS snippet in `nutrition/tomato/fertigation-recipe/derivation.md` rewritten to match the no-compost-subtraction policy (REQ-098 amended 2026-05-12); per-stage T1-T5 table recomputed from live constants (T5 kSulfate 5 322 g, mgSulfate 3 319 g — drift framing vs PA Taillon anchor 5 167 / 1 379 now explicit). The "Policy vs implementation drift" section + prior compost-credited JS body + reconciliation history moved to `nutrition/tomato/fertigation-recipe/learnings.md` under "Compost subtraction in K/Mg mass-balance — retired 2026-05-13". `npm test` 181/0 · `npm run check` 114/0 — green. No spec/REQ change; no calc/data/model edit.
- 07:16 — **REQ-105 ⚠ message wired (Path A, reconcile spec-vs-impl drift).** `nutrition/tomato/app/page.html` gains `<div id="nutr-light-ceiling-warning">` inside the ceiling card (hidden by default); `nutrition/tomato/app/logic.js` toggles its display + populates `innerHTML` with `⚠ Cible X kg/m²/sem au-dessus du plafond lumière (Y à Z J/g) — atteignable seulement si la conversion lumière→fruit est meilleure que l'hypothèse.` only when `target > ceiling`. Pairs with the existing orange-bg color flip so both visual signals appear together. New REQ-105 behavioral test in `nutrition/tomato/app/spec.test.mjs` exercises target=3 (over) → ⚠ visible, target=0.1 (under) → empty + display:none. `npm test` 181/181/0, `npm run check` 114/0 (89/93 wired). No spec/REQ change — implementation now matches REQ-105 statement.
- **Team-leader Phase −2 auto-commit.** New phase ahead of Phase −1: leader auto-commits its **owned surface** (`**/*.test.mjs`, `**/test-helpers.mjs`, `**/calc.js`, `**/model.js`, `**/data.js`, `**/app/logic.js`, `package.json` test-script line only) before any wave kickoff. Stages by explicit path (no `git add -A`), `package.json` skipped if diff is broader than the test-script line. Commit message style: `Checkpoint: team-leader Phase −2 — pre-wave owned-surface snapshot`. Foreign-dirty paths (specs, team-coordination, working files) untouched. Failed commit → `awaiting-Guillaume`. This lets inbox-driven auto-start clear Phase −1 cleanly without manual `git commit` between sessions. "Never commit yourself" hard constraint amended to "Never commit subagent output" — Phase −2 explicitly carves out pre-existing dirt.
- **Team-leader inbox listener + state machine.** Team-leader now **auto-starts** an incremental wave whenever its state is `idle` and `inbox.md` has unprocessed entries — no ack from Guillaume required. Per-turn poll on inbox; states: `idle` / `wave-in-flight` / `awaiting-Guillaume` / `awaiting-clean-tree`. Auto-start suspended in non-idle states; new entries queue. Loop guard: ≥ 2 failed attempts on an entry escalates to `awaiting-Guillaume` instead of retrying. Default wave mapping per change-type: `added` → test-writer + coder; `edited` → test-writer + coder; `deleted` → pruner + test-writer (remove orphan tests). `.claude/agents/team-leader.md` Phase 0 + Style rewritten.
- **Spec-change notification convention added.** New file `team-coordination/team-leader/inbox.md` (+ `inbox-done.md` archive). Product-owner and plant-nutrition-specialist personas now MUST append an inbox entry whenever a `spec.md` mutates (REQ added/edited/deleted), one entry per touched subproject, listing change-type + REQs + suggested waves. Team-leader reads inbox at session start and on every turn. Processed entries are cut to `inbox-done.md` with an outcome block. `.claude/agents/{product-owner,plant-nutrition-specialist,team-leader}.md` updated.
- **Team-leader wave wrap-up.** Post-Wave-2 fixes applied (test-helpers.mjs SIDEDRESS_MIN_EFF.Actisol_K name fix; ACCEPTED_EXCESSES entries for T4.Mg/T5.Mg downstream of REQ-098; verifier `scripts/check-recipes.mjs` recalibrated — REQ-098 mass-balance formula drops compost subtraction, REQ-024 predictedCE skip switched from 3000 g mass threshold to explicit `T1/T2 vegetative` set name since T2 fertigation mass crossed the old cutoff). Final: `npm test` 180/180/0, `npm run check` 114/0 (89/93 REQs wired). Wave 3: 4 pruners reported (domain, foliar, plant-needs, fertigation) — unanimous "no deletions, code surface already tight"; 9 borderline items surfaced. Last 2 Wave-3 pruners (app, sidedress) hit a rate limit; no edits attempted, tree state preserved.

## 2026-05-12

- evening (Wave 2 coder, foliar-recipe) — **REQ-115 surfactant-axis test green.** `BURN_CAP_BASE_G.Mo` 1 g → 2 g in `nutrition/tomato/foliar-recipe/data.js`. With Mo cap at 1 g, no element in the mid-sized `midGap = { Mn: 50, Zn: 50, Cu: 5, Fe: 200, Mo: 1, B: 20 }` lands below its cap with surfactant=true (Mo ideal_yucca 1.21 g still > 1 g cap), so both noSurf and yucca recipes collapsed to identical at-cap outputs. Bumping Mo cap to 2 g (still well within Sonneveld 50-200 mg/L foliar-Mo band; "wide tolerance" framing already in the data.js comment) lets Mo differentiate: noSurf 3.5 g (cap-bound, ceiled at 2 → 2.0 g) vs yucca 1.5 g (ideal-bound, ceiled from 1.21). All 24 foliar-recipe tests pass. calc.js untouched — algorithm was already spec-correct; the data was misaligned with the test's midGap magnitudes. Pre-existing failures in `nutrition/tomato/spec.test.mjs` (REQ-013 anti-stub NaN, REQ-014 Mg luxury) are unrelated to foliar/Mo.
- evening (Wave 2 coder, fertigation-recipe) — **REQ-098 Item 1 + REQ-151 Item 2 landed.** `calc.js`: dropped compost subtraction from K and Mg branches of `computeStageRecipe` (sidedress still subtracted from K only); T5 now lands at kSulfate ≈ 5537 g / mgSulfate ≈ 3320 g (vs old 5167 / 1379). `IMPLEMENTATION NOTE` block in calc.js removed (vestigial post-fix). Added `computeFertigationSupply(stage, opts = {}, recipe?)` in `calc.js` (REQ-151) — flat 11-element supply map (K/Mg/B computed via PRODUCT_PCT, all others numerical 0), reshapes `STORED_RECIPE.tomato.fertigation[stage]` to canonical g-keys when `recipe` omitted, stage-independent under explicit recipe. Exposed via `model.js` on `window.FertigationRecipeTomato.computeFertigationSupply`. `npm test` (post foliar-coder land): 180 / 178 pass / 2 fail. Both remaining failures are out-of-scope ripple: (a) `nutrition/tomato/spec.test.mjs` REQ-013 anti-stub fails with `expected NaN` because `nutrition/tomato/test-helpers.mjs` `EXPOSE_NAMES` omits `SIDEDRESS_MIN_EFF` — flagged for test-writer fix (single-line add); (b) same file REQ-014 luxury cap: T4.Mg / T5.Mg now exceed 1.3× demand (ratios 1.67 / 1.52) — needs `ACCEPTED_EXCESSES` entries (domain decision). `npm run check` red on 3 node checks: Fertigation mass-balance derivation (verifier pinned to old 5167/1379), Σ(channel_supply) ≤ 1.3× demand, predictedCE hors bande. Verifier recalibration explicitly out-of-scope per Wave-2 prompt — team-leader to handle post-wave.
- afternoon (test-writer Wave 1 re-run, fertigation-recipe) — **REQ-098 tests rewritten** to pin the no-compost-subtraction formula (sidedress still subtracted from K only; Mg = pure offtake). Tests now fail against current `calc.js` (which still subtracts compost) — failing-by-design until Wave 2 coder updates `calc.js`. Failure mode is explicit per-stage diff (e.g. `T1: got kSulfate=1647, expected 2016`; T5 will land at K=5537 / Mg=3320). Also added explicit INV-1 describe block (stage-coverage closed). REQ-151 tests unchanged (failing-by-design until `computeFertigationSupply` lands). Test-helper dependency on `window.CompostContribution` removed (no longer needed). Baseline: 20 tests / 8 pass / 12 fail (3 REQ-098 numeric + 9 REQ-151 deferred).
- afternoon (team-leader wave) — **REQ-098 amended:** compost no longer subtracted from K/Mg offtake in `computeStageRecipe` formula (sidedress still subtracted from K). Resolves the "policy vs implementation drift" the spec body itself acknowledged. Wave 2 coder will land the matching `calc.js` change; fertigation test-writer being re-spawned to pin the no-subtraction formula. `derivation.md` drift sections now vestigial (specialist to clean in a follow-up).
- afternoon (team-leader wave) — **REQ-145 collision resolved silently per P-06:** the contribution-block-recipe-table REQ at `nutrition/spec.md:947` renumbered to **REQ-152** (old REQ-145 in `nutrition/soil-contribution/spec.md` retains its slot + verifier wiring). PO-145 request body in `requests-done.md` updated end-to-end; collision-flag block collapsed to a one-line resolution note. xref on `nutrition/spec.md:793` (REQ-137 → REQ-145) also flipped to REQ-152.
- afternoon (team-leader wave) — **Wave 1 dispatched** for `nutrition/tomato/` + 5 nested subprojects (test-writers reconcile existing tests with current specs). Baseline (Node 24.15.0): 158 tests / 147 pass / 11 fail. Failures concentrated in fertigation REQ-151 (`computeFertigationSupply` deferred, Wave 2 coder picks up) + foliar REQ-115 (surfactant→coverage-axis impl gap surfaces as 2 fails). `npm test` script simplified to `node --test` auto-recurse; engines pinned `>=18`; `.nvmrc` added with `lts/*`. Team-leader principles P-01..P-08 captured under `team-coordination/team-leader/principles.md`.
- evening — **B2 cleared (specialist):** `FOLIAR_COVERAGE_DEFAULT` cert downgraded 4 → 3 across `nutrition/tomato/foliar-recipe/{spec.md REQ-101, data.js comment, derivation.md "Coverage — why 0.30 without yucca"}`. Rationale: claim was never directly measured at Décembre; Cu local-pool-toxicity image is pool concentration not surface coverage; Sentís et al. 2017 cuticle Mn ~3 % is absorption axis distinct from retention, so 30 % conflates two unseparated axes; no tissue test correlates predicted to measured uptake yet. Existing "Tissue test reveals per-element drift" refinement-trigger in `derivation.md` amended in place (no duplicate entry) to state the cert-bump-back condition: petiole panel correlating `computeFoliarSupply('T5').Mn` to measured Mn within ±20 % raises REQ-101 cert 3 → 4. Parallel `FOLIAR_COVERAGE_WITH_YUCCA = 0.80` cert 4 claim flagged for follow-up B2' downgrade (same evidence base, zero direct validation, yucca not on order). `npm run check` 114/0, REQs 89/92 wired (unchanged). Entry moved `requests.md` → `requests-done.md` for challenger verification.
- evening — **PO-145 flagged (specialist), not executed.** Two-part block: (a) routing — request is overwhelmingly UI / render-layer work (`app/index.html`, generic recipe-table renderer, "Recette proposée" block deletion, prose-strip in admin blocks), out of plant-nutrition-specialist scope per persona file; recommend reroute to coder persona for items 1-7 as a single bundle. (b) **REQ-NNN collision detected** — the new `nutrition/spec.md:947` REQ-145 (Contribution-block recipe table, landed afternoon) collides with the existing `nutrition/soil-contribution/spec.md:209` REQ-145 (Pourquoi modal interpretation strings, landed 2026-05-11 afternoon, already wired in `scripts/check-recipes.mjs:3940`). **Same failure mode hit twice today** — the fertigation-supply spec was *also* originally drafted as REQ-145, caught its own collision, renumbered to REQ-151. Two collisions in one day across parallel sessions suggests silent-allocation convention is losing to clock-skew; flagged to PO. Implementation blocked until contribution-block REQ-145 renumbers (probable target REQ-152). Entry moved `requests.md` → `requests-done.md` with scope-flag response block.
- evening — **Team-leader rig added.** New orchestrator persona at `.claude/agents/team-leader.md` walks the whole spec tree (every `*/spec.md` + `requirements.md`) and dispatches three waves of spawned subagents per subproject: **test-writer** (writes `node:test` unit tests covering each REQ-NNN), **coder** (writes minimal code to flip failing tests green), **pruner** (deletes code not reached by any test, reusing `.claude/agents/spec-pruner.md` discipline with the trigger swapped from "no REQ trace" to "no test reaches it"). Subagent role files: `.claude/agents/test-writer.md`, `.claude/agents/coder.md`. New test layer = `node:test` collocated as `<subproject>/*.test.mjs`, run via `npm test` (script added by leader before Wave 1 to avoid parallel-edit race on package.json). Verifier (`scripts/check-*` + `npm run check`) stays as cross-app contract — distinct from the new unit-test layer. Phase −1 clean-tree precondition mirrored from spec-pruner; never-touch list includes `STORED_RECIPE.tomato.*`, `RECIPE_HISTORY`, `dist/`, `scripts/check-*`, `*.test.mjs` (coder), `package.json` (post-Wave 1). Confirmation gates retained for spec gaps, STORED-recipe writes, red verifier, and Wave 3 mass-deletions > 200 lines. `team-coordination/team-leader/principles.md` stub added per the per-persona-principles convention.
- evening — **C3 cleared (specialist):** `BURN_CAP_SURFACTANT_FACTOR` (inert 1.0 constant) deleted from `nutrition/tomato/foliar-recipe/{data.js, model.js}`; `burnCapG(element, _surfactant)` body collapsed to `return BURN_CAP_BASE_G[element] || 0;`. REQ-115 in `spec.md` trimmed (no more "single operator-tunable knob" framing, verifier description down to three assertions). Rejected per-element multiplier table (Cu 1.2× / B 1.0× retired 2026-05-10) + intermediate inert-constant attempt captured in **new file** `nutrition/tomato/foliar-recipe/learnings.md` per the 2026-05-12 CLAUDE.md trace-placement update. `scripts/check-recipes.mjs` REQ-115 block stripped of the `burnCapG(el, true) === base × factor` loop. `npm run check` 114/0, REQs 89/92 wired (unchanged). Entry moved `requests.md` → `requests-done.md` for challenger verification.
- afternoon — **REQ-145 added (cross-app): Contribution-block recipe table.** Every contribution channel block on every Nutrition admin page (excluding Tomato Sol soil-bank) MUST render a 3-column table `Produit | Composition (% m/m) | Quantité` between its title and gap-grid. Composition = product label % as `·`-separated string in canonical element order (N · P · K · Ca · Mg · Fe · Mn · Zn · Cu · B · Mo), zero-value elements omitted. Quantité = channel-native dose. **REQ-137 amended in place** — recipe-header bullet (now superseded by REQ-145) dropped; REQ-137 retitled "Contribution-block gap-grid table" and trimmed to the gap-grid claim only, in the new statement-only shape. Implementation (recipe-table renderer + "Recette proposée" block deletion + REQ-145 verifier wiring + REQ-137 verifier update from recipe-header check to recipe-table-sibling check) handed to a specialist session. New principle: "never let stuff become vestigial" (memory entry `feedback_no_vestigial.md`) — when a new spec supersedes part of an old one, amend the old in place.
- afternoon — **PO specs are statement-only.** CLAUDE.md "Each spec entry has" reshaped to drop Rationale / Verification / Cert / Supersedes sub-sections from spec bodies. Verifier file is the verification record; rationale goes in commit message or `derivation.md`; cert dropped from PO surface specs. Memory entry `feedback_spec_concision.md` + PO persona example trimmed. Existing specs with the heavier shape stay until naturally edited; forward-only.
- afternoon — **`spec-council` persona dropped.** Per Guillaume: redundant once each persona learns its own principles autonomously. Deleted `.claude/agents/spec-council.md` and `team-coordination/spec-council/`. Memory entries updated (`team_personas_dir.md`, `MEMORY.md`, `feedback_persona_principles_files.md` — removed the meta-consolidation clause). Every persona is responsible for its own principles file; no meta-persona reads across roles. Active roster: product-owner, plant-nutrition-specialist, model-challenger, context-coherence, spec-pruner.
- afternoon — **Per-persona `principles.md` files added.** Five files at `team-coordination/<persona>/principles.md` (product-owner, plant-nutrition-specialist, model-challenger, context-coherence, spec-pruner). Each persona reads its `principles.md` on session load and appends a new entry whenever a Guillaume decision reveals a transferable pattern — terse format `- P-NN — [principle]. *Because:* [why]. (YYYY-MM-DD)`, monotonic numbering, soft cap 30 with consolidation. Goal: compound autonomy over time so personas stop re-asking what Guillaume already answered. Memory entry `feedback_persona_principles_files.md` added + indexed in MEMORY.md.
- **Fertigation supply delivers per-element mg/m²/wk (REQ-151) — drafted** (spec-only) in `nutrition/tomato/fertigation-recipe/spec.md`. `computeFertigationSupply(stage, opts = {}, recipe?)` returns per-element mg/m²/wk delivered by the fertigation channel (K, Mg, B at T5). Canonical g-keyed recipe arg `{ kSulfate_g, mgSulfate_g, solubore_g }`; caller reshapes from STORED or FP sources. Flat 11-element return shape (matches `computeFoliarSupply` precedent); per-element details + cap on contribution channels (REQ-136) are built at the caller, not in the model. Verification deferred — wires when `calc.js` lands the function and the consumer in `app/index.html` is refactored to call the namespace. Subproject preamble updated: subproject now answers two questions (sizing + supply). FertigationRecipeTomato namespace must expose its public API surface (REQ-099) — spec table kept at 2 keys (sizer surface); REQ-151 owns the supply-key assertion independently to avoid spec/verifier drift this turn. Derivation block "Supply derivation — `computeFertigationSupply` (REQ-151)" added to `derivation.md`: formula, per-element table at canonical T5, why-no-pH-response, caller-side reshape patterns (stored + FP), cert table. New `nutrition/tomato/fertigation-recipe/learnings.md` created per the 2026-05-12 CLAUDE.md convention: rejected polymorphic + stage-mode signatures with reasoning rooted in SRP + pure-function discipline. Memory: new `feedback_model_srp.md` ("model owns logic, caller owns glue — no mode flags at the model boundary"; complementary to `feedback_pure_code.md`); MEMORY.md index updated. Closes the TODO under "app must call subproject namespace, no inline reimplementation" (REQ-139) once implementation lands (next turn). **Original draft was REQ-145 — collision with the parallel-session pourquoi-modal-strings spec in `nutrition/soil-contribution/spec.md` (REQ-145) that landed 2026-05-11 afternoon. Renumbered to REQ-151** (skipping 146–150 reserved for the F1 Salanova carve-out). No verifier count change for this turn (114 PASS / 0 FAIL, 89/92 wired — REQ-151 deferred until calc.js lands).
- **Every REQ-NNN reference must lead with a statement, number in parentheses (new convention).** Memory `feedback_req_expand.md` rewritten; MEMORY.md index entry updated. Format: `<statement> (REQ-NNN)`, not `REQ-NNN — <label>` and not `REQ-NNN (<label>)`. Statement = subject + verb + complement (or a normative "X must Y" shape). A function name, namespace name, or noun-phrase label is NOT a statement and does not satisfy the rule. >6 words is fine if needed for the statement to be a real statement. Per Guillaume: "numbers don't tell me anything." Applied retroactively to today's REQ-151 spec / derivation / learnings additions and to this changelog entry; existing project-wide bare or label-form references not migrated (out of scope for this turn).
- morning — **Context-coherence F1 routed → specialist.** Approved finding: Salanova model dumped into `app/index.html` (4 `LETTUCE_*` constants, `calcLettuceNutrDemand`/`Supply`, `buildNutrimentLettuce`, `lettuceLeverFor`) + cross-domain leak at `nutrition/tomato/app/logic.js:6` calling `buildNutrimentLettuce`. Queued in `team-coordination/requests.md` as F1 — carve `nutrition/lettuce/plant-needs/` mirroring tomato subproject; keep `setNutrCrop` + crop-toggle DOM at integrator; calc layer must be pure + SRP-clean per recent feedback memories; trace placement follows new derivation.md / learnings.md split. Also widened `requests.md` header to support multi-source intake (`**Source:**` field on schema) so context-coherence + model-challenger share one inbound specialist queue.
- morning — **Challenger ↔ specialist three-file workflow set up.** New directory `team-coordination/` at repo root (per new convention: shared files flat, persona-only files in subdir). Three workflow files: `model-challenger/drafts.md` (challenger-only — raw findings tagged PENDING / APPROVED / REJECTED / DEFERRED), `requests.md` (shared — challenger → specialist queue with Action/Acceptance blocks), `requests-done.md` (shared — specialist → challenger verification queue with Specialist response + Challenger verdict blocks). PostToolUse hook in `.claude/settings.json` matches Edit/Write/NotebookEdit/MultiEdit on `*/derivation.md` and spawns a detached headless `claude -p` session loaded with the model-challenger persona in Mode A (auto-critique); hook script at `scripts/challenger-critique-hook.sh`. Both persona files updated: `model-challenger.md` rewritten with Mode A (headless, drafts-only) + Mode B (interactive, one-by-one review + verification) protocol; `plant-nutrition-specialist.md` reads `team-coordination/requests.md` at startup and writes `### Specialist response` blocks into `requests-done.md` on completion. Previous `working files/model-challenger-log.md` migrated → `team-coordination/model-challenger/drafts.md`; B1/B2/B3/B5/C1/C3 already approved verbally seeded into `team-coordination/requests.md` as actionable specialist requests; B4/C2/C4/D1/D2/D3 stayed in drafts.md as PENDING awaiting one-by-one review.
- morning — **Trace-as-comments rule reversed.** CLAUDE.md updated: calculation trace no longer lives in code comments — it moves to `<subproject>/derivation.md` (current REQ-tied why-this-number) and new `<subproject>/learnings.md` (rejected alternatives, historical decisions, why-not-X reasoning). `spec-pruner` persona now prunes trace comments along with unspecced narrative; `Phase −1` added — pruner refuses dirty working tree, requires commit-first so any prune can be reverted with `git reset --hard HEAD`. `plant-nutrition-specialist` persona owns `learnings.md` alongside `derivation.md`; never put rejected-alternative content in derivation.md again. Existing trace comments in codebase will be migrated to derivation.md / learnings.md on next pruner pass per subproject.

## 2026-05-11

- evening — **`.claude/agents/context-coherence.md` persona added** — fourth team persona, sibling to PO / specialist / challenger. Domain: cross-file coherence + single-responsibility hygiene across the spec / code / index / memory graph. Three lenses per walk (cross-ref rot, SRP violations, layer drift). Proposes restructures + routes findings to owner sessions; unilateral edits limited to stale directory-`CLAUDE.md` indices, `MEMORY.md`, memory bodies, and changelog typos. Walk-vs-drill modes mirror coherence-audit.md structure.
- evening — **`working files/model-challenger-log.md` created** — first entry. Adversarial review of `nutrition/tomato/foliar-recipe/` (in-flight REQ-112/115/116 + b377b60 baseline). Headline blindspot: derivation.md's T5 delivered-vs-demand table is out of sync with `plant-needs/data.js` — Mn ~9 vs actual 7.5 (under-fert is 72 % not 60 %), Zn ~6 vs 4.5 (delivered 6.1 = **136 % demand**, over REQ-014 1.3× cap on foliar channel alone), Mo ~0.3 vs 0.075 (delivered 0.31 = **413 % demand**). Other blindspots: coverage 0.30 cert 4 leans on Cu-image inference + literature midpoint (no direct measurement) → cert should drop to 3 until 2026-05-12 tissue panel; CE-cap-and-scale loop scales all elements proportionally, preferentially strips pH-locked Mn/Cu when Fe-heavy gap drives CE excess; min-dose floor 0.5 g is element-blind and pre-loads Mo/Cu into luxury-cap violation when gap is small. Complexity flags: `sprayCount` lever (REQ-112) directly contradicts inherited REQ-062 (single foliar spray per week); `BURN_CAP_SURFACTANT_FACTOR = 1.0` is inert; REQ-116 wiring of FP into Block 7 drift gauge for a burn-cap-constrained channel is mostly going to display "drift = 0 always". Verdict: hold — land after addressing the demand-table reconciliation (B1) and coverage cert downgrade (B2) at minimum.
- afternoon — **Path A — deterministic render of spec.** Drop `stable:` and bare `derived` from REQ-144; both fail the verifier loudly now. Allowed `data-prose-source` values become `derived:<funcName>` (function must be declared in source) | `REQ-NNN` (bytes owned by spec entry) | `label` (static UI nomenclature). Build pipeline (`scripts/build.mjs`) now parses fenced ` ```render <key> ` blocks across every `spec.md`/`requirements.md`, injects `window.SPEC_STRINGS = {...}` into `dist/index.html` via new `<!-- @spec-strings -->` marker. Runtime helper `renderSpec(reqId, key, interp)` resolves the lookup + optional `${var}` substitution. **First spec-owned strings: REQ-145 in `nutrition/soil-contribution/spec.md`** (6 Renders: keys — Ca / P / K-fert-routed / Mg-fert-routed / N-not-mehlich / default-not-mehlich) replacing hand-written pourquoi-modal interpretation prose in `nutrition/tomato/app/logic.js`. Modal renderer (`showPourquoi` in `app/index.html`) extended to accept `interpretation: { req, key, interp }` and stamp `data-prose-source` on the modal body. Verifier: REQ-144 rewritten (validates function-pointer set + spec-header set); REQ-145 added (asserts SPEC_STRINGS has expected keys + every `renderSpec('REQ-145', …)` call site uses a declared key). **114 PASS / 0 FAIL, 89/91 wired** (up from 113 / 88/90). Next: retrofit operator containers into `data-prose-check="strict"` mode (zero opted-in today).
- afternoon — **REQ-144 wired: operator-facing prose requires declared provenance** (opt-in). New spec in `requirements.md`: text nodes inside `[data-prose-check="strict"]` containers must have a `data-prose-source` ancestor attribute (`derived` | `stable:<tag>` | `REQ-NNN`). Verifier (`check-recipes.mjs`) walks marked subtrees, fails on undeclared text or unknown REQ pointers. Migration is opt-in by design: zero containers locked-down today (REQ-144 passes trivially). Containers opt in as they're touched; new operator surfaces opt in from day one. Tightens REQ-060 (which only requires `// stable —` source comments, invisible to DOM verifier) by promoting provenance into the rendered DOM. Suggested first retrofit target: Block 5 levers (`#nutr-missing`) — already 100 % auto-derived plus one stable pH-lock reminder. **113 PASS / 0 FAIL, 88/90 wired** (up from 112 / 87/89).
- afternoon — Memory audit performed (16 entries walked). Two entries rewritten: `feedback_release_candidate_validation.md` (updated to "spec" terminology + dual bash/node verifiers; was still on retired "requirement" framing) and `feedback_dev_server_systemd.md` (trimmed to Décembre-specific delta — unit name `decembre-devserver`, `dev-server-daemon.sh` — now that generic procedure is covered by `/static-dev-server` skill). New skill drafted at `~/.claude/skills/audit-memory/` to make this audit pass repeatable. Two entries still need user verification (review-workflow currency, model-challenger TODO — note: TODO already resolved by parallel session).
- 01:15 — **`nutrition/soil-contribution/` subproject created** — extracted from `app/index.html` (`SOIL_BANK_TOMATO_MG_M2` constant) and `nutrition/tomato/app/logic.js` (soilMg + monthsToDepletion + renderSoilGrid). Files: `spec.md` (REQ-140 bank shape · REQ-141 CONTRIBUTING scoping · REQ-142 monthsToDepletion · REQ-143 namespace), `data.js` (`SOIL_BANK_MG_M2.tomato` + `SOIL_CONTRIBUTING` + `WEEKS_PER_MONTH = 52/12`), `calc.js` (pure `soilWeeklyContribution` + `soilMonthsToDepletion`), `render.js` (`soilRenderGrid` — non-generic 6-col UI helper, lives in subproject per "non-generic renderers go in their subproject" rule), `model.js` (`window.SoilContribution` namespace). `app/index.html`: 4 new `@include` lines next to compost-contribution; old inline `SOIL_BANK_TOMATO_MG_M2` constant removed. `nutrition/tomato/app/logic.js` Block 2 now reads `SC.weeklyContribution / monthsToDepletion / renderGrid` (no bare constant access). Pourquoi modal interpretation strings stay in logic.js (page-specific, `// stable —` per REQ-060) and stripped of hardcoded kg/ha numbers (sourced via `SC.BANK_MG_M2.tomato[el]`). `nutrition/spec.md` "Cross-crop subprojects" section added. Verifier: 4 new REQ-140..143 checks wired in `scripts/check-recipes.mjs`. **112 PASS / 0 FAIL, 87/89 wired** (up from 108 / 83/85).
- 01:00 — Unspecced narrative pruned from Block 2 (Sol). Removed intro paragraph ("Banque Mehlich-3 (Berger Labs avril 2026...)") and the "Hypothèses du calcul" bullet added to `nutrition/tomato/app/page.html`. Spec discipline: user pointed out the intro was not specced. Memory updated (`feedback_no_unspecced_narrative.md`) — when adding a tightly-scoped block/column, don't pad with framing copy not in the request.
- 00:00 — Cap modal restructured for scannability. REQ-136 schema bumped from `{kind, reason, uncappedMg}` to **`{kind, constraint, limit, lever, uncappedMg}`** — three orthogonal short strings replace the prose `reason` field. REQ-138 modal layout now: header + kind badge + number-delta line (`233 → 467 +234`) + three labelled rows (Contrainte · Limite · Levier). Hover tooltip = `${constraint} · ${limit}`. No REQ citations in operator-facing copy (operator-facing prose was noise; specs remain in spec.md). Calc adapters: substrate-contribution + fertigation nursery (data.js + tomato logic.js for compost/sidedress/fert/foliar). Verifier REQ-136 schema check updated to assert all four fields populated as non-empty strings + uncappedMg numeric. Calc-author discipline: each cap field has a length budget (constraint ≤ 4 words, limit ≤ 8 words, lever ≤ 6 words, action verb / arrow first). 108 PASS / 0 FAIL.
- 00:30 — Nutrition page Tomate: **Block 2 (Sol — banque résiduelle)** inserted before compost. New constant `SOIL_BANK_TOMATO_MG_M2` in `app/index.html` near SME data: Mehlich-3 reservoir kg/ha → mg/m² (P 55 800 · K 211 800 · Ca 1 098 900 · Mg 164 600 ; Berger Labs avril 2026 tomate). Only **Ca and P** participate in the gap chain — `soilMg = min(demand, bank)` — banks large enough to carry the plant's weekly need outright (Ca ~70 ans, P ~3 ans theoretical depletion). Other elements (K/Mg with bank data, N + micros without) render as **disabled rows** (opacity 0.42, gap passes through unchanged). New column **Mois épuisement** = `bank / (demand × 4.33)` surfaces the reservoir runway for any element with bank data. Block numbering bumped: 2 compost → 3, 3 sidedress → 4, 4 fert → 5, 5 foliaire → 6, 6 leviers → 7, 7 drift → 8 (visual labels + inline JS comments; verifier label strings unaffected — they key on DOM ids). New `renderSoilGrid` helper in `nutrition/tomato/app/logic.js` (6-col layout: Él / Manque entrant / Apport ici / Manque sortant / Mois épuisement / icon). Compost block now reads `gapAfterSoil` instead of `gapAfterDemand`. 108 PASS / 0 FAIL.

## 2026-05-10

- 23:22 — Cap modal surfaces actual `cap.reason` instead of "Aucune raison de plafond enregistrée" placeholder. **REQ-138** spec tightened: hover tooltip + click modal MUST display the same `cap.reason` string (single source of cap copy). Implementation: `renderGapGrid` auto-registers the pourquoi entry inline when rendering the emoji, sourced from `details[el].cap` — no separate `registerPourquoi` call needed by any caller. Modal now shows: title `<el> — Plafond <kind> <emoji>` with cert badge, body = full reason string + auto-derived "Sans plafond, l'apport serait de X (au lieu de Y)" line when `uncappedMg > supplied`. Same approach across all four tomato blocks + nursery substrate + nursery fertigation. 108 PASS / 0 FAIL.
- 21:18 — REQ-136 + REQ-138 cap semantics reframed. `cap` now describes the **structural reason** a channel may under-deliver an element (set per element regardless of whether a numerical limit currently binds), not the artifact of a value being reduced. UI gates emoji rendering on **(`cap` non-null AND manque sortant > 0)** so emojis show only on rows with a real gap, but the explanation comes from the structural classification. Updated calc.js (substrate, fertigation nursery) and `nutrition/tomato/app/logic.js` (compost, sidedress, fert, foliar): every element now has a cap kind set per its operational lever (sourced + dose-bound = 🔥, chemistry-bound = 💧, no-source / mineralization-rate / cascade-priority = ❗). At default state: tomato compost shows 1 💧 (P phLocked) + 9 ❗; sidedress 1 💧 + 9 ❗; fert 1 💧 + 6 ❗; foliar 4 🔥 (sourced micros) + 2 💧 (N/P cuticle) + 1 ❗. Nursery substrate 1 🔥 (N germ cap) + 4 ❗. Nursery fertigation 9 🔥 (sourced elements) + 1 ❗. 108 PASS / 0 FAIL, 83/85 wired.
- 21:15 — MIXING_FACTOR_FERT retired (REQ-100 deleted). Stored-mode fertigation K/Mg/B supply now reported at full barrel mass — was discounted ×0.5 since 2026-05-05. Removed from data.js, model.js, app/index.html buildNutriment, page.html, logic.js Block 4 + pourquoi-modal, scripts/check-recipes.mjs. T5 stored-mode K supply doubles (1 891 → 3 781 mg/m²/sem); Mg doubles (180 → 359); B (FP-only) unchanged in stored mode (B is FP-only). 108 PASS / 0 FAIL, 83/85 wired.
- 19:30 — FP foliar supply routed through `computeFoliarSupply` (was inlined, ignored sprayCount + surfactant). `computeFoliarSupply(stage, opts, recipe?)` now accepts an optional 3rd label-string-array arg defaulting to `STORED_RECIPE.tomato.foliaire.A` — backwards-compatible. `buildNutriment` FP branch reshapes `FP_RECIPE_T5.foliar` as a label-string array (substring names aligned with calc.js `findG` searches) and calls the model with the FP foliar opts (sprayCount + surfactant), removing the inline `(g × pct × 1000 / area × cov)` block + dead `cov`/`feApplied_g` references for that branch. REQ-112 spec extended to document the 3rd arg + recipe-agnostic property; verifier extended with stub-recipe assertions (sprayCount=2 doubles, surfactant=true ×ratioYucca) so the recipe-arg path can't regress. Stored-mode behavior unchanged. 108 PASS / 0 FAIL.
- 16:55 — **REQ-139 added** (cross-app, `requirements.md`): app must call subproject `window.<Namespace>` functions, no inline reimplementation. Driver: today's foliar `sprayCount`/`surfactant` inline-drift bug — FP-mode foliar branch had inlined `(g × pct × 1000 / area × cov)` per element instead of calling `window.FoliarRecipeTomato.computeFoliarSupply`, silently ignoring operator levers. Wired in `scripts/check-recipes.mjs` with two layers: (1) registry-driven positive check seeds `(FoliarRecipeTomato.computeFoliarSupply, FoliarRecipeTomato.computeFoliarRecipeForGap, CompostContribution.releasePerWeek)` and asserts each appears as `window.<Namespace>.<fn>` in `app/index.html` — catches regression where a call site is deleted and the formula reinlined; (2) inline-formula blacklist forbids `PRODUCT_PCT.<XSO4_X|Solubore_B|NaMoO4_Mo|FeSO4_Fe>) / area * 1000 * cov` shapes anywhere in `app/index.html` — catches new drift on the foliar formula. Blacklist is foliar-only at first; extend with fertigation + sidedress shapes once `computeFertigationSupply` and `computeSidedressSupply` exist (TODO follow-up flagged in REQ-139 rationale — fertigation has `computeStageRecipe` and sidedress has `computeStageSidedress` but neither has a supply-side counterpart yet). Cert 4. 109 PASS / 0 FAIL, 84/85 wired (REQ-102 still deferred).
- 16:54 — REQ-137 cross-app rollout to **tomato Bilan blocks 2-5** (compost / sidedress / fertigation / foliaire). Each block now passes `details{el: {cert, cap}}` + `blockId` to `renderGapGrid`; cells carry `data-cell-key="${blockId}.cell.${el}"` and `onclick="showCellCert(blockId, el)"`. Details built inline in `nutrition/tomato/app/logic.js` (cert from existing per-element registerPourquoi cert values; cap=null at default since Ca-aware sidedress drops Actisol → P=0 → no precipitation cap fires). Sidedress fires 💧 precipitation cap on P only when `supply.sidedress.P > 0 AND phLocked` — that condition matches future state when Actisol returns OR a different stage with Actisol-bearing recipe. `showCellCert` extended to fall through `${blockId}.cell.${el}` → `${blockId}.${el}` legacy keys so existing tomato pourquoi entries (`compost.N`, `sidedress.K`, `fert.Mg`, `foliar.Mn`) work without rewriting. Verifier REQ-137 walks all four tomato blocks + asserts cell-keying. nursery/lettuce blocks now in same idiom. Salanova post-transplant blocks remain on legacy row-level pourquoi — adopt next pass. 108 PASS / 0 FAIL, 83/84 wired (REQ-102 sole deferred).
- 16:32 — REQ-136..138 IMPLEMENTED for nursery substrate + fertigation. Target default 50g per user request. (a) **REQ-136** — `cycleAverageReleasePerTray()` and `nurseryRecipeSupply()` now return `{perTray_mg, details}` where `details[el] = {cert, cap}`; `cap = null | {kind ∈ damage|precipitation|other, reason, uncappedMg}`. Substrate N fires `damage` cap when feather meal at LIMITS.maxFeatherMealPerTrayG (REQ-094, germination protection). Fertigation fires `damage` cap when CE near 95% of cap (REQ-098). Backwards-incompatible shape change for `cycleAverageReleasePerTray` — verifier REQ-096 callsite migrated. (b) **REQ-137 + REQ-138** — `renderGapGrid` extended to optional `details` + `blockId` params: when present, Apport ici cell carries `data-cell-key`, `onclick="showCellCert(blockId, el)"`, and inline cap emoji (🔥/💧/❗) with own `showCapReason(blockId, el, kind)` handler that uses `event.stopPropagation` to keep cell + emoji clicks distinct. New `showCellCert` + `showCapReason` helpers wrap existing `showPourquoi` modal infrastructure with the keying scheme `${blockId}.cell.${el}` / `${blockId}.cap.${el}.${kind}`. `buildNutrimentNursery` registers pourquoi entries for every (block, element) cell + every cap. (c) Target default 90 → 50 g per user request. (d) Verifier REQ-136 walks substrate + fertigation, asserts shape; REQ-138 asserts cell keys + 🔥 emoji presence + showCapReason wiring. Tomato/lettuce contribution blocks adopt in follow-up. 107 PASS / 0 FAIL, 82/84 wired (REQ-102 + REQ-137 deferred — REQ-137 covers cross-app rollout, only nursery wired today).
- 10:37 — REQ-115 surfactant-impact model **simplified to research-grounded 1.0×**. Web-research pass (Sentís et al. *Crop Protection* 2017 tomato-cuticle Mn study; vendor + extension lit on yucca; Cornell / U. Delaware / U. Missouri foliar guidance) showed surfactant raises COVERAGE / cuticle penetration substantially (3 → 20 % Mn, ~6.7×, already in model via `FOLIAR_COVERAGE_DEFAULT 0.30 → FOLIAR_COVERAGE_WITH_YUCCA 0.80`) but does NOT have published support for raising the BURN-CAP axis. Yucca is a wetting agent, not a penetration enhancer; under sun-only ops the longer wet-film property concentrates residue (burn-RISK amplifier, not relaxer). Retired the earlier per-element multiplier table (`SURFACTANT_SPREAD_FACTOR 1.5` + `Cu 1.2×` + `B 1.0×`) — operator-mid-band heuristics with no literature defense (cert 2 at best). Replaced with single `BURN_CAP_SURFACTANT_FACTOR = 1.0` constant — single operator-tunable knob, defaulted to "no benefit" until Décembre tissue + lesion data warrants a bump. `burnCapG(el, surfactant)` now uniform across elements; Cu and B element-specific overrides removed. data.js + model.js + spec.md REQ-115 + verifier all updated. 105 PASS / 0 FAIL, 80/84 wired (no change in counts; multiplier values are data, not specs).
- morning — REQ-135 colour-code framing clarified: bench DLI colour reflects the **young-plant** condition (d ≤ 14, before canopy closure) — at that stage spacing_factor=1.0, so per-plant DLI = bench. Late-cycle reality (per-plant after canopy closure ≈ bench × 0.40) is shown in the modal context line, not in the colour. Modal context line now annotated: bench DLI gets `(jeunes plants, j ≤ 14)`, per-plant gets `(j ≥ 28)`, and a small italic note explains the colour anchor. Resolves the operator confusion where green-at-bench (bench in 12-17 optimum) coexists with longer daysToPotential vs red-at-bench (bench saturated) — at packed density, late-cycle per-plant DLI in the saturated case is still in the productive band, while the green-bench case ends up light-starved late. No code-logic change beyond the modal annotation; colour computation still anchored to `f_light(dliBench)`. 105 PASS / 0 FAIL.
- 10:35 — REQ-138 (was REQ-135 before today's collision) tightened on modal scoping. Spec text now explicitly forbids aggregate modals: clicking any cell or emoji opens a modal scoped to **exactly one (block, element, click-target)** — never lists other elements, never pools cap reasons across elements, never reuses one modal pane for cell-click + emoji-click. Anti-patterns enumerated in spec body. Verifier extended to assert (a) cells keyed by `(blockId, element)`, (b) emojis keyed by `(blockId, element, capKind)`, (c) post-click modal body contains exactly one element symbol + one channel value. Implementation guidance updated with key shapes. Cross-app contribution-block REQ trio renumbered 133/134/135 → **136/137/138** to dodge today's morning yield-range collision (yield-range now owns 131-135, contribution-block owns 136-138). nursery/app/spec.md inherited-specs section follows. 105 PASS / 0 FAIL.
- morning — yield-range UI gains 3 small improvements: **REQ-133** Capacité plafond card now shows `pic à J<n>` next to the cap value (using existing `daysToPotential` output); **REQ-134** bench DLI display rounded to integer (modal keeps decimal for transparency); **REQ-135** bench DLI value color-coded green/yellow/red per f_light zone (auto-computed from `window.YieldRange.f_light`, follows REQ-016 3-tier convention). npm run build OK; npm run check 105/105 PASS.
- 10:25 — **REQ-116 wired**: FP foliar recipe live-derived from pre-foliar gap chain in `calcNutrSupply` (Option A — two-pass eval inside `calcNutrSupply`). When `mode === 'fp'`, before the foliar branch, compute pre-foliar fert/sidedress/compost contributions, derive per-element gap = max(0, demand − compost − sidedress − fert) for {Mn, Zn, Cu, Fe, Mo, B}, read sprayCount/surfactant from DOM levers, call `window.FoliarRecipeTomato.computeFoliarRecipeForGap(gap, opts)`, and mutate `FP_RECIPE_T5.foliar.{MnSO4, ZnSO4, CuSO4, FeSO4-7H2O, NaMolybdate, Solubore}` in-place — existing FP foliar branch + `renderPhase1Comparison` (Block 7 drift gauge) + `renderProposedRecipe` ("Recette proposée") all now read live values. Try/catch wraps the call; on failure (e.g. predictedCE not yet defined), the prior `FP_RECIPE_T5.foliar` literal stays as fallback. Block 7 drift gauge now meaningful for foliar — stored vs FP doses diverge as upstream channels close gaps. Stored mode untouched (still parses `STORED_RECIPE.tomato.foliaire.A`, /retire-recipe-governed). Verifier: REQ-116 calls `calcNutrSupply('T5', true, 1.0, 1.5, 'fp')` baseline, bumps `window.CompostContribution.releasePerWeek.Mn = 1.0` (≫ Mn demand), re-calls, asserts `FP_RECIPE_T5.foliar.MnSO4` shrunk to 0 (gap closed → min-dose clamp). Restores compost value + re-runs at canonical state. Spec updated: deferred → live runtime check. 105 PASS / 0 FAIL, 80/84 wired (4 deferred = REQ-102 nursery/fertigation + REQ-131/132 yield-range + REQ-133/134/135 nursery contribution blocks; REQ-116 retired from deferred list). Diff left uncommitted for review.
- 10:15 — Cross-app contribution-block spec added to `nutrition/spec.md`. **REQ-133** — every contribution channel function (compost / substrate / sidedress / fertigation / foliar / front-load) returns a per-element `details{cert, cap}` object alongside its flat mg map. `cap` is null or `{kind ∈ {damage, precipitation, other}, reason: string, uncappedMg: number}`. Backwards compatible — flat callers untouched. **REQ-134** — every contribution block on every Nutrition page renders the same two-zone layout (recipe header + 5-col `renderGapGrid` table). REQ-127 / REQ-128 (nursery) become crop-specific extensions. **REQ-135** — Apport ici cells clickable for cert modal; cap emojis (🔥 damage / 💧 precipitation / ❗ other) inline after the value, independently clickable for cap-reason modal. `cap.reason` strings auto-derived (REQ-060 inheritance), `precipitation` caps cite REQ-029a/b/c pair, `decorative` flags cite REQ-018, etc. REQ numbers chosen as 133-135 to dodge the 131/132 collision with yield-range's 10:01 work. Spec only — implementation pending. nursery/app/spec.md inherited-specs section updated to point at REQ-133..135. 104 PASS / 0 FAIL, 79/84 wired (5 deferred = REQ-102 + REQ-131/132 yield-range + REQ-133/134/135 just added; REQ-127/128/129 wired but inheriting REQ-134 doesn't auto-wire it).
- 10:01 — yield-range gains explicit double-poly transmission decomposition (**REQ-131**) + clickable DLI display with f_light response modal (**REQ-132**). `data.js` now declares `DLI_SUN_OUTDOOR_QC_ANNUAL = 30` × `GH_LIGHT_TRANSMISSION_DOUBLE_POLY = 0.55` → derived `DLI_SUN_GH_ANNUAL_AVG_QC = 16.5` (was hardcoded 16; refit-anchor comment updated 16 → 16.5). REQ-114 statement amended to reference the new decomposition. `model.js` exposes the two new constants + `dliBenchAvg` + `F_LIGHT_BREAKPOINTS` on the `window.YieldRange` namespace. `app/page.html`: clickable `DLI banc` card under Capacité plafond opens `#yr-dli-modal` (reuses `.pq-modal*` CSS classes). `app/logic.js`: `renderYieldRange` writes `#yr-dli-value`; `openDliModal()` builds the 5-row f_light table directly from `F_LIGHT_BREAKPOINTS` (REQ-060 — descriptive labels hardcoded, breakpoint x/y values not), plus a context line showing current bench DLI and per-plant DLI at full canopy bind (×0.40 floor). Escape key closes the modal. Task A/B REQ numbers used 131/132 (not 130/131 as the task brief suggested — REQ-130 was already taken by today's 09:25 nursery Block 1 work). `npm run build` OK; `npm run check` 104/104 PASS, 79/81 wired (REQ-131 + REQ-132 deferred as expected).
- 09:55 — **Foliar gap-maximizing recipe model** locked + implemented. (a) **REQ-115** (foliar-recipe) wired: `computeFoliarRecipeForGap(gap, { sprayCount, surfactant })` returns a recipe per 15 L master tank sized to maximize coverage of the residual per-element gap subject to per-element burn cap, per-tank predicted-CE cap (REQ-025), and a 0.5 g operational dosing floor. Surfactant impact via single droplet-spread mechanism (Gaskin & Holloway 1992 mid-range): `surfactantMultiplier = 1.5×` universal, with `Cu = 1.2×` (narrow toxicity threshold) and `B = 1.0×` (non-ionic, surfactant-irrelevant) chemistry overrides. Min-dose clamp: `ideal_g < 0.5 → 0` (sub-half-gram doses aren't operationally measurable). CE-cap-and-scale loop bounded at 4 iterations. (b) `data.js` adds `BURN_CAP_BASE_G` (literature mid-band, cert 3, refinable when Décembre lesion data lands), `SURFACTANT_SPREAD_FACTOR = 1.5`, `surfactantMultiplier(el, surfactant)`, `burnCapG(el, surfactant)` helpers. (c) `calc.js` adds `computeFoliarRecipeForGap`. (d) `model.js` namespace gains 4 keys (`BURN_CAP_BASE_G`, `SURFACTANT_SPREAD_FACTOR`, `burnCapG`, `computeFoliarRecipeForGap`). (e) Verifier covers tiny-gap → all-zero clamp, huge-gap → element-cap clipping, surfactant multipliers per element, predicted-CE-bound. **REQ-116** locked but verifier deferred — wires when `buildNutriment` / `calcNutrSupply` integrate the live derivation (replacing `FP_RECIPE_T5.foliar` literal with a runtime call against the current pre-foliar gap chain). Also: `roundUpHalf` helper renamed to `ceilToHalfGram` to dodge REQ-002 forbidden-products blocklist false-positive on "roundup" substring. 104 PASS / 0 FAIL, 79/81 wired (2 deferred = REQ-102 nursery/fertigation parallel session, REQ-116 awaiting integration).
- 09:25 — Semis Block 1 (Besoins) trimmed to 3 columns. **REQ-130** added to `nutrition/nursery/app/spec.md`. Layout: Él / Par plant / Cert. Per-tray and per-cohort columns dropped — they were computational steps, not display surface; both numbers feed Blocks 2-3 inside the gap-chain math. Per-plant remains because it's the operationally relevant unit for tissue-test reasoning. Cert column kept (cert 3 macros / cert 2 micros). Verifier asserts grid signature, "Par plant" header, "mg/sem" unit suffix, and ≥ 3/11 element symbols rendered. 103 PASS / 0 FAIL, 78/79 wired.
- morning — yield-range plateau enum extended from `{32, 50}` to `{18, 24, 32, 50}`. Math spec (REQ-114 input contract) and app spec (REQ-119 toggle) updated. `CANOPY_CAP_BY_PLATEAU` in `data.js` extends with `24: 80, 18: 120` (extrapolated via 2-anchor power-law fit W ∝ density^-1.585; cert 2 — refit when RZ density curves load or when 24/18 cohorts run). `app/page.html` toggle now has 4 buttons (18 / 24 / 32 / 50 cellules); `logic.js` data-binding pattern unchanged (already generic over plateau values). `npm run build` OK; `npm run check` 102/102 PASS, 77/78 wired.
- 09:00 — Semis subpage Blocks 2 + 3 refactored to gap-grid layout. **REQ-127..129** added to `nutrition/nursery/app/spec.md`. (a) **REQ-127** — Block 2 (Réserve substrat) now has scannable recipe header (`Farine de plumes 9 g/plateau` + OM2 placeholder note) + canonical `renderGapGrid` 5-column table (Él / Manque entrant / Apport ici / Manque sortant / icon) with color coding ✅🟢🟡🔴 inherited from tomato. (b) **REQ-128** — Block 3 (Fertigation) recipe header now lists per-product g/L doses + ×N/sem annotation, CE/pH readouts in green/red, then 5-col gap-grid for the same shape. (c) **REQ-129** — Gap chain locked: `gapAfterDemand → gapAfterSubstrate → gapAfterFert`; Block 2's "manque sortant" feeds Block 3's "manque entrant" (passed via the same map); Block 4 leviers reads `gapAfterFert` as residual. Verifier: REQ-127/128 do DOM walks asserting recipe header + grid signature + .pq-row count; REQ-129 reads the calc-side outputs and asserts gap-chain monotonicity for N/P/K/Ca/Mg. 102 PASS / 0 FAIL, 77/78 wired (REQ-102 still deferred upstream).

## 2026-05-09

- 17:30 — REQ-122..126 IMPLEMENTED. (a) `nurseryRecipeSupply(recipe, trayVolumeL, applicationsPerWeek)` 4th param wired, integer-coerced [1,7], scales weekly supply linearly. (b) New `minApplicationsPerWeek(recipe, demand, trayVolumeL, ceCap)` returns smallest integer N ≤ 7 covering all sourced elements at 100% of demand-after-substrate within EC cap; null if dose-bound. (c) New `nurseryElementsBySource(recipe, demand)` returns `{sourced[], unsourced[]}` with literal "any positive supply" semantics — Mo unsourced at default; everything else has at least kelp traces. (d) `nurseryRecipeCE` arity stays at 2 — REQ-125 verified at function-arity level. (e) `applicationsPerWeek` validated across 7 coercion cases (round, clamp, NaN→1, etc.). Both new functions exposed on `window.FertigationNursery`. UI: new `nutr-n-applications` input on the Semis subpage (default 1, range 1-7); fertigation block header shows "×N/sem"; leviers card renders `minApplicationsPerWeek` recommendation + unsourced-element warning. At default doses + 90g target, K is dose-bound (228 mg/fert × 7 < 3 730 mg demand-after-substrate) → page surfaces "Recette plafonnée — augmenter dose OU ajouter produit" warning. REQ-124 spec text updated to literal "any source" semantics + matched verifier test. 99 PASS / 0 FAIL, 74/75 wired (REQ-102 still deferred upstream).
- 17:25 — Tomato Nutrition app **Block 5 (Foliaire) extended with sprayCount + surfactant levers**. (a) **REQ-112** (foliar-recipe) — `computeFoliarSupply(stage, opts)` now accepts `{ sprayCount = 1, surfactant = false }`. `sprayCount` clamped 1-3 at the model boundary; multiplies delivery linearly. `surfactant=true` switches coverage from `FOLIAR_COVERAGE_DEFAULT` (0.30) to `FOLIAR_COVERAGE_WITH_YUCCA` (0.80). Defaults match prior single-arg behavior — call sites without opts unchanged. (b) **REQ-113** (tomato/app) — Block 5 renders two block-local inputs at the top of the card body: `nutr-foliar-spray-count` (number, 1-3, default 1) + `nutr-foliar-surfactant` (checkbox, default unchecked). First instance of block-local inputs in the spec convention; header REQ-104 stays at 5 scalars. (c) **REQ-114** — Block 5 reactive: mutating either input re-renders the foliar gap grid. `app/index.html` listener array picks up both ids; surfactant also wires `change` handler since checkbox `input` fires unevenly across browsers. `calcNutrSupply` foliar block reads the inputs and passes them through to `FoliarRecipeTomato.computeFoliarSupply(stage, foliarOpts)`. Verifier: REQ-112 calls function with three opt-shapes (defaults, sprayCount=2, surfactant=true) and asserts the multipliers; REQ-113 DOM walk + listener-array grep; REQ-114 runtime behavioral (mutate input → diff `#nutr-foliar` text). 99 PASS / 0 FAIL, 74 wired (1 deferred = REQ-102 nursery/fertigation parallel session).
- 16:25 — `nutrition/nursery/fertigation/spec.md` extended with **REQ-122..126** specifying multi-fertigation as a degree of freedom for hitting 100% of weekly demand. (a) **REQ-122** — `nurseryRecipeSupply` gains optional 4th param `applicationsPerWeek ∈ [1,7]` (default 1); per-week supply scales linearly. (b) **REQ-123** — new `minApplicationsPerWeek(recipe, demand, trayVolumeL, ceCap)` returns smallest integer N covering all sourced elements at 100% of demand within EC cap; null if infeasible at N≤7. (c) **REQ-124** — new `nurseryElementsBySource(recipe)` splits demand elements into sourced (frequency can close gap) vs unsourced (needs new product/substrate amendment) — distinguishes Ca structural gap from K dose-bound gap. (d) **REQ-125** — `nurseryRecipeCE` does NOT accept frequency parameter; cap binds per-fertigation, not per-week (the chemistry that makes frequency a useful lever in the first place). (e) **REQ-126** — applicationsPerWeek is integer in [1,7]; fractional values rejected. Spec only — no implementation yet per spec-discipline principle. 5 new REQs deferred until calc.js refactor lands. 91 PASS / 0 FAIL, 66/72 wired.
- evening — yield-range REQs renumbered **102→112, 103→113, 104→114, 105→115, 106→116, 107→117, 108→118, 109→119, 110→120, 111→121** to resolve full collision with tomato/app/spec.md REQ-104..111 (which had been wired earlier in parallel sessions). Bulk sed renumber across all 7 yield-range files (spec.md, app/spec.md, data.js, calc.js, model.js, app/page.html, app/logic.js) in descending order to avoid double-substitution. Yield-range now owns REQ-112 to REQ-121. `npm run build` OK; `npm run check` 91/91 PASS, 66/67 wired.
- evening — yield-range model + admin page implemented (fresh start, post-scrap). yield-range/{data.js, calc.js, model.js} per plant-needs convention. window.YieldRange API. app/page-rendement-content with 2 inputs (plateau toggle, DEL slider 0-18), Capacité plafond labeled display, inline-SVG chart of W(d) vs days from germination with axes labels + reference line at canopyCap + marker at daysToPotential or empty-state text. 'rendement' wired into PAGES + ADMIN_PAGES + setPage + applyAdminMode + admin nav button per CLAUDE.md conventions. npm run build OK; npm run check 91/91 PASS. Yield-range REQ verifier wiring deferred. (REQ numbers later renumbered to 112-121 to resolve collision — see next entry.)
- 16:38 — Tomato Nutrition app **Section 1 visual reorg + Section 2 (Block 1) locked**. (a) Section 1: J/g input dropped from the top input-row and embedded inside the light-ceiling card as an inline editor; ceiling headline now prominent (22px bold) with the input as small inline secondary text below — visual unit, ceiling primary. New `#nutr-light-ceiling-headline` slot for the kg/m²/sem number; container handles the bg/border warning state. (b) Section 2 locked specs REQ-108..111 in `nutrition/tomato/app/spec.md`: REQ-108 Block 1 reads via `PN.calcNutrDemand` (no bare-global lookups in render); REQ-109 modal exposes exactly cert + equation + plugged numbers — interpretation prose, micro caveats, transp-coupling notes all dropped from the build per the spec-discipline principle (single source = `nutrition/tomato/plant-needs/derivation.md`); REQ-110 reactive to target + stage changes (runtime behavioral, no buildNutriment fallback); REQ-111 4-column row layout (Él. / Fruit / Biomasse / Total) with `{fruit, biomass, total}` shape from PN. Equation labels rewritten from `TOMATO_FRUIT_EXPORT[el]`/`BIOMASS_DEMAND[stage][el]` to generic `fruitExport[el]`/`biomass[stage][el]` so they don't false-positive REQ-108's bare-global regex. Verifier regex tightened with negative-lookbehind (`(?<!\.)\b…`) so `PN.BIOMASS_DEMAND` passes while bare access fails. 91 PASS / 0 FAIL, 66 wired (1 deferred = REQ-102 nursery/fertigation parallel session).
- evening — Root `CLAUDE.md` gains a **"Conventions inherited by every page"** section codifying that REQ-001 (CE not EC), REQ-005 (URL hash routing), REQ-006 (Algue not Kelp), REQ-007 (no jargon) apply by default to every routable page/subpage. Page-level `spec.md` files no longer need to list these in an Inherited section unless they explicitly opt out (and document why). REQ-005 specifies what every new page MUST do: register slug in `PAGES`, define `<div id="page-<slug>-content">`, add `setPage` branch, ensure routing setters call `syncHash()`. `yield-range/app/spec.md` Inherited section trimmed to reference the convention rather than re-list each REQ.
- 16:15 — REQ-105 caught a real bug: user reported the J/g input wasn't actually changing the displayed ceiling, but the verifier was passing. Root cause: the verifier check called `buildNutriment()` directly as a fallback, masking a missing `input`-event listener. Browser users have no fallback → the input was disconnected from the render. Two fixes: (a) verifier tightened — removed the `buildNutriment()` fallback; the runtime test now relies entirely on the page's own listener wiring. Confirmed it failed with the broken state. (b) `app/index.html:6013` — input listener list updated: `nutr-solar-per-gram` added (was missing); `nutr-current` removed (input retired in the 15:20 round 1 work, listener was a stale ghost). 87/87 PASS post-fix; REQ-105 now genuinely behavioral. Lesson logged: behavioral specs that fall back to direct function calls on listener absence verify nothing about the user-facing wiring — drop the fallback.
- 16:08 — `NURSERY_PRODUCTS.AcadieKelp` populated from the Acti-Sol datasheet (consumes the doc landed at 16:02). Was treating kelp as ~zero on K + micros; reality per label: K 4.98% (6% K₂O), N 0.6%, P <0.09%, Ca/Mg 0.1%, Fe 60 ppm, Mn 7 ppm, Zn 10 ppm, B 30 ppm, Cu <4 ppm, S 0.3-0.6%. Updated `base{}`, `ions{}`, `phClass{}`, `chemistryTags` (added 'sulfate'), `phContribution` (−0.05 → +0.02 reflecting datasheet pH 7.4-8.2), and product `cert` (3 → 4). Resolves the REQ-060-flagged contradiction where lever copy said "covered by kelp" while the model showed 100% gap on Mn/Zn/B. Build + check 87/87 PASS, 62/63 wired.
- 16:02 — Acti-Sol "ACADIE Fresh Seaweed Concentrate" datasheet added to `nutrition/doc/` and indexed in `nutrition/doc/CLAUDE.md` (slotted under Fertilizer/amendment datasheets, after EZ-GRO Ocean). This is the AcadieKelp product already in `nutrition/nursery/fertigation/data.js:81` — the datasheet supplies the missing micros (B 20-40, Cu <4, Fe 30-90, Mn 3-11, Zn 4-17 ppm) needed to populate `NURSERY_PRODUCTS.AcadieKelp.ions` and close the 100%-gap flagged in the 15:50 entry. Index entry includes crisis-relevant notes (low Ca, alkaline pH 7.4-8.2 at undiluted) and a cert-status flag (data.js says cert 4 Ecocert-listed; the datasheet itself prints no badge).
- evening — `yield-range/` scrapped and rebuilt from scratch under canopy-cap framing. Old files deleted: `spec.md`, `derivation.md`, `data.js`, `calc.js`, `model.js`, `app/{spec.md, derivation.md, page.html, logic.js}`. All yield-range refs stripped from `app/index.html` (3 @include blocks, 'rendement' from PAGES + ADMIN_PAGES, page-rendement nav button + setPage block + applyAdminMode block, buildYieldRange dispatcher). Calibration data preserved at `yield-range/doc/yield-range-calibration-2026-spring.md` (after a brief mis-route through `nutrition/nursery/doc/` mid-pass — restored to canonical location); `yield-range/doc/CLAUDE.md` index re-created. New `yield-range/spec.md` (REQ-102 to REQ-108): time-to-canopy-cap model. Inputs: `plateauSize` (32/50), `ledHours`. Outputs: `canopyCapG`, `daysToPotential`, `trajectory[50]`. **Best non-light conditions assumed** (no f_T, f_CE, f_VPD, f_CO2, f_pH); **canopy cap is the operative ceiling** (not root cap); **logistic growth, no decay**; sun = annual avg constant; spacing always packed. New `yield-range/app/spec.md` (REQ-109 to REQ-111): 2 inputs (plateau toggle, heures DEL slider 0-18), 2 outputs (capacité plafond + chart of head weight vs days from germination). `requirements.md` domain table updated. `npm run build` OK; `npm run check` 87/87 PASS, 62/63 wired (yield-range REQs deferred until code lands).
- 15:52 — Tomato Nutrition app **Section 1 round 2** under new user-wide "Spec discipline" principle (saved to `~/.claude/CLAUDE.md`: build only what the spec demands; if not in spec, not in build; re-check earlier specs for relevance after every change). Applied: (a) REQ-105 verifier rewritten to **runtime behavioral** — reads `#nutr-light-ceiling` text at default `solarPerGram=7`, mutates input to `14`, triggers re-render via `buildNutriment()` (with input-event fallback), re-reads, fails if displayed text unchanged. Source-grep belt retained for default + no-`/7000`. (b) `logic.js` light-ceiling display trimmed to one line (`Plafond lumière ≈ X.XX kg/m²/sem` + optional warning) — formula breakdown text dropped per principle. (c) `syncNutrRecipeModeUI` recipe-mode helper note emptied in both branches — toggle + active-state styling carry the mode signal. (d) REQ-107 verifier extended to assert `#nutr-recipe-mode-note` text content empty. REQ-105 Statement + REQ-107 spec body unchanged from round 1 — principle does the pruning, not the spec text. 87 PASS / 0 FAIL, 62 wired (1 deferred = REQ-102 in `nutrition/nursery/fertigation/spec.md`, pre-existing from parallel session).
- 15:50 — Nursery subprojects integrated into `app/index.html`. (a) Inline `LETTUCE_NURSERY_*` constants + `calcLettuceNurseryDemand` replaced by 9 `@include` markers (data → calc → model × {plant-needs, substrate-contribution, fertigation}). (b) `buildNutrimentNursery()` consumer updated: `calcLettuceNurseryDemand` → `calcNurseryDemand` (no Lettuce prefix); `NURSERY_DEFAULTS.trayAreaM2` → `NURSERY_TARGETS.trayAreaM2`; output keys `perPlant`/`perTray` → `perPlant_mg`/`perTray_mg`. (c) Target default 150 → 90 g (per design discussion). (d) Block 2 (Substrate) live-renders from `window.SubstrateContributionNursery.cycleAverageReleasePerTray`; Block 3 (Fertigation) live-renders default recipe + predicted CE + tank pH from `window.FertigationNursery`; Block 4 (Leviers) auto-derives gap chain (demand − substrate − fertigation), sorts by % missing, renders per-element advice via new `nurseryLeverFor(el)` (mirrors `lettuceLeverFor` pattern). 87 PASS / 0 FAIL. Known gap: lever copy for Fe/Mn/Zn says "covered by kelp" while the model shows 100% gap — kelp's micro contribution wasn't quantified in `NURSERY_PRODUCTS.AcadieKelp.ions`; populate at next pass to align REQ-060.
- 15:48 — `yield-range-calibration-2026-spring.md` re-homed to `yield-range/doc/` (revised from the earlier 15:35 move to `nutrition/nursery/doc/` — co-locating with the model that consumes it beats co-locating with the crop subproject). New `yield-range/doc/CLAUDE.md` index created. `nutrition/nursery/doc/CLAUDE.md` reverted to its prior shape (no Field-observations section). `doc/CLAUDE.md` pointer rephrased to "domain-specific calibration datasets live next to the model that consumes them". Stale references fixed: `yield-range/spec.md` (1 occ.) and `yield-range/derivation.md` (5 occ.) updated from `calibration-data.md` → `doc/yield-range-calibration-2026-spring.md`; `requirements.md` doc table entry updated. CLAUDE.md root convention line still names `*/calibration-data.md` as a sample filename pattern — left untouched (convention discussion, not a broken path).
- 15:35 — `yield-range-calibration-2026-spring.md` moved from `doc/` to `nutrition/nursery/doc/` (closer to the nursery subproject it anchors). Index entries updated in both `doc/CLAUDE.md` (Field observations section removed; pointer left to nursery/doc) and `nutrition/nursery/doc/CLAUDE.md` (new Field observations section added). Stale references in `yield-range/spec.md`, `yield-range/derivation.md`, `requirements.md` still point at the legacy `calibration-data.md` filename — pre-existing from the prior `yield-range/` → `doc/` move; not fixed in this pass.
- 15:20 — Tomato Nutrition app: **Section 1 (Cible & contexte) locked**. REQ-104..107 added to `nutrition/tomato/app/spec.md` + wired in `scripts/check-recipes.mjs`. (a) **REQ-104** — header inputs are exactly five scalars (`target`, `solarPerGram`, `stage`, `phLocked`, `recipeMode`); `nutr-current` retired (page now answers "what's needed at target", aligning to plant-needs spec framing — `transpFactor` pins to 1.0; mid-cycle stunted-canopy correction lost, operator-discoverable not silent, cert 4 acknowledged). (b) **REQ-105** — light ceiling formula now `weekly_J_cm² ÷ (solarPerGram × 1000)` driven by new `nutr-solar-per-gram` input (default 7); no hardcoded `/7000` literal. (c) **REQ-106** — FP mode locks stage to T5 (auto-revert + snap), default `recipeMode = 'fp'` already wired. (d) **REQ-107** — toggle button order flipped: `[First principles] [Stockée]` (FP on left, English term-of-art kept untranslated, default-selected on initial render); `nutr-products` block removed from header (per-block products surface inline in Block 2-5). Header card stripped to 5 scalar inputs + ceiling display + stage selector + pH-lockout + mode toggle. 87 PASS / 0 FAIL, **62/62 wired**.
- 14:53 — `nutrition/tomato/fertigation-recipe/` subproject created (alongside sidedress-recipe / plant-needs, mirrors conventions). Owns `FP_RECIPE_T5.fertigation` (PA Taillon April 2026 anchor: K2SO4 5167 g, MgSO4·7H2O 1379 g, Solubore 9 g per cycle/area), `MIXING_FACTOR_FERT` (split into mode-aware `MIXING_FACTOR_FERT_STORED = 0.5` / `MIXING_FACTOR_FERT_FP = 1.0`, legacy alias `MIXING_FACTOR_FERT` = STORED for backwards-compat at the consumer line), `computeStageRecipe(stage)` (mass-balance K + Mg derivation, compost subtracted — pre-existing behavior, the "no compost subtraction" framing comment expresses a future-policy direction not yet wired through), and `wireFpFertigation()` IIFE that overwrites `FP_RECIPE_T5.fertigation` from `FIRST_PRINCIPLES_T5_FERTIGATION` (data.js) at script load. Files: `spec.md` (Contract / Cert scale link / INV-1 stage closure / REQ-098 mass-balance derivation matches formula / REQ-099 namespace `window.FertigationRecipeTomato` / REQ-100 mode-aware mixing factor with stored < fp and fp pinned at 1.0 / Inherited REQ-013/014/022/061/083/087/088), `derivation.md` (mass-balance K+Mg derivation + JS source, "policy vs implementation drift" section explaining compost-subtraction discrepancy + reconciliation plan, T5 refined-target derivation per element with PA Taillon anchor numbers, mode-aware mixing factor mechanism, per-stage table, caveats, refinement triggers, implementation map, what-extracted-vs-left-in-place), `data.js` (`MIXING_FACTOR_FERT_STORED`, `MIXING_FACTOR_FERT_FP`, `MIXING_FACTOR_FERT` legacy alias, `FIRST_PRINCIPLES_T5_FERTIGATION` constant), `calc.js` (`computeStageRecipe` + `wireFpFertigation` IIFE), `model.js` (declares `window.FertigationRecipeTomato` exposing `MIXING_FACTOR_STORED`, `MIXING_FACTOR_FP`, `FIRST_PRINCIPLES_T5`, `computeStageRecipe`). 3 `@include` markers wired into `app/index.html` (data → calc → model order) immediately after the inline `RECIPE_INPUTS` block + `wireFpSidedress` neighborhood — placed BEFORE the existing sidedress-recipe calc.js/model.js includes (sidedress doesn't depend on `computeStageRecipe`). Inline `MIXING_FACTOR_FERT = 0.5` declaration removed (line ~4527); replaced by a pointer comment to data.js. `FP_RECIPE_T5.fertigation` inline values kept as placeholders (overwritten by IIFE at load). LUXURY_FACTOR left in place next to `calcNutrSupply` (supply-side concern, not recipe-derivation). `STORED_RECIPE.tomato.fertigation` left untouched (audit-trail concern, governed by `/retire-recipe`). Verifier (scripts/check-recipes.mjs) gains `exposeNames` entries (`FP_RECIPE_T5`, `MIXING_FACTOR_FERT_STORED`, `MIXING_FACTOR_FERT_FP`, `FIRST_PRINCIPLES_T5_FERTIGATION`) + 3 new `header()` blocks inserted right after REQ-089: REQ-098 recomputes formula and asserts `computeStageRecipe(stage).{kSulfate, mgSulfate}` within ±5 g for every stage; REQ-099 namespace presence + 4-key surface + T5 shape; REQ-100 asserts `MIXING_FACTOR_STORED < MIXING_FACTOR_FP` and `MIXING_FACTOR_FP === 1.0`. Build OK (509,135 chars). 83 PASS / 0 FAIL, **58/58 wired** (3 added by this work — REQ-098/099/100; foliar agent's REQ-101/103 also wired concurrently for combined +6 over the 77-PASS baseline).
- 14:50 — `nutrition/tomato/foliar-recipe/` subproject created (alongside sidedress-recipe / fertigation-recipe / plant-needs, mirrors conventions). Owns `FOLIAR_COVERAGE_DEFAULT` (0.30 no-yucca, cert 4), `FOLIAR_COVERAGE_WITH_YUCCA` (0.80, refinement-trigger), `computeFoliarSupply(stage)` (per-element mg/m²/wk delivered under coverage). Files: `spec.md` (normative — INV-1 element coverage closed, REQ-101 coverage discount applied with formula `delivered = recipe_g × element_pct × 1000 / area × FOLIAR_COVERAGE_DEFAULT`, REQ-103 namespace, Pending yucca-coverage refinement trigger, Inherited specs REQ-013/014/018/022/025/053/055/061/062/083), `derivation.md` (burn-cap-constrained framing — foliar dose pinned at burn-safe maximum, NOT mass-balance-derived; coverage 0.30 rationale; 2026-05-05 yucca-drop dose cuts Cu 4→2 g / Mn 22→18 g / Zn 22→16 g; per-element delivered mg/m²/wk vs T5 demand table; no-macro-by-design; refinement triggers; implementation map), `data.js` (constants + commentary), `calc.js` (`computeFoliarSupply` reads STORED_RECIPE.tomato.foliaire.A label strings × PRODUCT_PCT × FOLIAR_COVERAGE_DEFAULT), `model.js` (declares `window.FoliarRecipeTomato` exposing 4 keys: AREA_M2 getter, FOLIAR_COVERAGE_DEFAULT, FOLIAR_COVERAGE_WITH_YUCCA, computeFoliarSupply). 3 `@include` markers wired into `app/index.html` after STORED_RECIPE / PRODUCT_PCT / TOMATO_NUM_BEDS·BED_AREA, after sidedress-recipe + fertigation-recipe blocks (data → calc → model order). `calcNutrSupply` foliar block (~50 lines) refactored: stored mode now delegates to `window.FoliarRecipeTomato.computeFoliarSupply(stage)`; FP mode keeps inline path reading from `FP_RECIPE_T5.foliar` (foliar FP mirrors stored — burn-cap-constrained — so the inline path stays meaningful for Block 7 drift gauge). `raw{}` traceability fields (mnSO4_g, znSO4_g, etc.) preserved. Verifier checks added in scripts/check-recipes.mjs: INV-1 stage × 11-element coverage + non-negative, REQ-101 Mn + Fe delivered match formula ±1 %, REQ-103 namespace shape + T5.Fe positive. **REQ-102 dropped — burn cap inherited from cross-crop REQ-025 already wired (predictedCE < 10 mS/cm tomato leaf burn cap, scripts/check-recipes.mjs:1493). nutrition/tomato/spec.md gains a Subprojects entry pointing at foliar-recipe (and backfills sidedress-recipe). 80 PASS / 0 FAIL, 55/58 wired (3 deferred = fertigation-recipe REQ-098/099/100 to be wired by parallel agent).
- 14:30 — **REQ-028 retired.** "Cert annotation on every empirical constant" was the last manual-review entry. Per the codified rule "if not auto-enforceable, not a spec" (CLAUDE.md Specs section), removed from `nutrition/spec.md`. Cross-references cleaned in 4 inherited-spec sections (compost-contribution, sidedress-recipe, nursery/plant-needs, nursery/substrate-contribution) and 2 comments in `scripts/check-recipes.mjs`. Cert annotations remain a team convention enforced socially — every new constant in `data.js` files carries `cert N` inline as before; removing the spec just acknowledges the rule isn't auto-enforceable in a principled way (grep would over- or under-count without a curated denominator). 77 PASS / 0 FAIL, **53/53 wired (0 deferred)**.
- 14:25 — `nutrition/nursery/substrate-contribution/` subproject scaffold added (data + calc + model + spec + derivation). Mirrors compost-contribution / nursery/plant-needs layout. Owns `OM2_STARTER_CHARGE_PPM` (5 macros, cert 2-3 placeholders pending Berger OM2 datasheet pull), `OM2_RELEASE_CURVE_BY_WEEK` (`[0.50, 0.30, 0.15, 0.05, 0.00]`), `FEATHER_MEAL_LABEL_PCT` (`{N: 0.13}`), `FEATHER_MEAL_MINERALIZATION_FRAC` (0.75, Sonneveld), `FEATHER_MEAL_RELEASE_CURVE_BY_WEEK` (`[0.10, 0.25, 0.25, 0.25, 0.15]`), `NURSERY_TRAY_SUBSTRATE_VOL_L` (1.65), `NURSERY_FEATHER_MEAL_DEFAULT_G_PER_TRAY` (9 — derived from 2 cups/sac × 30 trays/sac), `LIMITS.maxFeatherMealPerTrayG` (9, hard germination cap), `theoreticalSubstrateReleasePerWeek(week, fmG)` → `{N,P,K,Ca,Mg}` mg/tray/wk, `cycleAverageReleasePerTray(fmG)`, `window.SubstrateContributionNursery` namespace (10 keys). REQ-094..097 wired in node verifier (REQ-094 front-load cap ≤ 9 g + INV-2 release-curve mass balance, REQ-095 linearity in feather meal + OM2 invariance, REQ-096 cycle-average matches mass balance ±10 %, REQ-097 public-API namespace shape + INV-1 element-coverage closure). Verifier loads partials via Node `vm` sandbox so checks pass pre-integration; `exposeNames` updated to forward the namespace once integration lands. OM2 starter charge values are cert 2 placeholders pending datasheet pull — flagged in spec + derivation refinement triggers. Integration into `app/index.html` (replace "2. Réserve substrat — à venir" placeholder card with live-rendered substrate release block) deferred to orchestrator. 77 PASS / 0 FAIL, 53/53 wired.
- 14:15 — `nutrition/nursery/plant-needs/` subproject scaffold added (data + calc + model + spec + derivation). Mirrors compost-contribution / tomato/plant-needs layout. Owns `LETTUCE_NURSERY_TISSUE_DW` (11-element tissue conc), `LETTUCE_NURSERY_DM_FRACTION` (0.07), `NURSERY_TARGETS` ({targetG_default 90, cycleDays_default 35, cellsPerTray_default 50, traysPerCohort_default 50, trayAreaM2 0.149}), `calcNurseryDemand(targetG, cycleDays, cellsPerTray)` returning `{perPlant_mg, perTray_mg}` per element, `window.PlantNeedsNursery` namespace (5 keys + `demandPerTray(el)` convenience). REQ-090..093 wired in node verifier (INV-1 element-coverage closure, REQ-090 linearity in targetG ±0.1 %, REQ-091 inverse-linearity in cycleDays ±0.1 %, REQ-092 N demand ∈ [50, 70] mg/plant/wk sanity band, REQ-093 public-API namespace shape). Verifier loads partials via Node `vm` sandbox so checks pass pre-integration; `exposeNames` updated to also forward the namespace once integration lands. Refactor of existing inline `LETTUCE_NURSERY_TISSUE_DW` / `LETTUCE_NURSERY_DM_FRACTION` / `NURSERY_DEFAULTS` / `calcLettuceNurseryDemand` blocks in `app/index.html` (~50 lines) pending integration — orchestrator's responsibility. 70 PASS / 0 FAIL.
- 14:10 — Sidedress-recipe extended for alfalfa + REQ-089 generalized to "no Ca-bearing sidedress product." SIDEDRESS_PRODUCTS table replaces flat SIDEDRESS_MIN_EFF, declares ca_pct per product. computeStageSidedress(stage, product) picks Ca-free products; default 'FarinePlumes'. Eco-luzerne 3-0.5-2 added (cert 2). Spec REQ-089 + verifier updated. Pending alfalfa option in spec + derivation marked resolved. 65/65 PASS.
- (now) — `/new-subproject` skill added (`.claude/skills/new-subproject/SKILL.md`). Asks for intent, path, mode (from-scratch vs. extracting existing work), code-component yes/no; for extraction also asks which REQs/sections/symbols to move. Scaffolds `spec.md` (+ optional `derivation.md` / `data.js`+`calc.js`+`model.js`), updates parent spec with Subprojects pointer, wires `@include` markers, runs `npm run check`, appends changelog entry. Does NOT manage REQ-range allocation (per request) — Guillaume picks numbers himself or they ride along during extraction. Mirrors plant-needs / sidedress-recipe / compost-contribution / yield-range conventions.
- 14:05 — Repo root cleanup: build now outputs to `dist/` (gitignored). 5 static assets (`favicon.ico`, `favicon-32x32.png`, `apple-touch-icon.png`, `historique.html`, `history.json`) moved from root to `app/`; `build.mjs` copies them into `dist/` alongside the assembled `index.html`. Root `index.html` deleted (was the committed build artifact). `npm run check` now chains `build` first. `dev-server.sh` serves `dist/`. `.github/workflows/deploy.yml` adds Node 22 setup + `npm install` + `npm run build`, uploads `path: 'dist'`. `.github/workflows/notify-team.yml` reads/writes `app/history.json` (next deploy rebuilds dist). `scripts/check-recipes.mjs` and `scripts/check-requirements.sh` read `dist/index.html`. `npm run build` (17ms) + `npm run check` (65/65 PASS). Heads-up: Netlify staging dashboard needs publish-dir=`dist` and build-cmd=`npm run build` (no `netlify.toml` in repo).
- evening — yield-range model + admin page implemented. yield-range/{data.js (constants from REQ-063 to 071), calc.js (6 stress functions, predictNurseryYield with logistic + senescence, computeBestStrategy 4-combo sweep), model.js (window.YieldRange API)}. app page-rendement-content with 4 inputs (plateau, semaines, stratégie nutritive, DLI slider 5-29), 4 outputs (rendement par plant, jour de pic, g/m²/an, stratégie optimale), 7-item info block. Admin nav entry added. npm run build OK; npm run check 65/65 PASS. REQ wiring into verifier deferred.
- evening — yield-range/spec.md split into spec.md (REQ-063 to 071, bare normative) + derivation.md (formulas, stress functions, constants rationale, RGR_max + DLI history, calibration anchors, implementation map). calibration-data.md enhanced with per-row "what this row teaches" annotations + new "Model behaviors not yet anchored by data" section. Domain table in requirements.md updated.
- 13:45 — `nutrition/tomato/sidedress-recipe/` subproject created (alongside plant-needs, mirrors its conventions). Owns `SIDEDRESS_AREA_PER_PLANCHE`, `SIDEDRESS_MIN_EFF`, `FIRST_PRINCIPLES_SIDEDRESS`, `computeStageSidedress`, `wireFpSidedress` IIFE — extracted from `app/index.html` (~80 lines moved). Files: `spec.md` (normative — INV-1 stage coverage, REQ-087 mass-balance, REQ-088 namespace, REQ-089 Ca-aware Actisol-stays-zero, Pending alfalfa option, Inherited from REQ-013/014/022/028/080/083), `derivation.md` (mass-balance derivation + JS source, N-only by-design rationale per element, per-stage values table, mineralization efficiencies + Sonneveld cross-ref, why farine_g is hard zero at T1/T2, caveats, refinement triggers, implementation map), `data.js` (`SIDEDRESS_AREA_PER_PLANCHE`, `SIDEDRESS_MIN_EFF`, `FIRST_PRINCIPLES_SIDEDRESS` skeleton), `calc.js` (`computeStageSidedress` + `wireFpSidedress` IIFE), `model.js` (declares `window.SidedressRecipeTomato` exposing 4 keys). 3 `@include` markers wired into `app/index.html` (data → calc → model order). Verifier checks added: INV-1 stage coverage + numeric output, REQ-087 farine_g matches mass-balance ±5g, REQ-088 namespace shape, REQ-089 actisol_g === 0 across all stages. REQ numbers chosen as 087/088/089 to avoid collision with yield-range REQ-084 (DLI slider). 65/65 PASS, 45/46 wired (REQ-028 still the only deferred — manual-review-by-design).
- evening — `yield-range/app/` brought into new spec convention + DLI promoted to slider input. (a) **Spec/derivation split**: `yield-range/app/spec.md` trimmed to bare normative REQs (Statement / Rationale / Verification / Cert format); contract overview kept as a 4-line bullet list, no source columns or inline math. New `yield-range/app/derivation.md` holds source rationale for each info-block value, DLI bound math, design history (espacement dropped, transplant-readiness dropped, multi-stage out of scope, 8-block layout dropped), REQ tally history, deferred implementation map. (b) **DLI promoted to user input** as new **REQ-084** (REQ-079/080 already taken by compost-contribution at 13:25 — used 084 to avoid collision). Slider, range 5 to 29 mol/m²/j (5 = winter low; 29 = annual-avg sun 16 + LED at 18h 12.96), default 27,5. Removed from REQ-078 info block (now 7 items, was 8). Rationale: Quebec sun varies ~5–35 across the year — hardcoding annual avg hid the largest environmental swing. (c) REQ-072 updated to "4 inputs, 4 outputs". Yield-range REQ range now 063–078 + 084.
- 13:33 — RECIPE_INPUTS.compostReleasePerWeek consumers migrated to window.CompostContribution.releasePerWeek (computeStageRecipe, computeStageSidedress, buildNutriment, buildNutrimentLettuce). Delegating key removed from RECIPE_INPUTS. Spec-text references updated. 61/61 PASS.
- 13:25 — `nutrition/compost-contribution/` subproject created (cross-crop, not under tomato/ — same Savaria amendment was applied to lettuce planches too, and both subpages already read from the same data). Mirrors plant-needs conventions: `spec.md` (normative — INV-1 + REQ-079 mass-balance band + REQ-080 namespace + Pending decline curve + Inherited from REQ-002/022/028), `derivation.md` (Savaria source product, mass-balance formula, per-element table with cert per element, Mg label-gap conservative override, refinement triggers, implementation map), `data.js` (`COMPOST_AMENDMENT`, `COMPOST_LABEL_PCT`, `COMPOST_MINERALIZATION_YEAR1`, `COMPOST_SEASONAL_FACTOR`, `COMPOST_RELEASE_PER_WEEK`), `calc.js` (`theoreticalReleasePerWeek(el)` mass-balance helper), `model.js` (declares `window.CompostContribution` exposing 6 keys). `RECIPE_INPUTS.compostReleasePerWeek` now delegates to `COMPOST_RELEASE_PER_WEEK` — legacy consumers untouched, new ones go through `window.CompostContribution.releasePerWeek`. **REQ-079** wired (compost release within ±50% of mass-balance theoretical — catches transcription errors while allowing Mg conservative override). **REQ-080** wired (namespace presence + 6-key surface). `app/index.html` lost ~25 lines (inline compostReleasePerWeek block replaced by 3 `@include` markers + a delegating reference). 61/61 PASS, 42/43 wired (REQ-028 still the only deferred).
- evening — Yield-range **DLI correction + spec convention compliance**. (a) DLI was LED-only (11,5 mol/m²/j = 200 µmol × 16h). Greenhouse gets natural sun too — added `DLI_SUN_GH_ANNUAL_AVG_QC = 16` (cert 2, Quebec GH ~55% PAR transmission, annual avg) and `DLI_LED = 11,5`; total `DLI_BENCH_AVG = 27,5`. With corrected light, packed-canopy plants past d18 are no longer light-starved (per-plant DLI ≈ 11,2, `f_light ≈ 0.9`). (b) `RGR_MAX_LETTUCE_NURSERY` refit 0.35 → **0.22** — the 0.35 was compensating for the spurious LED-only light starvation; 0.22 is closer to the lit value for mature lettuce, and re-fits the same 4 Décembre cohorts under corrected DLI. d28→d35 mass loss is now attributed to bolting + heat + root-cap saturation (not light starvation). (c) `yield-range/app/spec.md` REQs 072-078 updated to new CLAUDE.md spec convention: each REQ now has explicit `Verification: deferred — wired when page lands` status, plus a `Cert:` line (5 for user-stated REQs; 4 for REQ-076 due to `TRAY_AREA_M2` 32-cell approximation). REQ-078 info block updates DLI banc value 11,5 → 27,5 with sun+LED decomposition. `calibration-data.md` notes the correction. Math spec (`yield-range/spec.md`) still has derivation content inline — full split into `derivation.md` deferred until the model lands in code.
- 12:55 — `/document` skill added at `~/.claude/skills/document/SKILL.md` (user-scoped — available in all projects). Three-step atomic procedure for adding a primary-source doc (PDF / lab report / datasheet / certificate / correspondence / stub): move file into target docs dir, write reach-for-this-when entry in that dir's `CLAUDE.md` (matching its existing format, scaffolding the index if missing), append a changelog line if the project keeps one. Project-agnostic — adapts to whatever index pattern exists locally. Anti-patterns spelled out (no data inlining, no inventing verification status, no half-done two-turn moves, no imposing CLAUDE.md when the project uses another index filename).
- 12:38 — Plant-needs **spec / derivation split** + root **CLAUDE.md "Specs" section**. (a) `nutrition/tomato/plant-needs/spec.md` trimmed from 440 → 158 lines: kept Contract, Cert scale, INV-1, REQ-081/082/083, pending tissue back-test placeholder, Inherited specs. Each REQ now Statement / Rationale / Verification / Cert only — no long context paragraphs, no cross-check tables, no implementation walkthroughs. (b) New `nutrition/tomato/plant-needs/derivation.md` (251 lines) holds everything cut from spec.md: formula + JS source, Term 1 / Term 2 source tables and per-element rationale, unit-convention quirk, per-(stage, el) cert table values, edge cases, caveats, refinement triggers, implementation map. (c) Root `CLAUDE.md` gains a "Specs" section codifying the convention: spec = normative + testable + minimal; derivations live in adjacent `*/derivation.md`; every REQ has globally-unique id + verification path + cert; "if not auto-enforceable, not a spec." Tally now 40/41 wired — the 1 deferred is REQ-028 (cert-annotation grep, manual-review-by-design). Was masked before because v1 spec used `### REQ-082` (h3) which the doc-counter skipped, balancing REQ-028's lack of wire; new spec uses `## h2` throughout so the real state surfaces.
- 12:35 — `nutrition/nursery/doc/` created. Moved Xavier Villeneuve-Desjardins (CETAB+, agr.) **24 avril 2026 nursery recommandation PDF** in. Indexed in new `nutrition/nursery/doc/CLAUDE.md`: lettuce + spinach 3+1-week protocol, OM2 Berger + Selectus substrate (3 kg/m³ lettuce, 5 kg/m³ spinach), 4-product fertigation recipe across 4 ppm tiers (50/75/100/150) — fish emulsion, K₂SO₄, EZ-Gro Océan 15-1-1, MgSO₄ — and PourThru EC targets (lettuce 0,4-0,6 → 1,0-1,2 mS/cm; spinach 0,7-0,9 → 1,4-1,8). EZ-Gro Océan flagged as CETAB+ best-N-per-$ for organic. Caveat noted: Xavier (nursery) is a second agronomist alongside PA Taillon (production); surface conflicts when their advice diverges.
- 12:30 — buildNutriment routed through window.PlantNeedsTomato (PN.calcNutrDemand, PN.BIOMASS_DEMAND, PN.TOMATO_FRUIT_EXPORT, PN.certFor). Bare-global access removed for plant-needs symbols. 59/59 PASS.
- evening — `yield-range/app/spec.md` extended for strategy-tool scope. Inputs unchanged (3): plateau (32/50), semaines germ→transplant, stratégie nutritive. Outputs grew from 1 → **4**: rendement par plant, **jour de pic de croissance** (= `optimalHarvestDay` from math model), **rendement par m² par an** (`plantsPerM2 × gPerSeedling × cyclesPerYear`), **stratégie optimale** (auto-sweep over 4 combos of plateau×stratégie nutritive, each at its peak day, surface highest g/m²/yr). New REQs: **REQ-075** (jour de pic de croissance is top-level output), **REQ-076** (rendement par m² par an formula + TRAY_AREA_M2=0.149 constant), **REQ-077** (stratégie optimale auto-sweep evaluates exactly 4 combinations). REQ-072 updated to "3 inputs, 4 outputs" (was "3 inputs, 1 output"). REQ-073/074 unchanged (live update; binary nutrient toggle). Espacement input dropped after operator critique: spreading trays only helps edge plants in 50-cell grid, weak lever. Density now = tray choice only. Production-bed/system scope (5 beds, allocation, alternate locations) explicitly out of scope. Transplant-readiness output dropped per operator request — replaced by jour de pic de croissance. Yield-range REQ range now 063-077; 078-080 free.
- 12:18 — `nutrition/doc/CLAUDE.md` gains a stub entry for **Eco-luzerne 3-0.5-2** (Acti-Sol) — no PDFs in repo yet. Pure alfalfa pellet, no Ca-loading carriers → crisis-compatible (unlike Selectus). Open items flagged: organic-cert status unverified, NPK label (3-0.5-2 vs. older "3-1-2") to confirm on bag, non-GMO source. Already wired in `app/index.html:534+` as the alfalfa half of the proposition tomato sidedress.
- 12:15 — `nutrition/doc/selectus-1-organic-4-2-5/` added (moved from `farm info/selectus fertilizer/`). 4 docs: fiche technique (FR), bilingual label, Ecocert evaluation Nº 164357 (valid through 2026-11-13, listed as soil amendment under CAN/CGSB-32.311 §4.2), FDS. Indexed in `nutrition/doc/CLAUDE.md` with crisis-relevant caveat: 4-2-5 NPK + 3.7-4.5% Ca + 1% Mg + 2.3% S, ingredients include gypsum and bone meal — wrong direction while soil is Ca-saturated and pH 7.3-7.5; reconsider only after bank/pH normalize.
- 12:08 — Plant-needs **internals migrated into the subproject**. All constants and functions defined by REQ-070..-083 (TOMATO_REMOVAL, TOMATO_FRUIT_EXPORT, BIOMASS_DEMAND, TOMATO_DEMAND_CERT, TRANSP_COUPLED_BIOMASS, calcNutrDemand) now live under `nutrition/tomato/plant-needs/` — split into `data.js` (constants, 328 lines) and `calc.js` (function + transp-coupled set, 35 lines). `app/index.html` lost ~360 lines (replaced by two `@include` markers). Build artifact byte-identical (md5-verified). Spec implementation pointers updated. 59/59 PASS, 40/40 wired. Subproject is now self-contained: `data.js` + `calc.js` + `model.js` (public API) + `spec.md` (contract).
- 12:00 — Plant-needs **public API hooked**. `nutrition/tomato/plant-needs/model.js` declares `window.PlantNeedsTomato` exposing 8 keys: 5 read-only tables (`TOMATO_FRUIT_EXPORT`, `BIOMASS_DEMAND`, `TOMATO_DEMAND_CERT`, `TOMATO_REMOVAL`, `TRANSP_COUPLED_BIOMASS`) + 3 functions (`calcNutrDemand`, `demandTotal`, `certFor`). `@include` marker placed in `app/index.html` after `calcNutrDemand` definition (line 4941) and before all consumers. **REQ-083** wired in verifier — asserts namespace exists with all 8 keys and that convenience functions return shape-correct values; structural cert 5. Consumer pattern documented in plant-needs/spec.md: `const PN = window.PlantNeedsTomato; PN.demandTotal(target, stage, tf)` for recipe-side gap math; `PN.calcNutrDemand(...)` when fruit/biomass split is needed (Bilan modal). Build artifact rebuilt; 59/59 PASS, 40/40 wired (was 39/39).
- evening — `yield-range/app/spec.md` trimmed from 9 REQs to **3 REQs** (REQ-072/073/074). Removed REQ-075 to REQ-080 — those were layout/visual decisions, not contract specs. The minimal page contract is: 3 inputs (plateau 32/50, semaines de germination à transplantation, stratégie nutritive actuelle/parfaite), 1 output (rendement prédit g/seedling, live). Environmental inputs (T_day, T_night, VPD, CO₂, DLI) hardcoded as model defaults — not page inputs. Page input "semaines de germination" maps to model `cycleDays` as `weeks × 7 + 5` (germination offset). REQs 075-080 dropped from the global pool (yield-range range now uses 063-074, freeing 075-080 for future use).
- 11:35 — Plant-needs spec reform applied (3 items from yesterday's critique). (a) **REQ-081** added + wired: Ca and Mg biomass terms in `calcNutrDemand` are now multiplied by `transpFactor` (xylem-mobile elements; uptake = mass-flow × concentration, not a fixed demand). Code change in `app/index.html` `calcNutrDemand` signature + `nutrition/tomato/app/logic.js` plumb-through + per-element pourquoi modal flag. Verifier: tf=1.0 vs 0.5 ratio test on Ca/Mg = 0.5, others unchanged. (b) **Cert recalibrated** to single transferability scale (0-5; transferability subsumes source quality): macros across T1-T3/T5 from cert 3-4 → cert 2 (Mediterranean hydroponic → QC organic is one major translation), T4 cert 1 (extrapolation of cert-2), micros stay cert 1. `TOMATO_DEMAND_CERT` constant updated. Tissue test results land ~2026-05-12 → push affected entries to cert 3. (c) **REQ-082** added + wired: `BIOMASS_DEMAND` stage-transition continuity — `|Δ| / prev ≤ 2.5` per element. Catches order-of-magnitude hand-edit errors while allowing legitimate phenological spikes (T2→T3 P at flowering is +167%, T3→T4 Fe drop is −72%). Plant-needs spec invariants pruned: dropped trivial INV-2 (monotone), INV-3 (non-negative), INV-5 (T1 fruit-zero-by-input), and tautological INV-1 (split-sum is by-construction); kept element-coverage-closed as INV-1; placeholder for tissue-test back-test invariant pending data. REQ-072/073 → REQ-081/082 to avoid yield-range collision (063-080 reserved). 58/58 PASS, 39/39 wired.
- evening — `yield-range/app/spec.md` written. New top-level admin page (slug `rendement`, container `page-rendement-content`) with 8 blocks: Cohorte/conditions (0), Récolte prédite headline (1), Trajectoire curve (2), Contrainte limitante (3), Top 3 leviers (4), Risques de qualité (5), Données calibrées (6), Hypothèses (7). Nine app REQs added: **REQ-072** (8 blocks), **REQ-073** (`optimalHarvestDay` is headline), **REQ-074** (regression warning when `cycleDays > optimalHarvestDay`), **REQ-075** (risk flags surface visually with reason+threshold+consequence; empty-state message), **REQ-076** (calibration status `n=4 minimum viable` + observed-vs-predicted table), **REQ-077** (top 3 levers with paired g/plant + kg/cohorte deltas), **REQ-078** (two presets: Zone tomate / Pépinière dédiée — A/B for heat hypothesis), **REQ-079** (cycle-average inputs only, no daily/weekly series), **REQ-080** (Hypothèses card enumerates all spec constants). Inherits REQ-001 (CE) and REQ-060 (no narrative drift). No code yet — spec only. Domain table in `requirements.md` updated.
- evening — `yield-range/` math-model refit against Décembre's 2026 spring batch (4 observed cohorts: d21=4g, d28=16g, d35=10g, d35=12g). Captured raw data + qualitative observations + operator hypothesis in new `yield-range/calibration-data.md`. Spec changes: `RGR_MAX_LETTUCE_NURSERY` 0.20 → **0.35** (cert 3, n=4); model formulation gains logistic factor `(1 − W/RootCap)` for natural deceleration; new **senescence branch** (decay when `growth < 2%` AND `Π stress < 0.5`) with `DECAY_RATE=0.10`; new headline output **`optimalHarvestDay`** = last day before dW/dt < 0; new **`bolting` risk flag** at `BOLTING_TDAY_THRESHOLD_C=26` (operator hypothesis: nursery in tomato zone runs too warm for lettuce); spacing curve shifted right (closure d14–28, floor 0.40 at d30+) — earlier values were too aggressive vs photo evidence; `f_Tday` re-banded with bolting threshold inside the 22–26 range. New REQs: **REQ-070** (senescence + optimalHarvestDay), **REQ-071** (bolting flag). Calibration anchor #1 status changed from "uncalibrated" to "minimum viable (n=4); refit at n≥5".
- evening — `yield-range/` project scaffolded. `yield-range/spec.md` is the math-model spec for Salanova nursery yield prediction (RGR-multiplier with environmental stress factors, hard RootCap). Inputs: cycle-average per environmental variable. Constants reserved: `RGR_MAX_LETTUCE_NURSERY=0.20`, `SHOOT_PER_ML_SUBSTRATE=1.6`, `YIELD_BAND_FACTOR_LOW/HIGH=0.85/1.15`. Five stress functions (light/Tday/Tnight/CE/VPD/CO₂) with piecewise-linear thresholds. Spacing curve hardcoded packed (floor 0.35 after d18). REQ-063 to REQ-069 reserved for the model (packed-canopy decay, no pH-lockout, photoperiod-weighted env inputs, VPD in g/m³, ±15% band, Salanova-only, binding-constraint output). No code yet — spec only. App-side specs deferred to `yield-range/app/spec.md`. Domain table in `requirements.md` updated.
- 10:35 — `nutrition/tomato/plant-needs/` subproject created. `plant-needs/spec.md` (334 lines) is the canonical spec for the demand model — contract (inputs / outputs), formula, Term 1 fruit export with TOMATO_REMOVAL × Yara split rationale, Term 2 biomass per stage with sources + Haifa→mg conversion, per-(stage, el) cert table, five invariants (INV-1 split-sum reconciles, INV-2 monotone in yield, INV-3 non-negative, INV-4 element coverage closed, INV-5 T1 fruit-zero-by-input), edge cases, caveats, refinement triggers. `tomato/spec.md` slimmed to the supply-coupling specs (REQ-011/013/014/033) + a Subprojects pointer to plant-needs. 56/56 PASS.
- 10:30 — `nutrition/tomato/spec.md`: added "Demand model" section (since moved into the plant-needs subproject — see 10:35).
- 10:20 — Build pipeline introduced. `app/index.html` is now the authoring source; `scripts/build.mjs` resolves `<!-- @include path -->` markers (line-aware: consumes leading hspace + trailing newline so partial indentation governs) and writes the artifact to root `index.html`. `--watch` mode rebuilds in <20ms via Node 22 native `fs.watch({ recursive:true })` over `app/` + `nutrition/`. `scripts/dev-server.sh` now launches the watcher in the background alongside live-server (end-to-end edit→reload typically <500ms). `package.json` adds `build`, `build:watch`, `dev` scripts. First real split: tomato Nutrition page extracted to `nutrition/tomato/app/page.html` (142 lines, the entire `<div id="nutr-tomato-content">` block) and `nutrition/tomato/app/logic.js` (565 lines: `buildNutriment`, `setNutrStage`, `setNutrRecipeMode`, `syncNutrRecipeModeUI`). Build artifact byte-identical to pre-extraction; 56/56 PASS.
- 09:55 — requirements.md split into nutrition/{spec, tomato/spec, tomato/app/spec, lettuce/spec, lettuce/app/spec, nursery/spec, nursery/app/spec}. 33 REQs migrated (28 cross-crop nutrition, 4 tomato model, 1 tomato Nutrition page UI; lettuce + nursery files scaffold-only). 5 cross-app REQs (REQ-001/005/006/007/008) stay in requirements.md. Verifier scripts updated (check-requirements.sh DOCUMENTED_REQS now scans requirements.md + nutrition/**/spec.md for the union). 56/56 PASS, REQs wired 37/38 unchanged. "Spec" terminology adopted in body content (was "requirement"); filenames requirements.md / check-requirements.sh kept as-is per user.
- 09:50 — nutrition/info/ created; moved 11 farm-info docs; nutrition/info/CLAUDE.md index written

## 2026-05-08

- afternoon — `FP_RECIPE_T5.fertigation` rewritten mass-balance pure (no SME credit): K2SO4 0→5167 g, MgSO4-7H2O 0→1379 g, **Solubore 9 g added** (boric acid non-ionic, single-channel for B; Ecocert validated). Foliaire Solubore stays 0. `calcNutrSupply` FP-mode branch updated: (a) `fertMixFactor = 1.0` instead of 0.5 (no SME pool to coexist with), (b) reads `FP_RECIPE_T5.fertigation['Solubore']` → `supply.fert.B`, (c) `soil[el] = 0` for everything except Ca and P (mass-balance philosophy: replenish offtake regardless of bank; soil is a verification target, not a supply credit). Block 4 rendering shows Solubore line + FP-mode "no mixing factor" note when `nutrRecipeMode === 'fp'`. Pourquoi modals updated for K/Mg/B with mode-aware equations. Stored-mode behavior unchanged. 56/56 PASS.
- afternoon — Salanova nutrition subpage refactored to mass-balance flow (mirrors tomato): 1) Besoins 2) Compost (NEW — Savaria ORGANIMIX residual via `RECIPE_INPUTS.compostReleasePerWeek`) 3) Front-load (moved before fertigation, presented as compost-style gap grid with click-to-modal rows) 4) Fertigation actuelle (now with `renderGapGrid`) 5) Leviers + new "Recette proposée" admin card (`renderProposedRecipeLettuce`). Soil block (Sol — SME × transpiration) removed from supply chain — soil mass-flow draws on the bank, doesn't replenish (parity with tomato 2026-05-07 refactor). Block 1 needs grid: DW tissu column dropped from visible row; cert + DW concentration + demand equation now surface in pourquoi modal on click. New pourquoi keys: `lettuce-demand.*`, `lettuce-compost.*`, `lettuce-frontload.*`, `lettuce-fert.*`. Hypothèses copy rewritten for mass-balance framing. 56/56 PASS.
- 08:10 — `STORED_RECIPE.{crop}.{channel}` consolidation. Fertigation re-locked à valeurs PA Taillon (avril 2026, oligos inclus T1-T5 : FeSO₄ 20g, ZnSO₄ 4g, MnSO₄ 7g, CuSO₄ 1g, Borax 9g, Mo Na 1g — K/Mg ramp avec stade). `TOMATO_SIDEDRESS` + `FOLIAR.tomato` migrés sous `STORED_RECIPE.tomato.{sidedress, foliaire}`. `computeStageRecipe` désormais source FP uniquement (lu par Block 7 drift gauge + nutrRecipeMode='fp'). `captureCurrentSnapshot()` simplifiée à `structuredClone(STORED_RECIPE.tomato)`. Block 7 stored-vs-FP rétabli comme jauge significative (T5 stored K=3489/Mg=1396 vs FP_RECIPE_T5 0/0 — drift visible). `/retire-recipe` skill + CLAUDE.md + check-recipes.mjs (alias verifier-internal pour migration douce) + check-requirements.sh tous mis à jour. Pas d'alias backwards-compat dans index.html. 56/56 PASS.
- 09:30 — `computeStageSidedress(stage)` added (mass-balance, Ca-aware, farine-only). `FIRST_PRINCIPLES_SIDEDRESS[T1..T5]` and `FP_RECIPE_T5.sidedress` now wired from this function at script load (was hand-set 1300 g farine T5 only). New T-by-T values: T1=0, T2=0 (compost covers vegetative N), T3=637g, T4=1064g, T5=1655g per planche. Stored TOMATO_SIDEDRESS unchanged — requires `/retire-recipe` to swap; FP recipe display reflects first-principles target.
- morning — Semis laitue subpage added to Nutrition page (third crop sub-toggle). New constants `LETTUCE_NURSERY_TISSUE_DW` (cert 3 seedling-adjusted: N 5%, K 6%, Ca 2% — others same as mature), `LETTUCE_NURSERY_DM_FRACTION = 0.07` (cert 3, drier than mature 5%), `NURSERY_DEFAULTS` (50-cell, 50 trays/cohort, 0.149 m²/tray, 1.25 L/tray fertigation). New fns `calcLettuceNurseryDemand` (per-plant + per-tray), `buildNutrimentNursery`. CROP_PAGES.nutriment += 'nursery'. Defaults: target 150g, cycle 35d, 50-cell. Per-tray as primary unit. Only context + Block 1 (besoins) populated; substrate / fertigation / leviers as placeholders. Cell-volume cap warning surfaces if target exceeds physical cap (50-cell ~90g; 32-cell ~175g).
- 08:05 — `/retire-recipe` correction: trigger remains `TOMATO_SIDEDRESS` + `FOLIAR.tomato` only, but `fullSnapshot` now captures fertigation again (as live OUTPUT of `computeStageRecipe(stage)` per T1-T5 — the K₂SO₄/MgSO₄ doses actually weighed). Trigger ≠ snapshot scope: plant-need edits don't fire the skill, but when a sidedress/foliar retirement happens, the snapshot reproduces the full applied recipe at that date. `captureCurrentSnapshot()`, `renderFullSnapshot()`, page intro copy, schema doc, SKILL.md updated. CLAUDE.md unchanged (already correct).
- 07:30 — `/retire-recipe` skill scope narrowed to STORED recipes only (`TOMATO_SIDEDRESS`, `FOLIAR.tomato`). Fertigation dropped — no stored constant since TOMATO_STAGES retired this morning, value derived live via `computeStageRecipe(RECIPE_INPUTS)`. `captureCurrentSnapshot()`, `renderFullSnapshot()`, page intro copy, schema doc comment, SKILL.md, CLAUDE.md all updated. Legacy entries unaffected. (Superseded by 08:05.)
- 07:18 — REQ-026/027 removed (predicted-vs-measured CE + substrate CE leach trigger). Both required measurement-logging UI we don't have. Re-add when infrastructure lands. Per "if not auto-enforceable, not a requirement" principle.
- 07:17 — INCOMPATIBLE_RECIPES + MIX_ORDER constants declared in index.html (REQ-030/031, both → PASS). REQ-018 effectiveEff foliar branch fixed: was applying soil-pH penalty (PH_RESPONSE['sulfate-metal'](7.4)=0.10) to MnSO4/ZnSO4/CuSO4 foliar products, dropping eff to 2.7%. Routing rule now: foliar products skip soil-pH curve entirely (cuticle uptake only sees foliarPhResponse(tankPh) × 0.30 coverage), soil/fertigation products keep PH_RESPONSE penalty. Verifier failures 9 → 6.
- 07:08 — REQ-002 wired with FORBIDDEN_PRODUCTS blocklist in scripts/check-recipes.mjs. 26 entries (urea, ammonium/calcium nitrate, mono/di-ammonium phosphate, monopotassium phosphate, KH2PO4, potassium chloride, muriate of potash, Fe-EDTA, Plagron/Canna/general hydroponics, glyphosate/roundup, mancozeb/chlorothalonil, French equivalents). Short acronyms (MAP/DAP/MOP/EDTA/GHE/KCl/CAN-17) intentionally omitted — collide with .map()/predictedTankPh/chelate-edta/KSP_SAFE reason fields/"higher". Was previously manual-review-only. PASS. Tally: 37→39 wired, 42→40 documented (parallel REQ-003/056/057 removal).
- 07:35 — REQ-003, REQ-056, REQ-057 removed. Principle codified: if not auto-enforceable, not a requirement. Manual review items belong in team process (Catherine's #review channel), not requirements.md.
- 07:03 — REQ-062 added: single fertigation tank + single foliar spray per week. Codifies team workflow simplicity. Wired in node verifier.
- 06:53 — TOMATO_STAGES const removed (Path A code-honesty cleanup). Was redundant with computeStageRecipe() — every reader now calls the function directly. REQ-016 (stored vs FP drift) retired since the comparison was meaningless. RECIPE_HISTORY entry added.

## 2026-05-07

- 21:15 — REQ wiring batch in scripts/check-recipes.mjs: tally 17/43 → 38/43. Wired REQ-013/014/015/016/017/018/020/021/024/025/026/027/029/030/031/032/053/054/055/060/061. Added exposure for RECIPE_INPUTS, PRODUCT_PCT, SIDEDRESS_AREA_PER_PLANCHE, SIDEDRESS_MIN_EFF, TOMATO_NUM_BEDS, TOMATO_BED_AREA, computeStageRecipe, predictedTankPh, foliarPhResponse, effectiveEff, INCOMPATIBLE_RECIPES, MIX_ORDER. Skip list (5): REQ-002/003/028/056/057 (manual-review by design). Genuine model-gap failures surfaced (NOT patched): REQ-013 P under-supply T4/T5 (P decorative at pH 7.4), REQ-014 N/Mg over-supply T1-T3 (low demand), REQ-018 MnSO4/ZnSO4/CuSO4 below 5% threshold under sulfate-metal pH curve, REQ-024 T1/T2 fertigation CE below 0.3 floor (zero-dose stages), REQ-025 foliar CE 9.48 > burn cap 8.0, REQ-026/027/030/031 stub-fail (UI/constants not implemented). Per task spec: did not change model to make checks pass.
- 21:00 — Historique nutriments refactorisé en table date + résumé, expand → snapshot complet (fertigation/sidedress/foliaire). Schema RECIPE_HISTORY étendu (`summary`, `fullSnapshot`). Helper `captureCurrentSnapshot()` ajouté. Entrées legacy gardées partielles (rendu "Snapshot partiel"). 34/34 PASS.
- 20:27 — T5 labels open-ended (Option C). "sem 19-26 · 8 sem" → "sem 19+ jusqu'à crop-out". Cycle totals shown as ranges (114-127 kg Actisol etc.) reflecting real spread of crop-out timing. Avoids the under-counting that the previous fixed labels created post-T6 removal.
- 20:25 — REQ-058 removed. Mass-balance refactor decoupled fertigation from transpiration; req no longer the right vehicle to enforce single-source for the formula. Irrigation + Banque sol pages still use the transpiration calc — flag if cross-page consistency becomes an issue.
- 20:18 — T6 stage removed. Mass-balance superseded OAQ §4.3.2 end-of-season heuristic — plants still remove K at end-of-cycle, arbitrary 50% cut had no first-principles basis. T5 extended to weeks 19-28. Snapshot logged in RECIPE_HISTORY.
- 20:00 — Historique nutriments admin page added. RECIPE_HISTORY constant captures retired recipe snapshots (TOMATO_STAGES hand-set, K/Mg multipliers, FOLIAR Spray B, Cu 4→2g). Audit trail for organic cert. Page slug `historique-nutriments`.
- evening — Nutrition recipe-source toggle now URL-persisted as a third path segment (`#nutriment/<crop>/<recipe>`, recipe ∈ {fp,stored}, sanitized + omitted when default). Default flipped `'stored'` → `'fp'`. parseHash/syncHash/toggleAdmin/hashchange + init block extended; `setNutrRecipeMode` calls syncHash, `setNutrStage` syncs hash on auto-revert. Loading `recipe=fp` forces `nutrStage='T5'`. 34/34 PASS.
- evening — Nutrition tomato page (Block 1-6): recipe-source toggle added in Cible & contexte (Stockée / Premiers principes). `calcNutrSupply(stage, phLocked, transpFactor, targetYield, recipeMode)` now reads either stored recipes or `FP_RECIPE_T5` (T5-only). Toggling to FP snaps `nutrStage` to T5; switching off T5 reverts mode to stored. New state `nutrRecipeMode`, new fns `setNutrRecipeMode`, `syncNutrRecipeModeUI`. `buildBanqueSol` call site unchanged (uses `supply.soil` only — recipe-independent).
- 08:55 — Nutrition tomato page reordered to mass-balance flow: 1) Besoin du plant 2) Compost (NEW — reads RECIPE_INPUTS.compostReleasePerWeek) 3) Engrais sol granulaire (sidedress now its own block, reads TOMATO_SIDEDRESS) 4) Fertigation 5) Foliaire 6) Leviers 7) Stockée vs calculée. Soil-passive (pantry) block REMOVED — clean break from gap-fill model. Gap chain now offtake → compost → sidedress → fert → foliar; deriveLever rewritten to drop soil-cap branch. Hypothèses copy rewritten for mass-balance framing. 34/34 PASS.
- late evening — Salanova subpage defaults retargeted: transplant 50→30 g, harvest cible 200→100 g (cycle 14 j, density 43/m² unchanged). New realistic next-cycle scenario per user; inputs remain editable for what-if.
- late evening — Mass-balance Phase 1b: compost K trimmed 0.42 → 0.40 g/m²/wk (mineralization 0.70 → 0.65), compost Mg trimmed 1.47 → 0.50 g/m²/wk (Mg % not on Savaria label, conservative 0.30% assumed and rounded down). TOMATO_STAGES auto-rederives — new values: T1 K=1647 Mg=0 · T2 K=2310 Mg=0 · T3 K=4465 Mg=0 · T4 K=3548 Mg=728 · T5 K=4953 Mg=1378 · T6 K=1181 Mg=0. TODO comment added above FOLIAR declaration for Phase 2 (foliar mass-balance refactor — label-string parsing required). 34/34 PASS.
- evening — Salanova subpage added to Nutrition page (admin). New crop sub-toggle (Tomate/Salanova) with `nutrCrop` page-local state routed via CROP_PAGES. Salanova model: continuous post-transplant (no stages) — demand from mass-gain × DM% × tissue conc. New constants: `SME_LETTUCE_PPM`, `LETTUCE_TISSUE_DW`, `LETTUCE_DM_FRACTION`, `LETTUCE_FRONTLOAD_DEFAULTS`. New functions: `calcLettuceNutrDemand`, `calcLettuceNutrSupply`, `setNutrCrop`, `buildNutrimentLettuce`, `lettuceLeverFor`. Inputs: transplant g, target g, cycle days, density, feather-meal front-load g/m², pH lockout. Renders 5 cards (besoins, sol, fertigation, front-load, leviers).
- 08:12 — Hook simplified (changelog-only, no git log). CLAUDE.md updated: Claude auto-appends to changelog after material changes; refresh-memory directive for older entries.
- 17:00 — Block 6 rebased: stored vs FP recipe (was stored vs naive computeRecipe). FP_RECIPE_T5 single source.
- 16:30 — REQ-060 added: narrative copy must not contradict current data (auto-derive or // stable —).
- 16:00 — Block 5 levers replaced with auto-derived `deriveLever(el)`. Stripped 8 stale advice strings; added 20 `// stable —` annotations.
- 15:00 — Banque sol page now shows stored vs FP trajectories side-by-side (5 columns).
- 14:00 — Recette proposée block updated to first-principles recipe (no anchoring on legacy).

## 2026-05-06

- evening — REQ-016 three-tier (green ≤5%, yellow 5-20%, red >20%). REQ-058 transpiration model declared explicitly.
- afternoon — Phase 3 cert UI added then removed (cert badges per number; user preferred no badges in admin UI).
- afternoon — Phase 2 node verifier added: scripts/check-recipes.mjs + package.json (jsdom). REQ-001/006/007/008 migrated bash → node.
- morning — Cu cut 4 g → 2 g in FOLIAR.tomato.A (Cu toxicity observed in image diagnostic, no surfactant).
- morning — Spray B (CaCl₂) removed from FOLIAR.tomato (Ecocert Teris industrial-grade listing not verified — audit Finding 9).

## 2026-05-05

- evening — Bilan → Nutrition rename (page slug `nutriment` unchanged for routing).
- afternoon — Cible sol + Cadre admin pages removed; Banque sol page added with bank-trajectory view.
- afternoon — LUXURY_FACTOR demand cap added (K=1.15, Ca/Mg/N=1.10, micros=1.0). Caps soil supply at min(mass-flow, demand × LUXURY_FACTOR).
- afternoon — Sidedress reframed as bank maintenance: dropped from supply sum, only feeds Banque view. MIXING_FACTOR_FERT=0.5 added (audit Finding 14).
- morning — Iron DL + yucca dropped permanently (until further notice). FeSO₄ 80 g/15 L is canonical foliar Fe.
- morning — Phase 1 model wired: PRODUCT (15), CHANNEL_ROLE, PH_RESPONSE (10 curves), KSP_PAIRS, etc. Functions: effectiveEff, predictedCE, predictedTankPh, computeRecipe.
- morning — Tissue test ordered (petiole NO₃-N + Mg + Cu/Mn/Zn panel; results in 5-7 days).
