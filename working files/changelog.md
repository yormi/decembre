# Décembre — Changelog

Append-only log of substantive changes (recipe edits, REQ additions, model architecture, page restructuring). Read at start of any session touching app state, especially under parallel Claude sessions.

Format: `YYYY-MM-DD HH:MM — brief description`. One line per change. Most recent at top. Keep ~10 entries; older history lives in `git log`.

## 2026-05-24

- **2026-05-24 — Process-rules refactor: `everyone/` (universal auto-load) + `lib/` (opt-in) split.** Root `CLAUDE.md` shrunk to 17 lines (voice + greenhouse infra + pointers). Five sections relocated: doc-dir convention → `team-coordination/everyone/doc-dirs.md`; changelog protocol → `everyone/changelog-protocol.md`; Guillaume's lane → `everyone/guillaume-lane.md`; spec discipline → `lib/spec-discipline.md`; learnings discipline → `lib/learnings-discipline.md`. New `lib/code-shape.md` (Elm-influenced JS, lifted from inline coder persona) + `lib/req-allocation.md` (lifted from inline PO + specialist personas, was duplicated). Derivation faithful-blueprint stays in specialist persona (sole author). `_shared/` → `everyone/` rename via `git mv` (7 persona load lines + `team-coordination/CLAUDE.md` § Procedures/Principles auto-rewritten). Each persona's entry block now lists `everyone/` (universal) + the specific `lib/*.md` files it needs: PO/specialist → spec + learnings + req-allocation; coder/test-writer → code-shape; pruner/model-challenger → spec-discipline; context-coherence → spec + learnings; team-leader → none. Storage-layers section in `team-coordination/CLAUDE.md` extended from 4 to 6 layers (added `everyone/` + `lib/`). Drift-risk fix: single-source-of-truth per process rule, no per-persona duplication, no universal-load bloat for subset-audience rules. `bash scripts/check-requirements.sh` 161/0; `npm test` 22 pass / 0 fail. No code change, no spec change, no STORED edit; process-doc reorg only.
- **2026-05-24 — Convention re-pivot: `derivation.md` MANDATORY for model layer, code = one valid implementation derived via TDD.** Earlier-today "derivation optional / code = source of truth" reversed. New model: spec.md + derivation.md are the semantic source of truth; code is one valid implementation that test-writer + coder + pruner derive from spec + derivation via TDD. Specialist writes `model/{spec,derivation}.md` and does NOT touch code. Project `CLAUDE.md` Specs section rewritten: "Does NOT belong in a spec" routes math → `derivation.md` (was: → code); new `## Derivation` section with faithful-blueprint contract (every formula, every coefficient+cert+source, every algorithm as numbered steps, every input/output shape, at least one worked example per non-trivial branch) + lane discipline (specialist owns .md, test-writer + coder + pruner own the code-derivation wave) + drift-detection mechanism. Specialist persona (`.claude/agents/plant-nutrition-specialist.md`): scope updated to `{spec,derivation}.md` pair owner, out-of-scope explicitly bars all code files including `.test.mjs`. Test-writer + coder personas updated to read derivation.md as a primary input (was: spec.md only); test-writer's contract extended to cover derivation worked examples + coefficients + algorithm steps, not just spec headers. Team-leader `run-wave.md` subagent prompt templates updated to brief test-writer + coder on derivation.md as mandatory input. Memory entry `feedback_no_trace_comments.md` updated with the re-affirmation. Lazy migration of 11 existing derivation.md files: each upgrades to faithful-blueprint bar when next touched (most current files lean narrative; will need expansion to schema + worked examples). No code change, no spec change, no STORED edit; convention re-pivot only.
- **2026-05-24 — Vestigial "Spec status" note in foliar-strategy derivation amended in place.** `nutrition/tomato/foliar-strategy/model/derivation.md` § "Ca-specific cuticle coverage" closing paragraph rewritten: previously claimed REQ-101 anchored a global `FOLIAR_COVERAGE_DEFAULT` with per-element axis "pending" — stale since the REQ-101 rework already landed per-recipe coverage (oligo 0.30 / 0.80, Ca 0.15 / 0.40). Replaced with accurate status: spec covers both recipes; remaining gate is the Ca-recipe `data.js` entry (coder lane) before `computeFoliarSupply` returns non-zero Ca. No spec change, no code change. `bash scripts/check-requirements.sh` 161/0; `npm test` 22/22.
- **2026-05-24 — Foliar-strategy rework — full subproject rename + PO specs + 3 learnings landed.** Single-PR completion of the 2026-05-24 grill. (1) `git mv nutrition/tomato/foliar-recipe → foliar-strategy` + tree-wide path sweep (~35 files: requirements.md, app/index.html, scripts/check-recipes.mjs, model/{contribution,recipe,data}.js path comments, persona files, mailbox archives, doc cross-refs). (2) New PO specs: `foliar-strategy/spec.md` (subproject-level, 3 slugs: strategy-contains-recipes, frequency-is-model-output, sprays-spread-across-farm-working-days), `procedure/spec.md` (4 slugs: weekly calendar derivation, even spread, one-recipe-per-spray, no operator day override), `operator/spec.md` (3 slugs: recipe-sheet-per-recipe, weekly-calendar-rendered, no-frequency-input). (3) `builder/spec.md` rewritten: REQ-113 (sprayCount input) RETIRED; REQ-114 trimmed to surfactant-only re-render (now slug `block-5-reactive-to-surfactant`); REQ-163 held verbatim (now slug `efficacite-reactive-to-surfactant`); new slug `surfactant-input` declares the lone Block 5 input. (4) `nutrition/spec.md` — new slug `farm-working-days` (Mon-Fri at Décembre, cross-crop); REQ-062 foliar paragraph fixed (stale sprayCount/REQ-112 reference replaced with frequency-is-model-output cross-ref). (5) Three learnings under new convention: `foliar-strategy/learnings/0001-strategy-recipe-hierarchy.md` (why the rename + multi-recipe shape), `0002-frequency-as-model-output.md` (frequency-as-output + day-spread decision), `0003-close-gap-bounded-by-leaf-tolerance-cap.md` (optimization criterion). (6) `nutrition/tomato/spec.md` foliar-strategy subproject summary refreshed for the new shape. Specialist's model/spec.md reshape (separate changelog entry below) landed in parallel via spawned agent. `bash scripts/check-requirements.sh` 161/0; `npm test` 22/22. Coder follow-up flagged: JS symbol rename `FoliarRecipeTomato` → `FoliarStrategyTomato` (separate PR). STORED key `foliaire` left intact (Guillaume's lane). No STORED edit.
- **2026-05-24 — Foliar-strategy model/spec.md reshaped for multi-recipe contract.** Per the 2026-05-24 PO grill locks: strategy = list of recipes (oligo today, Ca next PR), each REQ-029-clean independently; frequency is model OUTPUT not operator input; per-recipe `weeklyLeafToleranceCap` bounds the model-computed `sprayCount`; day assignment spreads evenly across `nutrition — farm-working-days` (PO writing that entry in parallel). Four new entries: `REQ-195` multi-recipe strategy structure; `REQ-196` per-recipe weekly leaf-tolerance cap (oligo=1, Ca=3, cert 3); `REQ-197` `sprayCount = min(sprays-to-close-gap, leaf-tolerance-cap)` per recipe; `REQ-198` day-spread rule (1→Wed, 2→Mon+Thu, 3→Mon+Wed+Fri). `REQ-115` rewritten — now returns `{doses, sprayCount}` bundle and iterates spray count internally; opts changed `sprayCount` → `recipeKind`. `REQ-116` reshaped to call `computeFoliarStrategy(stage, gap)` aggregator. `REQ-112` retained as legacy-override transitional surface (test churn deferred to coder migration). `REQ-101` extended to per-recipe coverage axis (Ca constants from derivation.md). New `computeFoliarStrategy(stage, gap)` surface added to REQ-103 namespace. Flags for follow-up: (a) coder symbol rename `FoliarRecipeTomato` → `FoliarStrategyTomato` separate task; (b) Ca recipe `data.js` entry + `computeFoliarSupply` Ca slot wiring blocks on PO data definition; (c) `nutrition — farm-working-days` cross-ref will resolve when PO lands it. `bash scripts/check-requirements.sh` 161/0; `npm test` 22/22.

- **2026-05-24 — First CONTEXT.md landed: foliar strategy vocabulary.** `nutrition/tomato/foliar-strategy/CONTEXT.md` written per grill-me skill format. Locks five terms from the 2026-05-24 PO grill: **foliar strategy** (one per crop, the whole feeding plan), **foliar recipe** (one tank's mix, many per strategy, REQ-029-clean), **spray** (one execution of a recipe on a specific day — replaces my prior "application" coinage), **weekly leaf-tolerance cap** (per-recipe cuticle-damage upper limit on sprays/week — replaces "cuticle cap"), **farm working days** (Mon-Fri at Décembre). Example dialogue + Avoid aliases per format. Scope: per-subproject (Guillaume's call); file lives at current path because rename to `foliar-strategy/` is pending coordination with in-flight model session (foliar-strategy/model/{derivation,spec}.md uncommitted from parallel work). No CONTEXT-MAP.md yet — first context only, lazy escalation. No code change, no spec change, no STORED edit.
- **2026-05-24 — Convention shift: `derivation.md` retired as required artifact + Elm-style JS adopted for calc/model layers.** Project `CLAUDE.md` Specs section updated: code (`calc.js`, `model.js`, `data.js`) is now the source of truth for current math; `derivation.md` becomes **optional**, kept only where code-only reading is genuinely hostile (dense integrators / multi-step coupling). Why-this-number + why-this-decision both flow to `learnings/NNNN-slug.md`. Lazy migration — existing derivation.md files stay until next touched; when next touched, formulas move to code (or code comments at the call site), why-this-number to a learning, file deleted. Maintainer keeps exceptions by judgment. New **Code style — Elm-influenced JS** section added: tagged-object discriminated unions (`{kind: 'foliar', ...}`), exhaustive switches with unreachable-throw default, Result/Maybe shapes for partial functions, immutable inputs, total functions over silent defaults, single-shape returns. Captures most of Elm's safety wins without toolchain. Lazy migration — existing JS stays until next touched, refactor on edit. Decision flagged as Elm-pilot deferred: full Elm reserved for fertilization-interaction model subproject if/when it lands (greenfield, pure, type-heavy). No code change, no spec change, no STORED edit; convention shift only.
- **2026-05-24 — Doc convention: flat `learnings.md` → numbered `learnings/NNNN-slug.md` per subproject.** Project-root `CLAUDE.md` Specs section updated: derivation.md keeps its current role (live math, evolves with spec), learnings split out to numbered append-only files dated per entry. Three-criteria filter (hard to reverse + surprising + real trade-off) keeps file count low. Voice stays research-notebook, not corporate ADR. Cross-cutting decisions land at the highest subproject scope they apply to (foliar-only → under foliar-strategy/, cross-crop → nutrition/learnings/, project-wide → root learnings/). Memory entry `feedback_no_trace_comments.md` also updated. **Migration: lazy** — existing 11 flat `learnings.md` files stay until next touched. First instance under new shape will land alongside the foliar-strategy spec rework starting next turn. No code change, no spec change, no STORED edit; convention shift only. `nutrition/tomato/foliar-strategy/model/derivation.md` updated to reflect Test 1 Path B operational decision: (a) "No-macro by design / Ca" entry rewritten — 2026-05-06 Spray B retirement framing superseded by 2026-05-24 reactivation (CaCl₂·2H₂O vendor + product Ecocert-verified), captures dose (100 g/15 L, 2×/wk), delivered Ca (~56 mg/m²/wk = ~18 % of 315 mg/m²/wk canopy gap), and the obstacles-chain math anchor; (b) new "Ca-specific cuticle coverage" subsection — 0.15 no-surfactant / 0.40 with-surfactant (cert 2, ratio 0.50 × sulfate-metal coverage at each surfactant state, Schönherr 2000 anchor + Test 1 tissue refit trigger at week-4); (c) `BURN_CAP_BASE_G.Ca = 100 g/15 L` row added (cert 3, anchored to REQ-025 9 mS/cm under 10 mS/cm cap × 10 % safety margin via CaCl₂·2H₂O solution-CE table). `computeFoliarSupply` still returns `Ca: 0` — schema wiring + per-element coverage axis spec change gated on PO coordination next turn. `bash scripts/check-requirements.sh` 161/0; `npm test` 22/22. No spec change, no code change, no STORED edit this turn.
- **2026-05-24 — Persona infra: added `procedures/` layer + on-demand load.** All 6 personas refactored (PO, plant-nutrition-specialist, model-challenger, team-leader, spec-pruner, context-coherence) so they no longer auto-read mailboxes / inbox files / drafts / full spec tree / MEMORY on entry — that load fills context before any real work. Persona files now read only: own file + CLAUDE.md + team-coordination/CLAUDE.md + own principles.md + _shared/principles.md + recent changelog. `requirements.md` also moved to lazy — loaded inside the procedure that needs it (PO at REQ-draft time; specialist at subproject-work time; pruner via `prune-subproject.md`; coherence via `audit-walk.md`; team-leader via `run-wave.md`). Procedural how-to extracted to 8 new files under `team-coordination/<persona>/procedures/`: PO `notify-team-leader.md`; specialist `respond-to-challenger.md` + `triage-fan-out.md` + `notify-team-leader.md`; challenger `mode-b-review-queue.md`; team-leader `process-mailbox.md` + `run-wave.md`; spec-pruner `prune-subproject.md`; context-coherence `audit-walk.md`. **Behavior change for team-leader:** auto-listener removed — mailbox is on-demand only now, won't kick off when idle. `team-coordination/CLAUDE.md` updated: skills/principles/persona → 4-layer model adding **procedures** (persona-local, trigger-loaded, not slash-invocable; distinct from cross-persona skills in `.claude/skills/`). Persona line counts: team-leader 230→71, plant-nutrition-specialist 253→120, spec-pruner 158→60, context-coherence 147→75. coder + test-writer skipped (already lazy via spawn-prompt). No code change, no spec change, no STORED edit.
- **2026-05-24 — Ca foliar Test 1 re-scoped: BER → canopy + disease.** Operator update: BER mostly gone. Goal shifts to (a) raise canopy tissue Ca toward modeled demand (close 315 mg/m²/wk canopy gap), (b) reduce fungal disease susceptibility — specifically stem canker at `nutrition/tomato/doc/stem-canker-2026-05-24/` (Ca-pectate cell-wall mechanism, cert 4 Conway/Sams/Volpin & Elad). Spray-target expanded foliage + fruit + **stems** indiscriminate; starting frequency bumped Path A (1×/wk) → **Path B (2×/wk, 100 g CaCl₂·2H₂O + surfactant per 15 L tank, 56 mg Ca/m²/wk delivered, 18 % of canopy gap)**. Path C (3×/wk) ramp option. Endpoints retargeted: tissue Ca trajectory (goal 1) + new-canker incidence vs 2026-05-24 baseline (goal 2); existing lesions expected NOT to reverse under Ca alone. New disease-management appendix: sanitation + humidity / ventilation are the **first** two levers; foliar Ca is the third; PA Taillon visual differential + biofungicide as 4 and 5. Spray itself is the cheapest disease-prevention consumable; operator load is sanitation + humidity time. No code change, no spec change, no STORED edit.
