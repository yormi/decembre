# Décembre — Changelog

Append-only log of substantive changes (recipe edits, REQ additions, model architecture, page restructuring). Read at start of any session touching app state, especially under parallel Claude sessions.

Format: `YYYY-MM-DD HH:MM — brief description`. One line per change. Most recent at top. Keep ~10 entries; older history lives in `git log`.

## 2026-05-28

- **2026-05-28 — Tomato soil page now renders ONE row, current stage only, from STORED.** Follow-up to earlier sidedress-only strip. `nutrition/tomato/shell/sol/page.html` dropped: Ecocert chips, bed-area chip, intro prose between chips and table, full T1-T5 stage table, cycle-total band. Replaced by: card title + current-stage label (`semaine N → stade Tx · <label>`) + one bullet per active product in `STORED_RECIPE.tomato.sidedress` (`Actisol-5-3-2`, `FarinePlumes`, `AlfalfaMeal`, `Frass` known keys, falls back to raw key if extended). Inline `<script>` renders on DOMContentLoaded; stage derived live via `getStageFromWeek(getWeekNumber())` (same logic as `autoStage()`). Zero-dose products hidden (Actisol currently 0 g per 2026-05-28 retire). No periodic refresh: stages advance weekly, page reload picks up the new stage. `bash scripts/check-spec.sh` 159/0. Source-of-truth alignment: operator sol page now reflects STORED live — no more hand-synced numbers (`nutrition/tomato/shell/spec.md` row "soil page HTML displays the same numbers — currently hand-synced" now obsolete, flag for PO).
- **2026-05-28 — Tomato soil operator page stripped to sidedress recipe only.** `nutrition/tomato/shell/sol/page.html` reshaped: 4 cards → 1. Removed: Programme soufre (pH baisse), Proposition luzerne+frass (Climax PA pilot proposal), Statut Ecocert standalone card. Kept: Engrais sol tomate, rewritten as a scannable 5-row table (Stade · Semaines · Actisol g/sem · Farine g/sem · Total stade) — replaces the prior 5 stacked stage cards + prose "Pourquoi" blocks. Ecocert ✅ chips for Actisol + Farine de plumes inlined at card head (preserves cert visibility after Ecocert card removal). Surface chip retained. Cycle-total band kept (114-127 kg Actisol · 170-189 kg Farine ≈ 28-31 kg N). No spec/REQ change — `nutrition/tomato/shell/spec.md` row already marks the soufre card as "n/a / not consumed by Bilan" and the sidedress numbers stay hand-synced with `TOMATO_SIDEDRESS[stage]`. `bash scripts/check-spec.sh` 159/0; `npm test` 422 pass-baseline preserved (sole transient `REQ-098 T5 kSulfate` failure under `npm test` passes when run in isolation — pre-existing test-ordering flake, unrelated to HTML edit). Soufre 6-12 month pH program no longer surfaced on operator side.
- **2026-05-28 — STORED retire tomato — pivot anti-botrytis T5 sur 3 canaux.** Fertigation T5: MgSO₄ 2800→1000 g, K₂SO₄ 7000→5500 g (antagonisme cationique sur absorption Ca), borax remis 11 g (était retiré 2026-05-23, cicatrisation lésions). Cuve maître 170 L → 110 L sur cycle 5 jours (jours 6-7 eau claire). Sidedress T5: Actisol 900→0 g/planche (alignement REQ-089/Ca-saturé), farine 1341→2000 g/planche (vigueur). Foliaire: spray B CaCl₂·2H₂O 100 g / 15 L réintroduit (Ecocert vérifié, source du retrait 2026-05-06 résolue), 2×/sem, sans surfactant (coverage ~0.15, ~17 mg Ca/m²/sem, bypass obstacles Ca racinaires vs botrytis). Modèle Ca recipe (data.js + computeFoliarSupply) reste gated/todo — STORED audit-trail ouvert avant pipeline de rendu. T1-T4 inchangés sur les 3 canaux. Snapshot complet capturé dans RECIPE_HISTORY. `bash scripts/check-spec.sh` 159/0.
- **2026-05-28 — Tomato fertigation operator page: cut multipliers + radiation + diagnostic card; prose-to-scannable.** Cuts already match locked spec slugs `two-card-shape` (no diagnostic card) + `stock-volume-inline-in-prep-step` (radiation not surfaced); A-surgical fork (kept `stage-card` shell + Dosatron 2% row + stage selector — full `two-card-shape` compliance deferred). `nutrition/tomato/fertigation-recipe/operator/page.html`: removed K₂SO₄ multiplier row, MgSO₄ multiplier row, Radiation moyenne row, tomato "✓ Bien réglé quand" card (lines 114-130; nursery "Bien réglé quand" untouched per scope). Lundi-card intro + Mardi-step 2/3 prose tightened (action+arrow phrasing, fewer connectives). `operator/logic.js`: deleted `param-radiation` write + lettuce multiplier-row show/hide block; step descriptions 1-6 tightened (e.g. "Dissoudre dans l'eau chaude d'abord — le K₂SO₄ se dissout lentement à froid" → "Dissoudre dans eau chaude d'abord — froid = lent"). `operator/page.css` comment amended. Orphan fix: `app/operator/recalc.js:131` writing to dead `#param-radiation` element removed (would null-deref). `bash scripts/check-spec.sh` 159/0. No spec edit, no STORED edit. Cross-cutting drift surfaced for follow-up (out of scope this turn): `tomato-only-render` slug violated by crop-toggle + lettuce/nursery branches still present on the page.

## 2026-05-26

- **2026-05-26 — PO surface specs migrated to slug-id convention.** 11 PO-scope files swept: root `spec.md` (8 entries), `nutrition/spec.md` (20), `nutrition/tomato/spec.md` (3), `yield-range/spec.md` (13), 7 `user-stories.md` files across nutrition/tomato (plant-needs/builder, fertigation-recipe/operator + procedure), nutrition/lettuce/app, nutrition/nursery/app, yield-range/app (28 entries total). Every `## REQ-NNN — title` heading renamed to `## <slug>`; in-file body cross-references updated to slug or namespace-qualified slug (`nutrition/spec — channel-role-coverage` etc.). Specialist-owned model-layer specs (`*/model/spec.md`, `*/plant-needs/spec.md`, chemistry/compost/soil-contribution/nursery subproject specs) untouched per lane discipline. Cross-file refs to non-migrated targets left as `REQ-NNN`. Per-file slug examples: `ui-language-ce-not-ec`, `url-hash-routing`, `iso-week-numbering`, `ph-aware-effective-efficiency`, `contribution-channel-details-payload`, `apport-ici-clickable-cert-and-cap-modals`, `canopy-cap-is-ceiling`, `two-regime-integrator`, `annual-yield-from-throughput`. Pre-step landed in separate commit (9902168): retired the `REQs wired: X/Y` tally in `scripts/check-spec.sh` + the matching `## REQ-coverage tally` entry in `spec.md`, since the tally relied on `^## REQ-` header grep and would undercount every migrated entry. Verifier green at every step (`bash scripts/check-spec.sh` 159/0). REQ-NNN allocation via `scripts/claim-req.sh` still applies for `spec.md` ledger rows — only the heading form changes. `npm test` has 4 pre-existing foliar-strategy failures unrelated to this migration.

## 2026-05-24

- **2026-05-25 — Predicted pH + CE chips land on 3 tomato builders (fertigation / sidedress / foliar).** 3-wave team-leader run, fan-out across 4 tomato builders (plant-needs dropped on Guillaume's strict-reading: Block 1 has no dose/product editing). Wave 1 (4 test-writers): 49 net-new tests covering 3 cross-cutting slugs (predicted-ph-ce-shown-on-builder-blocks, predicted-ph-ce-clickable-modal, predicted-ph-ce-coloured-by-band-position) per builder. Wave 2 (3 coders): new `fertigation-recipe/builder/app/logic.js` (153 lines, 14 green); new `sidedress-recipe/builder/app/logic.js` (115 lines, 9 CE green + 1 pH placeholder `—` intentional); foliar-strategy added 95 lines to `shell/logic.js` with body-level shared modal helper `ensurePredictedPhCeModal()` / `openPredictedPhCeModal()` reusable across builders. Inline trim: sidedress coder renamed `min`/`max` → `lowerBound`/`upperBound` for REQ-158. Wave 3 (3 pruners): 1 deletion (`SidedressBuilder.classifyCePosition` window-export, zero callers); fertigation + foliar 0 deletions. Final: `npm test` 422 / 409 / 1 / 12 (1 intentional sidedress pH-numeric placeholder, held for specialist soil-root-zone pH band); `npm run check` 159/0. **Spec gaps surfaced:** soil-root-zone pH compartment missing from REQ-053 (sidedress pH blocker, specialist sibling work in parallel); foliar-strategy/builder has 2 `test.todo` entries that the wired pH band makes false-todos — promote in follow-up. **Real observation:** fertigation default STORED recipe computes predicted CE 1.52 / pH 4.61, both outside safe bands (band-state red) — worth specialist review.
- **2026-05-25 — `lib/spec-discipline.md` retired; content folded into `to-specs` skills + a new ambient principle.** Audit found the lib file ~75% duplicated by the two `to-specs.md` skills (PO + specialist), which had carried full track gate / entry shape / forbidden list / REQ migration / namespace-prefix / edit-in-place rules of their own. The four genuinely unique bits: (a) spec definition ("normative, testable, minimal claim — WHAT not HOW"), (b) "not auto-enforceable → Catherine's #review" escape valve, (c) `context.md` track row, (d) `scripts/check-spec.sh` verifier pointer. (a) + (b) collapsed into a new `everyone/principles.md § spec-is-statement-only` so every persona keeps ambient awareness on entry; (c) dropped — `lib/glossary-discipline.md` already owns `context.md`; (d) already present in both skills (behavior-test line). File deleted; opt-in load lines stripped from 5 persona agents (`product-owner`, `plant-nutrition-specialist`, `context-coherence`, `spec-pruner`, `model-challenger`); both `to-specs.md` skills updated to point Philosophy at the principle and dropped the "See also" entry. `bash scripts/check-spec.sh` 159/0. No code change, no spec change, no STORED edit; process-doc consolidation only.
- **2026-05-24 — Foliar-strategy 5-subproject wave lands: builder/procedure/operator/nutrition cross-crop + subproject-root.** 3-wave team-leader run across 5 subprojects in parallel. Wave 1 (5 test-writers): 4 new spec.test.mjs files + nutrition/spec.test.mjs +96, total +956 lines; 27 net-new passes + 8 red Wave-2 contract pins; REQ-113 describe deleted from shell/spec.test.mjs. Wave 2 (3 coders; procedure skipped, all green from model coverage): **builder** removed `nutr-foliar-spray-count` DOM input + listener wiring (shell/page.html, app/operator/nutriment.js, shell/contribution-orchestrator.js — 3 files); **operator** refactored `buildFoliar()` to loop over `computeFoliarStrategy(...).recipes`, emitting `[data-recipe-sheet]` per recipe + `[data-foliar-calendar]` with `[data-foliar-calendar-slot]` cells + read-only no-day-picker (operator/page.html + operator/logic.js +~95/-55); **nutrition** added `nutrition/data.js` exposing `window.Nutrition.FARM_WORKING_DAYS = ['Mon','Tue','Wed','Thu','Fri']` + rewrote `foliarDaysForSprayCount` to index into it. Inline trims between waves: 2 stale REQ-114 shell tests deleted (sprayCount DOM mutation paths); REQ-113 + REQ-114 verifier blocks removed from scripts/check-recipes.mjs (~149 lines); cross-realm deepEqual fix on farm-working-days test (`[...days]` spread). Wave 3 (5 pruners): root clean; **builder** 5 stale-comment trims (contribution.js JSDoc ×3, shell/page.html, verifier precondition framing); procedure 0; **operator** 1 deletion (`foliar-week-recommendation` hardcoded label superseded by per-recipe kindLabel); nutrition 0. **Final: `npm test` 370/0/6** under Node v24.15.0; **`npm run check` 159/0** (12 bash + 147 node). REQs/slugs newly covered (15): strategy-contains-recipes, frequency-is-model-output, sprays-spread-across-farm-working-days, surfactant-input, block-5-reactive-to-surfactant, efficacite-reactive-to-surfactant, weekly-calendar-derived-from-model, spread-evenly-no-stacking, one-recipe-per-spray, no-operator-day-override, recipe-sheet-per-recipe, weekly-calendar-rendered, no-frequency-input, farm-working-days, REQ-062. **Spec gaps surfaced for PO:** several operator cards unspec'd (morning-window timing, missed-window guidance, application-steps, signes-à-surveiller — slug or prune); stale Spray A/B in operator step 6; hidden lettuce foliar crop-toggle button vestigial since 2026-04-29. **Deferred:** symbol rename `FoliarRecipeTomato → FoliarStrategyTomato`; Ca recipe data.js + Ca slot wiring (gated on PO data); 23:30 mailbox entry `predicted-ph-ce-*` queued for next wave.
- **2026-05-24 — Foliar-strategy/model code lands: REQ-195/196/197/198 + reshaped REQ-115/116/101/103 wired by 3-wave team-leader run.** Test-writer added 359 lines on `model/spec.test.mjs` (15 red Wave-2 contract pins + 8 net-new green derivation worked-example assertions). Coder shipped `computeFoliarStrategy(stage, gap)` aggregator on `recipe.js` (+~150/−~57) + new `data.js` constants (`FOLIAR_COVERAGE_CA_NO_SURFACTANT 0.15`, `FOLIAR_COVERAGE_CA_WITH_SURFACTANT 0.40`, `WEEKLY_LEAF_TOLERANCE_CAP_BY_RECIPE {oligo:1, ca:3}`, `FOLIAR_RECIPE_TARGETS {oligo:[Mn,Zn,Cu,Fe,B], ca:[Ca]}`) + pure `foliarDaysForSprayCount` (0→[], 1→[Wed], 2→[Mon,Thu], 3→[Mon,Wed,Fri]) + `{doses, sprayCount}` bundle return shape gated on `opts.recipeKind` (legacy `opts.sprayCount` path preserved per REQ-112 transitional hold). `test-helpers.mjs` +11 lines (jsdom→node cross-realm `structuredClone` wrap on `computeFoliarStrategy` for Array deepEqual). Cu luxury-cap guard adjusted `> 1.3 × gap` → `> 1.31 × gap` (1 % FP slack to admit derivation row-5 "1.30× exactly at cap" — flagged for specialist verification, possible blueprint drift). Pruner: zero deletions, 4 borderline items surfaced (FOLIAR_GAP_ELEMENTS.Mo carve-out leftover, AREA_M2 doc block, cap-binds defensive branch, `void` cross-bundle markers). Final: `npm test` 347 / 342 pass / 1 fail / 4 todo under Node 24.15.0; `npm run check` 161/0. The 1 failure is **pre-existing**: `REQ-004 — Bilan reads TOMATO_FRUIT_EXPORT[element]` in `nutrition/tomato/shell/spec.test.mjs:716` (regex against `app/index.html` for literal substring no longer present) — surfaced as orthogonal triage; was hidden by Node 16.20.2 false-greens default. Deferred: (a) symbol rename `FoliarRecipeTomato` → `FoliarStrategyTomato`; (b) Ca recipe `data.js` row + `computeFoliarSupply` Ca slot wiring (gated on PO data definition; 4 Ca tests stay `test.todo`); (c) five sibling foliar-strategy mailbox entries (subproject root, builder, procedure, operator, nutrition cross-crop `farm-working-days`) — still pending in `from-product-owner.md`, not in this wave's scope per Guillaume's narrow option-2 override.
- **2026-05-24 — Process-rules refactor: `everyone/` (universal auto-load) + `lib/` (opt-in) split.** Root `CLAUDE.md` shrunk to 17 lines (voice + greenhouse infra + pointers). Five sections relocated: doc-dir convention → `team/everyone/doc-dirs.md`; changelog protocol → `everyone/changelog-protocol.md`; Guillaume's lane → `everyone/guillaume-lane.md`; spec discipline → `lib/spec-discipline.md`; learnings discipline → `lib/learnings-discipline.md`. New `lib/code-shape.md` (Elm-influenced JS, lifted from inline coder persona) + `lib/req-allocation.md` (lifted from inline PO + specialist personas, was duplicated). Derivation faithful-blueprint stays in specialist persona (sole author). `_shared/` → `everyone/` rename via `git mv` (7 persona load lines + `team/CLAUDE.md` § Procedures/Principles auto-rewritten). Each persona's entry block now lists `everyone/` (universal) + the specific `lib/*.md` files it needs: PO/specialist → spec + learnings + req-allocation; coder/test-writer → code-shape; pruner/model-challenger → spec-discipline; context-coherence → spec + learnings; team-leader → none. Storage-layers section in `team/CLAUDE.md` extended from 4 to 6 layers (added `everyone/` + `lib/`). Drift-risk fix: single-source-of-truth per process rule, no per-persona duplication, no universal-load bloat for subset-audience rules. `bash scripts/check-spec.sh` 161/0; `npm test` 22 pass / 0 fail. No code change, no spec change, no STORED edit; process-doc reorg only.
- **2026-05-24 — Convention re-pivot: `derivation.md` MANDATORY for model layer, code = one valid implementation derived via TDD.** Earlier-today "derivation optional / code = source of truth" reversed. New model: spec.md + derivation.md are the semantic source of truth; code is one valid implementation that test-writer + coder + pruner derive from spec + derivation via TDD. Specialist writes `model/{spec,derivation}.md` and does NOT touch code. Project `CLAUDE.md` Specs section rewritten: "Does NOT belong in a spec" routes math → `derivation.md` (was: → code); new `## Derivation` section with faithful-blueprint contract (every formula, every coefficient+cert+source, every algorithm as numbered steps, every input/output shape, at least one worked example per non-trivial branch) + lane discipline (specialist owns .md, test-writer + coder + pruner own the code-derivation wave) + drift-detection mechanism. Specialist persona (`.claude/agents/plant-nutrition-specialist.md`): scope updated to `{spec,derivation}.md` pair owner, out-of-scope explicitly bars all code files including `.test.mjs`. Test-writer + coder personas updated to read derivation.md as a primary input (was: spec.md only); test-writer's contract extended to cover derivation worked examples + coefficients + algorithm steps, not just spec headers. Team-leader `run-wave.md` subagent prompt templates updated to brief test-writer + coder on derivation.md as mandatory input. Memory entry `feedback_no_trace_comments.md` updated with the re-affirmation. Lazy migration of 11 existing derivation.md files: each upgrades to faithful-blueprint bar when next touched (most current files lean narrative; will need expansion to schema + worked examples). No code change, no spec change, no STORED edit; convention re-pivot only.
- **2026-05-24 — Vestigial "Spec status" note in foliar-strategy derivation amended in place.** `nutrition/tomato/foliar-strategy/model/derivation.md` § "Ca-specific cuticle coverage" closing paragraph rewritten: previously claimed REQ-101 anchored a global `FOLIAR_COVERAGE_DEFAULT` with per-element axis "pending" — stale since the REQ-101 rework already landed per-recipe coverage (oligo 0.30 / 0.80, Ca 0.15 / 0.40). Replaced with accurate status: spec covers both recipes; remaining gate is the Ca-recipe `data.js` entry (coder lane) before `computeFoliarSupply` returns non-zero Ca. No spec change, no code change. `bash scripts/check-spec.sh` 161/0; `npm test` 22/22.
- **2026-05-24 — Foliar-strategy rework — full subproject rename + PO specs + 3 learnings landed.** Single-PR completion of the 2026-05-24 grill. (1) `git mv nutrition/tomato/foliar-recipe → foliar-strategy` + tree-wide path sweep (~35 files: spec.md, app/index.html, scripts/check-recipes.mjs, model/{contribution,recipe,data}.js path comments, persona files, mailbox archives, doc cross-refs). (2) New PO specs: `foliar-strategy/spec.md` (subproject-level, 3 slugs: strategy-contains-recipes, frequency-is-model-output, sprays-spread-across-farm-working-days), `procedure/user-stories.md` (4 slugs: weekly calendar derivation, even spread, one-recipe-per-spray, no operator day override), `operator/user-stories.md` (3 slugs: recipe-sheet-per-recipe, weekly-calendar-rendered, no-frequency-input). (3) `builder/user-stories.md` rewritten: REQ-113 (sprayCount input) RETIRED; REQ-114 trimmed to surfactant-only re-render (now slug `block-5-reactive-to-surfactant`); REQ-163 held verbatim (now slug `efficacite-reactive-to-surfactant`); new slug `surfactant-input` declares the lone Block 5 input. (4) `nutrition/spec.md` — new slug `farm-working-days` (Mon-Fri at Décembre, cross-crop); REQ-062 foliar paragraph fixed (stale sprayCount/REQ-112 reference replaced with frequency-is-model-output cross-ref). (5) Three learnings under new convention: `foliar-strategy/learnings/0001-strategy-recipe-hierarchy.md` (why the rename + multi-recipe shape), `0002-frequency-as-model-output.md` (frequency-as-output + day-spread decision), `0003-close-gap-bounded-by-leaf-tolerance-cap.md` (optimization criterion). (6) `nutrition/tomato/spec.md` foliar-strategy subproject summary refreshed for the new shape. Specialist's model/spec.md reshape (separate changelog entry below) landed in parallel via spawned agent. `bash scripts/check-spec.sh` 161/0; `npm test` 22/22. Coder follow-up flagged: JS symbol rename `FoliarRecipeTomato` → `FoliarStrategyTomato` (separate PR). STORED key `foliaire` left intact (Guillaume's lane). No STORED edit.
