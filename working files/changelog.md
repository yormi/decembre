# Décembre — Changelog

Append-only log of substantive changes (recipe edits, REQ additions, model architecture, page restructuring). Read at start of any session touching app state, especially under parallel Claude sessions.

Format: `YYYY-MM-DD HH:MM — brief description`. One line per change. Most recent at top. Keep ~10 entries; older history lives in `git log`.

## 2026-05-30

- **2026-05-30 — Tomato yield-recovery subgoals defined (grill, docs-only).** Diagnosed the reported triad (thin growth · flower abortion · botrytis in a dry house) as ONE pH-7.4 lockout crisis with three faces, not three problems — N −26%, P −90%, Ca below resistance band (cert 4 tissue + pour-through CE 1.5 vs 3.0 target). Flower abortion ruled NOT boron (tissue B 57 ppm suff). Priority frame = root-cause first: SG1 free P via pH drawdown (7.4→~6.5, multi-cycle) → SG2 N to ~4% tissue (weeks) → SG3 leaf Ca into 4.5–5.5% band; all start now, finish on different clocks. Cheap gauges: petiole sap NO₃-N (Horiba) + pour-through pH replace monthly tissue for fast elements; tissue stays quarterly anchor for P+Ca. Lettuce ceiling/transplant model ranked below all of it. Refined: thin growth = N *and* Ca (Ca immobile → new growth + stem strength); N target ~4–4.5% (3.1% floor = exit-crisis milestone only); monitoring = tissue-only (sap/SPAD/Brix rejected as too noisy, pour-through soil pH kept); non-nutrition levers flagged (pollination for abortion; biofungicide/airflow/dry-morning de-leafing for botrytis). New `nutrition/tomato/doc/tomato-yield-recovery-subgoals-2026-05-30.md` + tomato-scoped learning `nutrition/tomato/learnings/0001-2026-decline-single-ph-lockout-crisis.md`. No spec/REQ, no STORED, no code.
- **2026-05-30 — Tomato T5 tissue panel: Mn / Cu contamination ruled out (Open Q #7 resolved).** Lab confirmed samples washed before digestion — the prep-protocol confirmation the panel was waiting on. Wash strips loose surface MnSO₄ / CuSO₄ foliar residue; still-élevé after washing → real tissue over-supply, not contamination (cert 3; residual uncertainty = cuticle-penetrated foliar reads as tissue + plain-rinse dust caveat, but Fe reads suff so no dust signal). `nutrition/tomato/doc/tomato-t5-tissue-analysis-2026-05-23.md` updated: §3 Mn/Cu "Match?" cells flipped to "real (wash-confirmed)", §4.3 resolution note added, priority #4 (drop foliar Mn 22→18 / Zn 22→16 / Mo 1→0.5) ungated, Open Q #7 struck. No spec/REQ, no STORED edit, no code. Foliar dose trim itself is a STORED `/retire-recipe` edit, operator timing. Also created cross-crop learnings dir `nutrition/learnings/` (first tenant `0001-tissue-wash-clears-residue-not-cuticle-or-dust.md`) — the transferable rule for reading any future panel's élevé values (tomato + lettuce).

## 2026-05-29

- **2026-05-29 11:33 — Tiger 90CR product label/spec PDF filed.** `nutrition/tomato/soil-ph/doc/tiger-90cr-organic-sulphur-label.pdf` (Tiger-Sul TS5525, ©2015) + doc-index entry. Confirms granular degradable elemental-S form behind the derivation 90%-S assumption; numeric spec table is a graphic (open PDF). **Cert still UNVERIFIED** — sheet carries no OMRI/Ecocert listing; brand name only. Derivation product flag stays cert-0 until a real certificate lands.
- **2026-05-29 — Tiger 90CR recorded as on-hand sulphur in soil-ph derivation.** New `On-hand product — Tiger 90CR` block in `nutrition/tomato/soil-ph/model/derivation.md`: 90% elemental S + ~10% bentonite → product dose = elemental ÷ 0.90; pastille-form wetting-lag caveat vs micronized-powder cadence assumption (cert 3, inside 4-wk window); cert status UNKNOWN/cert-0 flagged (formulation needs OMRI/Ecocert confirm despite S+bentonite being CAN/CGSB-32.311-permitted), refinement trigger added. Also created `soil-ph/doc/calcimeter-lab-pricing.md` + doc index (cert-2 per-lab CAD estimates, Berger media-lab mismatch). Docs-only. `check-spec` 12/0 bash, 146/1 node (the 1 = pre-existing REQ-002 Canna scan).
- **2026-05-29 — `nutrition/lettuce/soil-ph/` subproject created from scratch (model-layer, docs-only).** Mirrors the tomato sibling; shared sulphur chemistry, lettuce numbers re-anchored. Files: `model/spec.md` (intent + scope + inherited specs; empty spec section, blocked on lettuce root-zone target band + calcimeter), `model/derivation.md` (lettuce soil state SME pH 7.48 / Ca 10612 kg/ha from Berger 596617 — more alkaline than tomato; bed 30.4 m² / block 136.8 m² / 27 360 kg topsoil; two-tier cap re-scaled — tilled ~3.0 kg/bed, standing ~1.4; passes-by-calcimeter table; tilled-at-flip default cadence; TIGHTER guardrails ECe ≤ 1.3 dS/m + pH floor 6.0), `model/learnings/0001-lettuce-soil-ph-scope-and-deltas-from-tomato.md` (delta table vs tomato; do-not-port-5.4-kg; pre-transplant EC check; data gaps — no direct water-pH, carbonate unmeasured, PO target-band gap). Also amended tomato sibling derivation + learnings this session: two-tier cap (5.4 tilled / 2.5 standing) replacing the mislabeled turf number, monthly-micronized cadence validated, annual cap reframed as monitorable (EC/pH/aeration), gypsum salinity self-limit. No code, no STORED edit. `bash scripts/check-spec.sh` 12/0 bash · 146/1 node (the 1 = pre-existing REQ-002 Canna scan, unrelated).
- **2026-05-29 — `nutrition/tomato/soil-ph/` subproject created from scratch (model-layer, docs-only).** Files: `model/spec.md` (intent + scope + inherited specs; spec-entry section empty, blocked on the soil-root-zone target band + a calcimeter free-carbonate reading), `model/derivation.md` (soil-state anchor table, free-carbonate feasibility gate, sulphur dose rule S = 0.32 kg/kg CaCO₃ with calcimeter %→dose table, refinement triggers, pH-scale rule), `model/learnings/0001-carbonate-gates-ph-correction-feasibility.md` (acid amendment rejected — sulphuric ~375–1500 kg + cert-unresolved + gypsum/CE; sonotube Ca leaching makes any programme a holding loop; citric fertigation kept as separate micro-chelation lever). No code, no STORED edit. Parent `nutrition/tomato/spec.md` Subprojects list gains a `soil-ph/` pointer. `npm run check` 158 pass / 1 fail — the 1 (REQ-002 non-Ecocert `Canna` copy scan) is pre-existing, unrelated to this docs-only scaffold.

## 2026-05-28

- **2026-05-28 — Diagnostic-page reference images wired (5 tomato entries; asset wiring, no REQ).** Optional `image` field added to 5 `DIAG_DATA.tomato` symptom objects in `app/diagnostic/logic.js`: `t-mg`, `t-k`, `t-n`, `t-ber` (BER), `t-b-fruit` — note the curation doc / inbox label `fruit` is the *location* id; the BER symptom object id is `t-ber`, so the BER image maps there. Result-card render (`#diag-result`) gains a guarded `${s.image ? '<img …>' : ''}` block — imageless entries (the other 8, incl. `t-mn`) emit no `<img>`, no layout shift. **Mn-toxicity guardrail respected:** `t-mn` stays imageless. Image-src approach: the 5 source photos (one was 7.4 MB) resized to ≤900px / q82 (all <100 KB) into a new app-served dir `app/diagnostic/images/`; `scripts/build.mjs` taught to copy `app/diagnostic/images/ → dist/diagnostic/images/` (new `STATIC_ASSET_DIRS` + recursive `cp`), so `<img src="diagnostic/images/…">` resolves from the served dist root. Verified: all 5 return HTTP 200 from the running dev server. New `page.css` rule `.diag-result-image` (max-width 360px, lazy-load). `bash scripts/check-spec.sh` 158 pass / 1 fail — the 1 (REQ-002 non-Ecocert copy scan) is pre-existing (fails identically with changes stashed). `npm test` 26/29 — the 3 failures (foliar-strategy ×2, sidedress-recipe builder ×1) are pre-existing in-flight work on this branch, unrelated (fail identically stashed). No spec/REQ, no STORED edit. Original doc-tree photos under `nutrition/tomato/doc/` left in place (sourcing record); the web copies are derived assets.
- **2026-05-28 — Diagnostic-page symptom images sourced + coder wave queued.** 5 verified tomato symptom images staged in `nutrition/tomato/doc/diagnostic-images/` (+ `SOURCES.md`, internal-use-only): `t-k` + `fruit` BER from Wikimedia, `t-mg` + `t-b-fruit` from IRIIS Phytoprotection (MAPAQ, French credit); `t-n` uses existing farm photo. Mapping in `nutrition/tomato/doc/diagnostic-image-curation.md`. Wiring into `app/diagnostic/` handed to team-leader (`from-plant-nutrition-specialist.md`) — coder lane. 8 of 13 tomato entries stay imageless: no library (Wikimedia / IRIIS) has tomato-specific shots for Fe/Mn/Zn/Cu/B-apex/P/Mo/B-stem; await own-farm photos. Mn-toxicity farm photo deliberately NOT mapped to `t-mn` (deficiency) — wrong mechanism.
- **2026-05-28 — Recipe-history detail panel reworked to "what + why" (render-only; audit data untouched).** `app/historique-nutriments/logic.js` rewritten. Each expanded entry shows a strategy-level "Pourquoi" headline (the `reason` preamble) then a card per changed channel (💧 Fertigation / 🌱 Sol / 🍃 Foliaire). Each card leads with before→after coloured chips (🔺 up / 🔻 down, friendly names Magnésium/Potassium/Bore/Actisol/Farine de plumes…) — the scannable "what" — and tucks the per-channel reasoning (split from the source `reason`'s (1)/(2)/(3) markers) behind a `▸ Détails` toggle so the audit prose stays reachable without cluttering the default view. "What" is fully data-derived: before = the retired `fullSnapshot`, after = the next-newer entry's snapshot or live `STORED_RECIPE.tomato` for the newest entry — so only genuinely-changed stages/values appear (T1–T4 drop out automatically). Spec ids + code identifiers (`REQ-NNN`, `STORED_RECIPE…`, `FP_RECIPE…`, `computeFoliarSupply`, `data.js`) stripped from displayed prose via `stripJargon` (whole spec-citing parentheticals dropped, redundant "Fertigation T5 :" lead-ins removed). Replaced the prior raw-prose RAISON wall + JS-object REMPLACÉE-PAR dump + always-on full grams tables. Legacy entries (no `fullSnapshot`) fall back to stripped reason + partial-snapshot JSON. RECIPE_HISTORY never edited (cert audit trail). `bash scripts/check-spec.sh` 158/1 — the 1 failure (REQ-002 non-Ecocert copy scan) is pre-existing, fails identically with the change stashed.
- **2026-05-28 — Tomato soil page now renders ONE row, current stage only, from STORED.** Follow-up to earlier sidedress-only strip. `nutrition/tomato/shell/sol/page.html` dropped: Ecocert chips, bed-area chip, intro prose between chips and table, full T1-T5 stage table, cycle-total band. Replaced by: card title + current-stage label (`semaine N → stade Tx · <label>`) + one bullet per active product in `STORED_RECIPE.tomato.sidedress` (`Actisol-5-3-2`, `FarinePlumes`, `AlfalfaMeal`, `Frass` known keys, falls back to raw key if extended). Inline `<script>` renders on DOMContentLoaded; stage derived live via `getStageFromWeek(getWeekNumber())` (same logic as `autoStage()`). Zero-dose products hidden (Actisol currently 0 g per 2026-05-28 retire). No periodic refresh: stages advance weekly, page reload picks up the new stage. `bash scripts/check-spec.sh` 159/0. Source-of-truth alignment: operator sol page now reflects STORED live — no more hand-synced numbers (`nutrition/tomato/shell/spec.md` row "soil page HTML displays the same numbers — currently hand-synced" now obsolete, flag for PO).
- **2026-05-28 — Tomato soil operator page stripped to sidedress recipe only.** `nutrition/tomato/shell/sol/page.html` reshaped: 4 cards → 1. Removed: Programme soufre (pH baisse), Proposition luzerne+frass (Climax PA pilot proposal), Statut Ecocert standalone card. Kept: Engrais sol tomate, rewritten as a scannable 5-row table (Stade · Semaines · Actisol g/sem · Farine g/sem · Total stade) — replaces the prior 5 stacked stage cards + prose "Pourquoi" blocks. Ecocert ✅ chips for Actisol + Farine de plumes inlined at card head (preserves cert visibility after Ecocert card removal). Surface chip retained. Cycle-total band kept (114-127 kg Actisol · 170-189 kg Farine ≈ 28-31 kg N). No spec/REQ change — `nutrition/tomato/shell/spec.md` row already marks the soufre card as "n/a / not consumed by Bilan" and the sidedress numbers stay hand-synced with `TOMATO_SIDEDRESS[stage]`. `bash scripts/check-spec.sh` 159/0; `npm test` 422 pass-baseline preserved (sole transient `REQ-098 T5 kSulfate` failure under `npm test` passes when run in isolation — pre-existing test-ordering flake, unrelated to HTML edit). Soufre 6-12 month pH program no longer surfaced on operator side.
- **2026-05-28 — STORED retire tomato — pivot anti-botrytis T5 sur 3 canaux.** Fertigation T5: MgSO₄ 2800→1000 g, K₂SO₄ 7000→5500 g (antagonisme cationique sur absorption Ca), borax remis 11 g (était retiré 2026-05-23, cicatrisation lésions). Cuve maître 170 L → 110 L sur cycle 5 jours (jours 6-7 eau claire). Sidedress T5: Actisol 900→0 g/planche (alignement REQ-089/Ca-saturé), farine 1341→2000 g/planche (vigueur). Foliaire: spray B CaCl₂·2H₂O 100 g / 15 L réintroduit (Ecocert vérifié, source du retrait 2026-05-06 résolue), 2×/sem, sans surfactant (coverage ~0.15, ~17 mg Ca/m²/sem, bypass obstacles Ca racinaires vs botrytis). Modèle Ca recipe (data.js + computeFoliarSupply) reste gated/todo — STORED audit-trail ouvert avant pipeline de rendu. T1-T4 inchangés sur les 3 canaux. Snapshot complet capturé dans RECIPE_HISTORY. `bash scripts/check-spec.sh` 159/0.
- **2026-05-28 — Tomato fertigation operator page: cut multipliers + radiation + diagnostic card; prose-to-scannable.** Cuts already match locked spec slugs `two-card-shape` (no diagnostic card) + `stock-volume-inline-in-prep-step` (radiation not surfaced); A-surgical fork (kept `stage-card` shell + Dosatron 2% row + stage selector — full `two-card-shape` compliance deferred). `nutrition/tomato/fertigation-recipe/operator/page.html`: removed K₂SO₄ multiplier row, MgSO₄ multiplier row, Radiation moyenne row, tomato "✓ Bien réglé quand" card (lines 114-130; nursery "Bien réglé quand" untouched per scope). Lundi-card intro + Mardi-step 2/3 prose tightened (action+arrow phrasing, fewer connectives). `operator/logic.js`: deleted `param-radiation` write + lettuce multiplier-row show/hide block; step descriptions 1-6 tightened (e.g. "Dissoudre dans l'eau chaude d'abord — le K₂SO₄ se dissout lentement à froid" → "Dissoudre dans eau chaude d'abord — froid = lent"). `operator/page.css` comment amended. Orphan fix: `app/operator/recalc.js:131` writing to dead `#param-radiation` element removed (would null-deref). `bash scripts/check-spec.sh` 159/0. No spec edit, no STORED edit. Cross-cutting drift surfaced for follow-up (out of scope this turn): `tomato-only-render` slug violated by crop-toggle + lettuce/nursery branches still present on the page.
