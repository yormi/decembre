# Décembre — Changelog

Append-only log of substantive changes (recipe edits, REQ additions, model architecture, page restructuring). Read at start of any session touching app state, especially under parallel Claude sessions.

Format: `YYYY-MM-DD HH:MM — brief description`. One line per change. Most recent at top. Keep ~10 entries; older history lives in `git log`.

## 2026-06-04
- **2026-06-04 — Foliaire : carte « Signes à surveiller » entièrement supprimée.** `foliar-strategy/operator/page.html` : la carte `#foliar-signs-card` (brûlure foliaire, toxicité cuivre, ligne « Reporter sur Slack #recherche-et-developpement ») retirée. `foliar-strategy/operator/logic.js` : bloc show/hide Spray-B orphelin (`signsCard.style.display`) supprimé. Aucune réf résiduelle, aucun spec/matcher. check-spec node 145/2 (Block 1 / Foliar réactivité, pré-existants/instables, zéro ajout).
- **2026-06-04 — Fertigation tomate : sélecteur de recette (Nutriments / Correction racinaire) sous la ligne Dosatron.** `fertigation-recipe/operator/page.html` : deux boutons `.stage-btn` (`#fert-recipe-nutrient` / `#fert-recipe-rootfix`) ajoutés dans `#stage-card` sous Dosatron ; bloc « Stade de croissance » enveloppé dans `#fert-stage-block` (masqué en mode rootfix) ; titre carte étapes → `id="steps-card-title"`. `app/operator/state.js` : `let currentFertRecipe = 'nutrient'`. `app/operator/logic.js` : `setFertRecipe(mode)` + `applyFertRecipeUI()` (active boutons + toggle stage-block), appelé depuis `setCrop` ; export window. `fertigation-recipe/operator/logic.js` : `buildSteps()` court-circuite vers `buildRootFixSteps()` (tomate + rootfix) — recette fixe **15 g Ocean / 4 L**, dosatron 2 %, valve chapelle 1 10 min (Orisha), remettre ligne, rincer seau ; tuiles 💧/🦀 ; render extrait dans `renderStepsList()`. Pas de stage/scaling (recette unique). Cert Ocean : **à vérifier** (statut Ecocert non confirmé). Aucun spec/STORED/matcher touché. check-spec : bash 12/0, node 145/2 (Block 1 / Foliar réactivité, pré-existants, zéro ajout).
- **2026-06-04 — Operator : étape « Écrire sur Slack #recherche-et-developpement » (log post-action R&D) retirée.** Foliaire `foliar-strategy/operator/page.html` (dernière étape 6) + fertigation `fertigation-recipe/operator/logic.js` (branche laitue, dernière étape 5) supprimées — toutes deux en fin de liste, pas de renumérotation. Retrait temporaire (« for now »), texte verbatim conservé en mémoire pour restauration. Hors scope (conservés) : alerte solution vide, note restes, escalade diagnostic, report symptômes. check-spec node 145/2 (Block 1 / Foliar réactivité, pré-existants/instables, zéro ajout).
- **2026-06-04 — Fertigation semis (pouponnière) : ligne Produits + Instructions retirées, doses 2 c.s., « Bien réglé quand » admin-only.** `fertigation-recipe/operator/page.html` : ligne UI « Produits : Acadie Poisson… » supprimée (données inchangées) ; carte **Instructions** (9 étapes + Fréquence) supprimée ; carte « ✓ Bien réglé quand » dotée d'`id="nursery-signs-card"`. `app/operator/recalc.js` `recalcNursery()` : tuiles Eau/Poisson/Algue/Fer arrondies à **2 chiffres significatifs** (`Number(v.toPrecision(2))`, ex. 1219→1200, 188→190, 1,41→1,4). `app/operator/logic.js` `applyAdminMode()` : carte signs masquée hors `#admin`. Aucun spec/matcher/STORED touché. check-spec : bash 12/0 ; node 145/2 (Block 1 / Foliar réactivité, pré-existants, zéro ajout).
- **2026-06-04 — Fertigation : crop Laitue (champ) rendu admin-only.** `app/operator/logic.js` `applyAdminMode()` : bouton `fert-crop-lettuce` masqué hors `#admin` (tomate + semis laitue restent publics) ; garde ajoutée — un non-admin sur `fertigation` avec `currentCrop === 'lettuce'` est rebasculé sur tomate (`setCrop('tomato')`), couvre bookmark `#fertigation/lettuce` + sortie du mode admin. Init OK (setCrop puis applyAdminMode corrige). Pas de nouvelle entrée `ADMIN_PAGES` (gate au niveau crop, pas page). Aucun spec/matcher sur ce bouton. check-spec : node 145/2 (Block 1 / Foliar réactivité, pré-existants, zéro ajout).
- **2026-06-04 — Fertigation : titre carte épuré + position d'unité uniformisée.** `operator/page.html` : titre « 🌅 Vendredi matin — préparer la solution » → « **🌅 Vendredi matin** ». `operator/logic.js` `buildSteps()` : tuiles produits passées de « 5 500 g » (valeur+unité sur une ligne) à **grand nombre + ligne unité « g »** séparée, comme la tuile 💧 Eau (champ `unit` désormais utilisé partout). Helper `fmtG` → `formatNumber` (non abrégé ; `fmtNum` déclenchait `identifiers-unabbreviated`). check-spec node 145/2 (Block 1 / Foliar réactivité, pré-existants, zéro ajout).
- **2026-06-04 — Fertigation : volume d'eau en tuile 💧 + étape 2 renommée « Mélanger ».** `fertigation-recipe/operator/logic.js` `buildSteps()` : le badge « 8,5 chaudières de 20 L » devient une **tuile 💧 « Eau »** (grand nombre + ligne unité « chaudières de 20 L »), placée en 1ʳᵉ position de la grille de l'étape 2, aux côtés des produits ; ligne `unit` optionnelle ajoutée au rendu des tuiles. Titre étape 2 « Remplir et ajouter les produits » → **« Mélanger »**. Badge `step-amount` retiré de l'étape (volume porté par la tuile). check-spec node 145/2 (Block 1 / Foliar réactivité, pré-existants, zéro ajout).
- **2026-06-04 — Fertigation : étapes 2 et 3 fusionnées.** `fertigation-recipe/operator/logic.js` `buildSteps()` : « Remplir la solution » (badge chaudières) + « Ajouter les produits » (tuiles) regroupées en une seule étape 2 « **Remplir et ajouter les produits** » (badge volume + tuiles ensemble) ; étapes suivantes renumérotées (tomate 3 Brasser / 4 ligne dosatron ; laitue 3 Fe / 4 Brasser / 5 Slack). Condition de rendu du badge `s.amount` assouplie (`!s.ingredients` retiré) pour afficher volume ET tuiles sur la même étape. check-spec node 145/2 (Block 1 / Foliar réactivité, pré-existants, zéro ajout).
- **2026-06-04 — Pages operator alignées sur le déplacement Mo (team-leader).** `nutrition/tomato/fertigation-recipe/operator/logic.js` : `getNutrients()` (branche tomate) repasse `naMolybdate` non-multiplié depuis `STORED_RECIPE.tomato.fertigation[stage]` (miroir du pattern `borax`/Solubore de drift.js, défaut 0) ; `buildSteps()` pousse une tuile **Molybdène** (🔶, dose en g via `fmtG`) après Solubore. Foliaire : `buildFoliar()` est data-driven sur `STORED_RECIPE.tomato.foliaire.A` → la ligne Mo retirée disparaît automatiquement (aucune réf Mo résiduelle ; watch-list = brûlure + Cu seulement). page.html inchangés (tuiles rendues depuis l'array). Hors scope : rename borax→solubore (lane séparée), STORED, RECIPE_HISTORY, dist/. check-spec : bash 12/0, node 145/2 (Block 1 / Foliar réactivité pré-existants, zéro ajout).
- **2026-06-04 — Fertilisation/sol : tuile Soufre — Tiger masquée quand la dose = 0 (Arrêt).** `nutrition/tomato/shell/sol/page.html` `renderRecipe()` : la branche `else` qui poussait la tuile muette « Arrêt » (mot + raison) est retirée — à dose 0 la tuile Tiger n'apparaît plus du tout. Cas « choisir le pH » (aucun pH sélectionné) conservé ; dose > 0 inchangée. `recommendSulphurDose` inchangée (retourne toujours `{grams:0, word, sub}` — contrat testé). Test de câblage `spec.test.mjs` mis à jour : « pH 6,6- renders Arrêt » → « pH 6,6- (dose 0) hides the Tiger tile » (`assert.doesNotMatch(/Tiger/)`). `node --test` vert ; check-spec node 145/2 (Block 1 / Foliar réactivité, pré-existants, zéro ajout).

_Older entries (≤ 2026-05-30) live in `git log`._
