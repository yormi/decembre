# Décembre — Changelog

Append-only log of substantive changes (recipe edits, REQ additions, model architecture, page restructuring). Read at start of any session touching app state, especially under parallel Claude sessions.

Format: `YYYY-MM-DD HH:MM — brief description`. One line per change. Most recent at top. Keep ~10 entries; older history lives in `git log`.

## 2026-05-24

- **2026-05-24 — First CONTEXT.md landed: foliar strategy vocabulary.** `nutrition/tomato/foliar-recipe/CONTEXT.md` written per grill-me skill format. Locks five terms from the 2026-05-24 PO grill: **foliar strategy** (one per crop, the whole feeding plan), **foliar recipe** (one tank's mix, many per strategy, REQ-029-clean), **spray** (one execution of a recipe on a specific day — replaces my prior "application" coinage), **weekly leaf-tolerance cap** (per-recipe cuticle-damage upper limit on sprays/week — replaces "cuticle cap"), **farm working days** (Mon-Fri at Décembre). Example dialogue + Avoid aliases per format. Scope: per-subproject (Guillaume's call); file lives at current path because rename to `foliar-strategy/` is pending coordination with in-flight model session (foliar-recipe/model/{derivation,spec}.md uncommitted from parallel work). No CONTEXT-MAP.md yet — first context only, lazy escalation. No code change, no spec change, no STORED edit.
- **2026-05-24 — Convention shift: `derivation.md` retired as required artifact + Elm-style JS adopted for calc/model layers.** Project `CLAUDE.md` Specs section updated: code (`calc.js`, `model.js`, `data.js`) is now the source of truth for current math; `derivation.md` becomes **optional**, kept only where code-only reading is genuinely hostile (dense integrators / multi-step coupling). Why-this-number + why-this-decision both flow to `learnings/NNNN-slug.md`. Lazy migration — existing derivation.md files stay until next touched; when next touched, formulas move to code (or code comments at the call site), why-this-number to a learning, file deleted. Maintainer keeps exceptions by judgment. New **Code style — Elm-influenced JS** section added: tagged-object discriminated unions (`{kind: 'foliar', ...}`), exhaustive switches with unreachable-throw default, Result/Maybe shapes for partial functions, immutable inputs, total functions over silent defaults, single-shape returns. Captures most of Elm's safety wins without toolchain. Lazy migration — existing JS stays until next touched, refactor on edit. Decision flagged as Elm-pilot deferred: full Elm reserved for fertilization-interaction model subproject if/when it lands (greenfield, pure, type-heavy). No code change, no spec change, no STORED edit; convention shift only.
- **2026-05-24 — Doc convention: flat `learnings.md` → numbered `learnings/NNNN-slug.md` per subproject.** Project-root `CLAUDE.md` Specs section updated: derivation.md keeps its current role (live math, evolves with spec), learnings split out to numbered append-only files dated per entry. Three-criteria filter (hard to reverse + surprising + real trade-off) keeps file count low. Voice stays research-notebook, not corporate ADR. Cross-cutting decisions land at the highest subproject scope they apply to (foliar-only → under foliar-strategy/, cross-crop → nutrition/learnings/, project-wide → root learnings/). Memory entry `feedback_no_trace_comments.md` also updated. **Migration: lazy** — existing 11 flat `learnings.md` files stay until next touched. First instance under new shape will land alongside the foliar-strategy spec rework starting next turn. No code change, no spec change, no STORED edit; convention shift only. `nutrition/tomato/foliar-recipe/model/derivation.md` updated to reflect Test 1 Path B operational decision: (a) "No-macro by design / Ca" entry rewritten — 2026-05-06 Spray B retirement framing superseded by 2026-05-24 reactivation (CaCl₂·2H₂O vendor + product Ecocert-verified), captures dose (100 g/15 L, 2×/wk), delivered Ca (~56 mg/m²/wk = ~18 % of 315 mg/m²/wk canopy gap), and the obstacles-chain math anchor; (b) new "Ca-specific cuticle coverage" subsection — 0.15 no-surfactant / 0.40 with-surfactant (cert 2, ratio 0.50 × sulfate-metal coverage at each surfactant state, Schönherr 2000 anchor + Test 1 tissue refit trigger at week-4); (c) `BURN_CAP_BASE_G.Ca = 100 g/15 L` row added (cert 3, anchored to REQ-025 9 mS/cm under 10 mS/cm cap × 10 % safety margin via CaCl₂·2H₂O solution-CE table). `computeFoliarSupply` still returns `Ca: 0` — schema wiring + per-element coverage axis spec change gated on PO coordination next turn. `bash scripts/check-requirements.sh` 161/0; `npm test` 22/22. No spec change, no code change, no STORED edit this turn.
- **2026-05-24 — Persona infra: added `procedures/` layer + on-demand load.** All 6 personas refactored (PO, plant-nutrition-specialist, model-challenger, team-leader, spec-pruner, context-coherence) so they no longer auto-read mailboxes / inbox files / drafts / full spec tree / MEMORY on entry — that load fills context before any real work. Persona files now read only: own file + CLAUDE.md + team-coordination/CLAUDE.md + own principles.md + _shared/principles.md + recent changelog. `requirements.md` also moved to lazy — loaded inside the procedure that needs it (PO at REQ-draft time; specialist at subproject-work time; pruner via `prune-subproject.md`; coherence via `audit-walk.md`; team-leader via `run-wave.md`). Procedural how-to extracted to 8 new files under `team-coordination/<persona>/procedures/`: PO `notify-team-leader.md`; specialist `respond-to-challenger.md` + `triage-fan-out.md` + `notify-team-leader.md`; challenger `mode-b-review-queue.md`; team-leader `process-mailbox.md` + `run-wave.md`; spec-pruner `prune-subproject.md`; context-coherence `audit-walk.md`. **Behavior change for team-leader:** auto-listener removed — mailbox is on-demand only now, won't kick off when idle. `team-coordination/CLAUDE.md` updated: skills/principles/persona → 4-layer model adding **procedures** (persona-local, trigger-loaded, not slash-invocable; distinct from cross-persona skills in `.claude/skills/`). Persona line counts: team-leader 230→71, plant-nutrition-specialist 253→120, spec-pruner 158→60, context-coherence 147→75. coder + test-writer skipped (already lazy via spawn-prompt). No code change, no spec change, no STORED edit.
- **2026-05-24 — Ca foliar Test 1 re-scoped: BER → canopy + disease.** Operator update: BER mostly gone. Goal shifts to (a) raise canopy tissue Ca toward modeled demand (close 315 mg/m²/wk canopy gap), (b) reduce fungal disease susceptibility — specifically stem canker at `nutrition/tomato/doc/stem-canker-2026-05-24/` (Ca-pectate cell-wall mechanism, cert 4 Conway/Sams/Volpin & Elad). Spray-target expanded foliage + fruit + **stems** indiscriminate; starting frequency bumped Path A (1×/wk) → **Path B (2×/wk, 100 g CaCl₂·2H₂O + surfactant per 15 L tank, 56 mg Ca/m²/wk delivered, 18 % of canopy gap)**. Path C (3×/wk) ramp option. Endpoints retargeted: tissue Ca trajectory (goal 1) + new-canker incidence vs 2026-05-24 baseline (goal 2); existing lesions expected NOT to reverse under Ca alone. New disease-management appendix: sanitation + humidity / ventilation are the **first** two levers; foliar Ca is the third; PA Taillon visual differential + biofungicide as 4 and 5. Spray itself is the cheapest disease-prevention consumable; operator load is sanitation + humidity time. No code change, no spec change, no STORED edit.
- **2026-05-24 — Ca/BER test doc: pH 7.4 P-lockout folded in + Test 7 (sulfur) added + Test 5 promoted.** `nutrition/tomato/doc/ca-ber-investigation-tests-2026-05-24.md` updated after recognizing root-zone bone meal isn't a viable P lever at Décembre's pH 7.4: (a) hydroxyapatite dissolution stalls > pH 7 and (b) any dissolved P re-fixes as Ca-phosphate (both cert 4). Test 4 viability caveat split in two — caveat 1 retains foliar drip-limit math; caveat 2 documents the root-zone lockout and withdraws prior "switch to root-zone bone meal" pivot. New **Test 7 — elemental sulfur amendment to drop pH 7.4 → 6.5** added as the real P lever (multi-month, PA-Taillon-scoped, cert 4 allowed under CAN/CGSB-32.311). Test 5 (destructive root sample) promoted from end-of-T5 slot to decision-critical fast read on mechanism A — only in-season A diagnostic after foliar+root-zone P both locked out. Run order rearranged: Test 5 → slot 2, Test 4 → optional positive-only diagnostic at slot 3, Test 7 → slot 4 as next-season workstream. Model-change table extended (Test 7 → `rootHealthMultiplier` activation + `PH_RESPONSE` cert bump). Out-of-scope updated — pH correction is no longer out-of-scope, just a separate slow workstream. No code change, no spec change, no STORED edit.
- **2026-05-24 — `nutrition/tomato/research/` renamed to `nutrition/tomato/doc/`** to match project convention (`<ancestor>/doc/` per CLAUDE.md). `git mv` preserved history on the two tracked files (`tomato-t5-tissue-analysis-2026-05-23.md`, plus the freshly-added subdirs `symptoms-2026-05-21/`, `stem-canker-2026-05-24/`, `ca-ber-investigation-tests-2026-05-24.md`). 15 inbound refs rewritten across `team-coordination/model-challenger/drafts.md`, `working files/changelog.md`, `nutrition/soil-contribution/{integrator.js,derivation.md}`, `nutrition/tomato/{fertigation-recipe,sidedress-recipe}/model/derivation.md`, and the two CLAUDE.md / diagnosis.md files in the moved subdir. `bash scripts/check-requirements.sh` 161/0; `npm test` 22/22. NOT touched: spec.md files, STORED, dist/, app/. Parallel sessions running in this tree need to refresh their snapshots — paths `nutrition/tomato/research/*` no longer exist.

- **2026-05-24 — Stem-canker photo filed (pathology, not nutrition).** New `nutrition/tomato/doc/stem-canker-2026-05-24/` with `IMG_4455.HEIC` + JPG companion + `CLAUDE.md`. Mid-stem elongated tan-brown sunken lesion ~5-7 cm on a main tomato stem, no sporulation visible in frame. Visual differential listed (Botrytis cinerea most likely; Didymella, Alternaria, bacterial canker also flagged) — no diagnosis assigned. Cross-pointer to `symptoms-2026-05-21/` photo #4 — if that leaf blotch turns out Alternaria, pairing the two could close both differentials. Filed under research/ at Guillaume's call (no pathology subtree exists yet); flagged for migration to a sibling `health/` subtree if disease docs accumulate.

- **2026-05-24 — Test 4 (foliar P) viability re-scoped after operator drip-threshold input.** `nutrition/tomato/doc/ca-ber-investigation-tests-2026-05-24.md` Test 4 updated: KH₂PO₄ marked NOT certified (synthetic, barred under CAN/CGSB-32.311) — kept in dose table only as literature reference, not deployable. Application volume corrected to **40 mL/m²** (15 L tank / 382.9 m², operator-confirmed drip ceiling) — prior protocol assumed ~1 L/m² literature default, 25× off. Redone P-delivery math: at 40 mL/m², Ecocert-clean liquid bone-meal extract (5 % uptake, 2-5 % P density) delivers 1-9 mg P/m²/wk vs ~400 mg P/m²/wk gap → closes 0.3-4.5 %. Test 4 re-scoped from therapy to **positive-only diagnostic**: tissue-P rise = mechanism A weakly confirmed; tissue-P unchanged = uninterpretable. Real P lever flagged as root-zone bone meal (fertigation or banded sidedress) — separate test, weeks-to-months timeline. No code change, no spec change, no STORED edit.
- **2026-05-24 — Ca foliar dose computed with surfactant + indiscriminate-target operator decision.** Test 1 in `nutrition/tomato/doc/ca-ber-investigation-tests-2026-05-24.md` rewritten: full obstacle chain (Ca demand T5 2 250 mg/m²/wk → Vmax 0.50 × antagonism 0.80 × root-mass 0.70 × pH 0.94 = net 0.27 of bank → effective canopy delivery ~1 935, canopy gap 315, fruit gap 55), CaCl₂·2H₂O burn-cap math (100 g per 15 L tank = 0.67 % = 9 mS/cm EC, 10 % safety margin under REQ-025), Ca-with-surfactant coverage 0.40 (cert 2 — 0.15 × 2.67 surfactant-boost ratio matching sulfate-metals 0.30 → 0.80). **Recommended dose: 100 g CaCl₂·2H₂O + surfactant per 15 L tank, 1 spray/week T3-T5, foliage + fruit indiscriminate, separate tank from oligo spray (REQ-029).** Closes ~51 % of fruit gap (BER-relevant) at Path A; bumps to ~100 % at Path B (2 sprays/week). No code change, no spec change, no STORED edit (gated on `/retire-recipe` + surfactant vendor sourcing).
- **2026-05-24 — `transpirationFactor` locked at 1.0 (yield-target proxy retired).** `nutrition/soil-contribution/integrator.js` `transpirationFactor()` now returns 1.0 unconditionally; signature preserved (args ignored) for caller compatibility. Comment block points at research doc Test 6 (variants 6a sunlight × RUE / 6b FAO-56 ET₀) as the physics-anchored replacements; operational value held at 1.0 until either variant ships. Justification: full canopy + Test 6b worked-example factor ≈ 1.0 at current irrigation regime + field signal traces to root-side obstacles, not transpiration shortfall. `npm test` 22/22 pass.
