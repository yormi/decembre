# Décembre — Changelog

Append-only log of substantive changes (recipe edits, REQ additions, model architecture, page restructuring). Read at the start of any session that will answer questions about app state, especially when running parallel Claude sessions.

Format: `YYYY-MM-DD HH:MM — brief description`. One line per change. Most recent at the top.

## 2026-05-08

- afternoon — `FP_RECIPE_T5.fertigation` rewritten mass-balance pure (no SME credit): K2SO4 0→5167 g, MgSO4-7H2O 0→1379 g, **Solubore 9 g added** (boric acid non-ionic, single-channel for B; Ecocert validated). Foliaire Solubore stays 0. `calcNutrSupply` FP-mode branch updated: (a) `fertMixFactor = 1.0` instead of 0.5 (no SME pool to coexist with), (b) reads `FP_RECIPE_T5.fertigation['Solubore']` → `supply.fert.B`, (c) `soil[el] = 0` for everything except Ca and P (mass-balance philosophy: replenish offtake regardless of bank; soil is a verification target, not a supply credit). Block 4 rendering shows Solubore line + FP-mode "no mixing factor" note when `nutrRecipeMode === 'fp'`. Pourquoi modals updated for K/Mg/B with mode-aware equations. Stored-mode behavior unchanged. 56/56 PASS.
- afternoon — Salanova nutrition subpage refactored to mass-balance flow (mirrors tomato): 1) Besoins 2) Compost (NEW — Savaria ORGANIMIX residual via `RECIPE_INPUTS.compostReleasePerWeek`) 3) Front-load (moved before fertigation, presented as compost-style gap grid with click-to-modal rows) 4) Fertigation actuelle (now with `renderGapGrid`) 5) Leviers + new "Recette proposée" admin card (`renderProposedRecipeLettuce`). Soil block (Sol — SME × transpiration) removed from supply chain — soil mass-flow draws on the bank, doesn't replenish (parity with tomato 2026-05-07 refactor). Block 1 needs grid: DW tissu column dropped from visible row; cert + DW concentration + demand equation now surface in pourquoi modal on click. New pourquoi keys: `lettuce-demand.*`, `lettuce-compost.*`, `lettuce-frontload.*`, `lettuce-fert.*`. Hypothèses copy rewritten for mass-balance framing. 56/56 PASS.
- 08:10 — `STORED_RECIPE.{crop}.{channel}` consolidation. Fertigation re-locked à valeurs PA Taillon (avril 2026, oligos inclus T1-T5 : FeSO₄ 20g, ZnSO₄ 4g, MnSO₄ 7g, CuSO₄ 1g, Borax 9g, Mo Na 1g — K/Mg ramp avec stade). `TOMATO_SIDEDRESS` + `FOLIAR.tomato` migrés sous `STORED_RECIPE.tomato.{sidedress, foliaire}`. `computeStageRecipe` désormais source FP uniquement (lu par Block 7 drift gauge + nutrRecipeMode='fp'). `captureCurrentSnapshot()` simplifiée à `structuredClone(STORED_RECIPE.tomato)`. Block 7 stored-vs-FP rétabli comme jauge significative (T5 stored K=3489/Mg=1396 vs FP_RECIPE_T5 0/0 — drift visible). `/retire-recipe` skill + CLAUDE.md + check-recipes.mjs (alias verifier-internal pour migration douce) + check-requirements.sh tous mis à jour. Pas d'alias backwards-compat dans index.html. 56/56 PASS.
- 09:30 — `computeStageSidedress(stage)` added (mass-balance, Ca-aware, farine-only). `FIRST_PRINCIPLES_SIDEDRESS[T1..T5]` and `FP_RECIPE_T5.sidedress` now wired from this function at script load (was hand-set 1300 g farine T5 only). New T-by-T values: T1=0, T2=0 (compost covers vegetative N), T3=637g, T4=1064g, T5=1655g per planche. Stored TOMATO_SIDEDRESS unchanged — requires `/retire-recipe` to swap; FP recipe display reflects first-principles target.
- morning — Semis laitue subpage added to Nutrition page (third crop sub-toggle). New constants `LETTUCE_NURSERY_TISSUE_DW` (cert 3 seedling-adjusted: N 5%, K 6%, Ca 2% — others same as mature), `LETTUCE_NURSERY_DM_FRACTION = 0.07` (cert 3, drier than mature 5%), `NURSERY_DEFAULTS` (50-cell, 50 trays/cohort, 0.149 m²/tray, 1.25 L/tray fertigation). New fns `calcLettuceNurseryDemand` (per-plant + per-tray), `buildNutrimentNursery`. CROP_PAGES.nutriment += 'nursery'. Defaults: target 150g, cycle 35d, 50-cell. Per-tray as primary unit. Only context + Block 1 (besoins) populated; substrate / fertigation / leviers as placeholders. Cell-volume cap warning surfaces if target exceeds physical cap (50-cell ~90g; 32-cell ~175g).
- 08:05 — `/retire-recipe` correction: trigger remains `TOMATO_SIDEDRESS` + `FOLIAR.tomato` only, but `fullSnapshot` now captures fertigation again (as live OUTPUT of `computeStageRecipe(stage)` per T1-T5 — the K₂SO₄/MgSO₄ doses actually weighed). Trigger ≠ snapshot scope: plant-need edits don't fire the skill, but when a sidedress/foliar retirement happens, the snapshot reproduces the full applied recipe at that date. `captureCurrentSnapshot()`, `renderFullSnapshot()`, page intro copy, schema doc, SKILL.md updated. CLAUDE.md unchanged (already correct).
- 07:30 — `/retire-recipe` skill scope narrowed to STORED recipes only (`TOMATO_SIDEDRESS`, `FOLIAR.tomato`). Fertigation dropped — no stored constant since TOMATO_STAGES retired this morning, value derived live via `computeStageRecipe(RECIPE_INPUTS)`. `captureCurrentSnapshot()`, `renderFullSnapshot()`, page intro copy, schema doc comment, SKILL.md, CLAUDE.md all updated. Legacy entries unaffected. (Superseded by 08:05.)
- 07:18 — REQ-026/027 removed (predicted-vs-measured CE + substrate CE leach trigger). Both required measurement-logging UI we don't have. Re-add when infrastructure lands. Per "if not auto-enforceable, not a requirement" principle.
- 07:17 — INCOMPATIBLE_RECIPES + MIX_ORDER constants declared in index.html (REQ-030/031, both → PASS). REQ-018 effectiveEff foliar branch fixed: was applying soil-pH penalty (PH_RESPONSE['sulfate-metal'](7.4)=0.10) to MnSO4/ZnSO4/CuSO4 foliar products, dropping eff to 2.7%. Routing rule now: foliar products skip soil-pH curve entirely (cuticle uptake only sees foliarPhResponse(tankPh) × 0.30 coverage), soil/fertigation products keep PH_RESPONSE penalty. Verifier failures 9 → 6.
- 07:08 — REQ-002 wired with FORBIDDEN_PRODUCTS blocklist in scripts/check-recipes.mjs. 26 entries (urea, ammonium/calcium nitrate, mono/di-ammonium phosphate, monopotassium phosphate, KH2PO4, potassium chloride, muriate of potash, Fe-EDTA, Plagron/Canna/general hydroponics, glyphosate/roundup, mancozeb/chlorothalonil, French equivalents). Short acronyms (MAP/DAP/MOP/EDTA/GHE/KCl/CAN-17) intentionally omitted — collide with .map()/predictedTankPh/chelate-edta/KSP_SAFE reason fields/"higher". Was previously manual-review-only. PASS. Tally: 37→39 wired, 42→40 documented (parallel REQ-003/056/057 removal).
- 07:35 — REQ-003, REQ-056, REQ-057 removed. Principle codified: if not auto-enforceable, not a requirement. Manual review items belong in team process (Catherine's #review channel), not requirements.md.
- 07:03 — REQ-062 added: single fertigation tank + single foliar spray per week. Codifies team workflow simplicity. Wired in node verifier.
- 06:53 — TOMATO_STAGES const removed (Path A code-honesty cleanup). Was redundant with computeStageRecipe() — every reader now calls the function directly. REQ-016 (stored vs FP drift) retired since the comparison was meaningless. RECIPE_HISTORY entry added.

## 2026-05-07

- 21:15 — REQ wiring batch in scripts/check-recipes.mjs: tally 17/43 → 38/43. Wired REQ-013/014/015/016/017/018/020/021/024/025/026/027/029/030/031/032/053/054/055/060/061. Added exposure for RECIPE_INPUTS, PRODUCT_PCT, SIDEDRESS_AREA_PER_PLANCHE, SIDEDRESS_MIN_EFF, TOMATO_NUM_BEDS, TOMATO_BED_AREA, computeStageRecipe, predictedTankPh, foliarPhResponse, effectiveEff, INCOMPATIBLE_RECIPES, MIX_ORDER. Skip list (5): REQ-002/003/028/056/057 (manual-review by design). Genuine model-gap failures surfaced (NOT patched): REQ-013 P under-supply T4/T5 (P decorative at pH 7.4), REQ-014 N/Mg over-supply T1-T3 (low demand), REQ-018 MnSO4/ZnSO4/CuSO4 below 5% threshold under sulfate-metal pH curve, REQ-024 T1/T2 fertigation CE below 0.3 floor (zero-dose stages), REQ-025 foliar CE 9.48 > burn cap 8.0, REQ-026/027/030/031 stub-fail (UI/constants not implemented). Per task spec: did not change model to make checks pass.
- 21:00 — Historique nutriments refactorisé en table date + résumé, expand → snapshot complet (fertigation/sidedress/foliaire). Schema RECIPE_HISTORY étendu (`summary`, `fullSnapshot`). Helper `captureCurrentSnapshot()` ajouté. Entrées legacy gardées partielles (rendu "Snapshot partiel"). 34/34 PASS.
- 20:27 — T5 labels open-ended (Option C). "sem 19-26 · 8 sem" → "sem 19+ jusqu'à crop-out". Cycle totals shown as ranges (114-127 kg Actisol etc.) reflecting real spread of crop-out timing. Avoids the under-counting that the previous fixed labels created post-T6 removal.
- 20:25 — REQ-058 removed. Mass-balance refactor decoupled fertigation from transpiration; req no longer the right vehicle to enforce single-source for the formula. Irrigation + Banque sol pages still use the transpiration calc — flag if cross-page consistency becomes an issue.
- 20:18 — T6 stage removed. Mass-balance superseded OAQ §4.3.2 end-of-season heuristic — plants still remove K at end-of-cycle, arbitrary 50% cut had no first-principles basis. T5 extended to weeks 19-28. Snapshot logged in RECIPE_HISTORY.
- 20:00 — Historique nutriments admin page added. RECIPE_HISTORY constant captures retired recipe snapshots (TOMATO_STAGES hand-set, K/Mg multipliers, FOLIAR Spray B, Cu 4→2g). Audit trail for organic cert. Page slug `historique-nutriments`.
- evening — Nutrition recipe-source toggle now URL-persisted as a third path segment (`#nutriment/<crop>/<recipe>`, recipe ∈ {fp,stored}, sanitized + omitted when default). Default flipped `'stored'` → `'fp'`. parseHash/syncHash/toggleAdmin/hashchange + init block extended; `setNutrRecipeMode` calls syncHash, `setNutrStage` syncs hash on auto-revert. Loading `recipe=fp` forces `nutrStage='T5'`. 34/34 PASS.
- evening — Nutrition tomato page (Block 1-6): recipe-source toggle added in Cible & contexte (Stockée / Premiers principes). `calcNutrSupply(stage, phLocked, transpFactor, targetYield, recipeMode)` now reads either stored recipes or `FP_RECIPE_T5` (T5-only). Toggling to FP snaps `nutrStage` to T5; switching off T5 reverts mode to stored. New state `nutrRecipeMode`, new fns `setNutrRecipeMode`, `syncNutrRecipeModeUI`. `buildBanqueSol` call site unchanged (uses `supply.soil` only — recipe-independent).
- 08:55 — Nutrition tomato page reordered to mass-balance flow: 1) Besoin du plant 2) Compost (NEW — reads RECIPE_INPUTS.compostReleasePerWeek) 3) Engrais sol granulaire (sidedress now its own block, reads TOMATO_SIDEDRESS) 4) Fertigation 5) Foliaire 6) Leviers 7) Stockée vs calculée. Soil-passive (pantry) block REMOVED — clean break from gap-fill model. Gap chain now offtake → compost → sidedress → fert → foliar; deriveLever rewritten to drop soil-cap branch. Hypothèses copy rewritten for mass-balance framing. 34/34 PASS.
- late evening — Salanova subpage defaults retargeted: transplant 50→30 g, harvest cible 200→100 g (cycle 14 j, density 43/m² unchanged). New realistic next-cycle scenario per user; inputs remain editable for what-if.
- late evening — Mass-balance Phase 1b: compost K trimmed 0.42 → 0.40 g/m²/wk (mineralization 0.70 → 0.65), compost Mg trimmed 1.47 → 0.50 g/m²/wk (Mg % not on Savaria label, conservative 0.30% assumed and rounded down). TOMATO_STAGES auto-rederives — new values: T1 K=1647 Mg=0 · T2 K=2310 Mg=0 · T3 K=4465 Mg=0 · T4 K=3548 Mg=728 · T5 K=4953 Mg=1378 · T6 K=1181 Mg=0. TODO comment added above FOLIAR declaration for Phase 2 (foliar mass-balance refactor — label-string parsing required). 34/34 PASS.
- evening — Salanova subpage added to Nutrition page (admin). New crop sub-toggle (Tomate/Salanova) with `nutrCrop` page-local state routed via CROP_PAGES. Salanova model: continuous post-transplant (no stages) — demand from mass-gain × DM% × tissue conc. New constants: `SME_LETTUCE_PPM`, `LETTUCE_TISSUE_DW`, `LETTUCE_DM_FRACTION`, `LETTUCE_FRONTLOAD_DEFAULTS`. New functions: `calcLettuceNutrDemand`, `calcLettuceNutrSupply`, `setNutrCrop`, `buildNutrimentLettuce`, `lettuceLeverFor`. Inputs: transplant g, target g, cycle days, density, feather-meal front-load g/m², pH lockout. Renders 5 cards (besoins, sol, fertigation, front-load, leviers).
- 08:12 — Hook simplified (changelog-only, no git log). CLAUDE.md updated: Claude auto-appends to changelog after material changes; refresh-memory directive for older entries.
- 17:00 — Block 6 rebased: stored vs FP recipe (was stored vs naive computeRecipe). FP_RECIPE_T5 single source.
- 16:30 — REQ-060 added: narrative copy must not contradict current data (auto-derive or // stable —).
- 16:00 — Block 5 levers replaced with auto-derived `deriveLever(el)`. Stripped 8 stale advice strings; added 20 `// stable —` annotations.
- 15:00 — Banque sol page now shows stored vs FP trajectories side-by-side (5 columns).
- 14:00 — Recette proposée block updated to first-principles recipe (no anchoring on legacy).

## 2026-05-06

- evening — REQ-016 three-tier (green ≤5%, yellow 5-20%, red >20%). REQ-058 transpiration model declared explicitly.
- afternoon — Phase 3 cert UI added then removed (cert badges per number; user preferred no badges in admin UI).
- afternoon — Phase 2 node verifier added: scripts/check-recipes.mjs + package.json (jsdom). REQ-001/006/007/008 migrated bash → node.
- morning — Cu cut 4 g → 2 g in FOLIAR.tomato.A (Cu toxicity observed in image diagnostic, no surfactant).
- morning — Spray B (CaCl₂) removed from FOLIAR.tomato (Ecocert Teris industrial-grade listing not verified — audit Finding 9).

## 2026-05-05

- evening — Bilan → Nutrition rename (page slug `nutriment` unchanged for routing).
- afternoon — Cible sol + Cadre admin pages removed; Banque sol page added with bank-trajectory view.
- afternoon — LUXURY_FACTOR demand cap added (K=1.15, Ca/Mg/N=1.10, micros=1.0). Caps soil supply at min(mass-flow, demand × LUXURY_FACTOR).
- afternoon — Sidedress reframed as bank maintenance: dropped from supply sum, only feeds Banque view. MIXING_FACTOR_FERT=0.5 added (audit Finding 14).
- morning — Iron DL + yucca dropped permanently (until further notice). FeSO₄ 80 g/15 L is canonical foliar Fe.
- morning — Phase 1 model wired: PRODUCT (15), CHANNEL_ROLE, PH_RESPONSE (10 curves), KSP_PAIRS, etc. Functions: effectiveEff, predictedCE, predictedTankPh, computeRecipe.
- morning — Tissue test ordered (petiole NO₃-N + Mg + Cu/Mn/Zn panel; results in 5-7 days).
