# Décembre — Changelog

Append-only log of substantive changes (recipe edits, REQ additions, model architecture, page restructuring). Read at start of any session touching app state, especially under parallel Claude sessions.

Format: `YYYY-MM-DD HH:MM — brief description`. One line per change. Most recent at top. Keep ~10 entries; older history lives in `git log`.

## 2026-05-23

- **2026-05-24 02:05 — Phase 3 nutrition reorg — REQ-116 extracted + shell orchestrator carve.** `nutrition/tomato/shell/supply.js` deleted; replaced by `nutrition/tomato/shell/contribution-orchestrator.js` (same `calculateNutritionSupply` signature + `statusFor` + page-local state + `LUXURY_FACTOR` cap + DOM reads — preserved verbatim). REQ-116 FP foliar gap-derivation block extracted into a new pure `deriveFoliarRecipeFromGap` in `nutrition/tomato/foliar-recipe/model/contribution.js`: inputs are `{ demand, compostRelease, fertigationPre, sidedressPre, foliarOpts }`, returns the FP_RECIPE_T5.foliar key shape (or null). Orchestrator pre-computes sidedress + fertigation per-element maps via the existing sibling contribution functions and routes the call through `window.FoliarRecipeTomato.deriveFoliarRecipeFromGap` (namespace surface). Defensive try/catch around the foliar derivation dropped (was a guard against a window.* indirection that no longer exists; same script bundle). `window.FoliarRecipeTomato` namespace gains `deriveFoliarRecipeFromGap` re-export (recipe.js tail). `app/index.html` @include line 559 rewired; `scripts/check-recipes.mjs` REQ-139 registry row updated `computeFoliarRecipeForGap` → `deriveFoliarRecipeFromGap` (raw function still has its own REQ-115 namespace-level test). `npm test` 20/20; `npm run check` 161/0. NOT touched: `STORED_RECIPE.tomato.*`, `RECIPE_HISTORY`, `dist/`, `scripts/check-requirements.sh`, spec.md files. Two mailbox entries filed (nutrition/tomato/shell + nutrition/tomato/foliar-recipe).
- **2026-05-23 21:45 — Phase 2 chemistry pull-up (code split + dead-code deletion).** Carved `nutrition/tomato/lib/recipe-math.js` (533 lines, tomato-scoped framing) into a cross-crop `nutrition/chemistry/model/` layer (four files) + tomato-side `nutrition/tomato/channel-role.js`. New chemistry files: `products.js` (PRODUCT catalog, pure data, `window.NutritionProducts`); `ph-response.js` (PH_RESPONSE curves + foliarPhResponse + effectiveEfficiency, `window.NutritionPhResponse`); `compatibility.js` (KSP_PAIRS / KSP_SAFE / TAG_INCOMPATIBILITIES / TAGS_INERT / MIX_ORDER / INCOMPATIBLE_RECIPES + the validatePhase1ModelCoverage IIFE, `window.NutritionCompatibility`); `predicted.js` (predictedCE + predictedTankPh, `window.NutritionPredicted`). New tomato file: `channel-role.js` (CHANNEL_ROLE, `window.TomatoChannelRole`). Deleted dead code: `computeRecipe(crop, stage, channel)` (~105 lines, no live callers — only doc-comment mentions in `app/index.html` lines 175/533) and `passiveSupplyMassFlow(crop, element, week)` (~15 lines, only its own internal caller inside `computeRecipe`). `app/index.html`: one `@include nutrition/tomato/lib/recipe-math.js` became five includes in dependency order (products → ph-response → compatibility → predicted → tomato/channel-role); section-7 header comment rewritten. `scripts/check-recipes.mjs` stale `'computeRecipe'` expose-name dropped from the instrumented globals list + leading-comment + REQ-022 narrative trimmed accordingly (typeof-guarded so absence was already safe). `nutrition/tomato/lib/` directory retained — still holds `styles.css`. Verifiers green: `npm test` 20/20; `npm run check` 161/0. NOT touched: `STORED_RECIPE.tomato.*`, `RECIPE_HISTORY`, `dist/`, `scripts/check-requirements.sh`. Two mailbox entries filed (nutrition/chemistry + nutrition/tomato).
- **2026-05-23 23:30 — Phase 2 spec reorg — REQ-011 lifted to cross-crop + new `nutrition/chemistry/spec.md`.** Per locked Phase 2 plan: (1) REQ-011 moved from `nutrition/tomato/spec.md` to `nutrition/spec.md` with generalized wording — every crop with a biomass-demand table at `nutrition/<crop>/plant-needs/model/data.js` MUST ship `nutrition/<crop>/channel-role.js` exporting `CHANNEL_ROLE` covering every demand element, channel fractions sum to 1.0 ± 0.05. (2) New `nutrition/chemistry/spec.md` framing the cross-crop chemistry layer: product catalog (REQ-010, 019, 022, 023, 029a, 053 phContribution, 054 stablePhRange), tank-level predictions (REQ-015, 021, 024, 025), mixing-compatibility tables (REQ-029, 029b, 029c, 030, 031, 032), pH curves (REQ-053, 054, 055). 17 REQs migrated out of `nutrition/spec.md`; content byte-identical. Cross-crop nutrition rules (mass-balance REQ-013/014/017/018/020/061/062, contribution payloads REQ-136..162) stayed in `nutrition/spec.md`. (3) `requirements.md` Domain organization table gains chemistry row; nutrition row reworded to drop "chemistry" from its scope. REQ-012 now redundant under new REQ-011 wording (flagged to pruner via mailbox; not deleted unilaterally). Code split into `nutrition/chemistry/model/{products,ph-response,compatibility,predicted}.js` is specialist's parallel lane (not touched). Three mailbox entries filed to team-leader (nutrition / nutrition/chemistry / nutrition/tomato).
- **2026-05-23 21:25 — Phase 1 code reorg under `nutrition/tomato/` (model / procedure / operator shape; companion to the spec reorg).** Per recipe channel (fertigation / sidedress / foliar): `data.js`, `derivation.md`, `learnings.md`, `spec.test.mjs`, `test-helpers.mjs` moved into `model/`; `calc.js` → `model/recipe.js` with the channel's `window.Xxx = { … }` export from the retired `model.js` appended as tail (`// Browser-globals export`). New `model/contribution.js` per recipe channel extracts the channel's slice of `calculateNutritionSupply` as a pure function: `computeFertigationContribution`, `computeSidedressContribution`, `computeFoliarContribution`. `app/stored.js` → `procedure/stored.js` (byte-identical). New `procedure/steps.js` stub per channel. Operator-facing UI (`logic.js`, `page.html`, `page.css`, `drift.js`) moved to `operator/` for fertigation + foliar (sidedress has none). `plant-needs` same pattern but `calc.js` → `model/demand.js` (no procedure / operator). `nutrition/tomato/app/` → `nutrition/tomato/shell/`; `supply.js` refactored to call the three per-channel contribution functions and aggregate — DOM reads + FP-vs-stored selection + REQ-116 `FP_RECIPE_T5.foliar` mutation stay at the shell layer; top-of-file flagged `TRANSITIONAL` (Phase 2 move to `shell/contribution-orchestrator.js`). `app/index.html` @include paths rewired (≈30); `scripts/check-recipes.mjs` source-grep paths `tomato/app/logic.js` → `tomato/shell/logic.js` (4×). NOT touched: `lib/recipe-math.js`, `computeRecipe` / `passiveSupplyMassFlow` (Phase 2); `STORED_RECIPE.tomato.*` data; `RECIPE_HISTORY`. `npm test` 20/20; `npm run check` 161/0. Five mailbox entries filed to team-leader.
- **2026-05-23 22:00 — Phase 1 spec reorg under `nutrition/tomato/` (structural, no semantics changed).** Per locked plan: 4 model specs moved into `model/` subdirs (`fertigation-recipe`, `sidedress-recipe`, `foliar-recipe`, `plant-needs`). Admin-page spec `nutrition/tomato/app/spec.md` split into `shell/spec.md` (REQ-104..107, REQ-004, REQ-153) + `plant-needs/builder/spec.md` (REQ-108..111) + `foliar-recipe/builder/spec.md` (REQ-113, REQ-114, REQ-163); original deleted. Operator-page spec `nutrition/tomato/fertigation-recipe/app/spec.md` split into `procedure/spec.md` (REQ-182 stage-resolution, REQ-183, REQ-185..188 calc, REQ-190, REQ-191) + `operator/spec.md` (REQ-181, REQ-192 NEW header-label, REQ-184, REQ-193 NEW anchor-placement, REQ-189); original deleted. Two new ids claimed: REQ-192, REQ-193 for the operator halves of the REQ-182 / REQ-188 splits. Scaffolded empty `fertigation-recipe/builder/spec.md` + `sidedress-recipe/builder/spec.md`. Updated `requirements.md` Domain organization table. Specialist owns parallel code-file moves; team-leader owns test relocation. Mailbox entries filed to team-leader (one per touched subproject). No `Bilan` → `Builder` user-facing renames executed — every Bilan occurrence in `app/**` + `nutrition/**/*.{html,js}` is inside `//` or `<!-- -->` comments; the admin page header in the live nav reads "Nutrition", not "Bilan". Surfaced as a no-op item to team-leader.
- **2026-05-23 17:36 — STORED_RECIPE.tomato.fertigation.T5 revu PA Taillon.** MgSO₄ 1396 → 2800 g et K₂SO₄ 3489 → 7000 g (doublés) pour le pic de production T5 ; borax 9 g retiré du canal fertigation (B continue d'être livré via foliaire Solubore 7 g / 15 L). Oligos Fe / Zn / Mn / Cu / Mo inchangés. T1-T4 inchangés. Snapshot complet (fertigation T1-T5 + sidedress T1-T5 + foliaire) capturé dans `RECIPE_HISTORY` (`app/historique-nutriments/history.js:51`). Touché : `nutrition/tomato/fertigation-recipe/app/stored.js:20` (T5 + commentaire d'ancrage). `bash scripts/check-requirements.sh` 161/0 ; `npm test` 20/20.
- **2026-05-23 — Cation-antagonism hypothesis + § 4.5 reframe captured in research doc.** Three documentation additions landed (no constants moved, no spec change): (1) `nutrition/tomato/research/tomato-t5-tissue-analysis-2026-05-23.md` § 4.5 reframe — operator cold-leaf observation (leaf < ambient by touch, consistent with healthy transpiration) invalidates the "Ca tissue gap is solely transpiration-side" framing; root-uptake-side candidates (cation antagonism + P-starved root mass) flagged as remaining in modelling gaps; (2) same doc, new § 4.7 cation antagonism hypothesis as root-uptake-side candidate explanation for Ca tissue gap — mechanism (K⁺ + Mg²⁺ fertigation flux from K₂SO₄ 3 489 g + MgSO₄ 1 396 g/wk competing with Ca²⁺ at root-membrane transporters), modelling gap (REQ-155 `PH_UPTAKE_FACTOR_AT_CURRENT_SOIL` only encodes the Ca→K/Mg direction, not reverse), cohort-validation gate (n ≥ 2 stage-tagged + paired Mehlich-3 + STORED traces) for adding a Ca entry to that constant; (3) `nutrition/tomato/fertigation-recipe/derivation.md` REQ-155 refinement triggers gained a new "Symmetric extension — Ca entry candidate" bullet pointing at the research doc § 4.7 hypothesis trace, probable starting value 0.85-0.90 mid-band-mirroring-K-discount. All hypothesis-stage cert 3; no constant change. MgSO₄ STORED trim candidate (1 396 → ~700 g/wk, three-for-one: tissue Mg above floor, Mg→Ca antagonism reduced, sulfate-S over-supply trimmed) flagged operator-side, gated on `/retire-recipe`.
- **2026-05-23 — FarinePlumes mineralization eff refit 0.75 → 0.70.** Single-cohort refit triggered by TOM #1 T5 tissue (leaf N 2.27 % vs lab floor 3.10 %, -27 %). Modelled STORED N delivery under old 0.75 eff was 2.39 g/m²/wk vs T5 demand 4.05 g/m²/wk (-10 % gap); under new 0.70 eff (Sonneveld literature floor 70-85 % band) it's 2.23 g/m²/wk (-26 % gap), matching observed tissue -27 % almost exactly. Touched: `nutrition/tomato/sidedress-recipe/data.js` (`SIDEDRESS_PRODUCTS.FarinePlumes.efficiency` + `SIDEDRESS_EFFICIENCY_AT_CURRENT_PRODUCTS.N`); `derivation.md` (Option A table T3 / T4 / T5 farine_g recomputed 637/1064/1655 → 683/1140/1773; cert defense + channel-eff map updated; refinement-trigger entry reframed as "refit done, sub-floor gated on n ≥ 5"); `spec.md`; `app/stored.js` header (effective N comment + new FP target 1 773 g/planche/wk); `spec.test.mjs` + `scripts/check-recipes.mjs:1567` fallback defaults; research doc `tomato-t5-tissue-analysis-2026-05-23.md` sections 3, 4.4, 4b, 5, 7 — FP T5 sidedress shifts 1 655 → 1 773 g/planche, STORED gap widens 19 % → 24 % vs new FP, Honest Read flips to sidedress ramp as the dominant lever. Cert held at 3 — value still inside lit band. Further sub-floor refit gated on n ≥ 5 cohorts + petiole NO₃-N. Lettuce + nursery feather-meal eff untouched (different contexts; deliberate test fixtures at 0.75 preserved). `npm test` 20/20; `npm run check` 161/0. STORED ramp 1 341 → 1 773 g/planche/wk T5 gated on `/retire-recipe`.
- **2026-05-23 — Specialist tomato T5 research doc — STORED vs FP clarification.** New § 4b in `nutrition/tomato/research/tomato-t5-tissue-analysis-2026-05-23.md` documenting per-channel STORED-vs-FP divergence at T5: fertigation identical (REQ-154 boot-pin), sidedress STORED under-applies Farine ~38 % vs FP (1 341 vs ~1 850 g/planche/wk), foliar STORED slightly over FP on Mn / Zn / Mo. Implication: hypothetical FP-level sidedress would close the modelled N gap to ±0 % vs demand, but observed tissue gap (-27 % of floor) is wider — additional factors (Farine eff possibly optimistic, cumulative earlier-stage shortfall). Priority ranking updated: sidedress ramp moves to #1 (most direct N-gap lever), foliar Ocean to #2 (complementary). All STORED edits gated on `/retire-recipe`.
- **2026-05-23 — Specialist tomato T5 findings propagated into three derivation files.** Pointers back to the research doc landed at the natural homes: (1) `nutrition/soil-contribution/derivation.md` § "Example runway: tomato P" — tissue validation block confirms the mass-flow-binds lockout-regime prediction at peak demand (lockout-mechanism cert 3 → 4); (2) `nutrition/tomato/fertigation-recipe/derivation.md` Caveats — sulfate-form S over-supply at T5 documented as a known structural characteristic of K2SO4+MgSO4 chemistry (+300 % vs demand, validated by tissue 2.29 % vs ceiling 0.70 %), with sulfate-vs-KCl trade-off explained; (3) `nutrition/tomato/sidedress-recipe/derivation.md` Refinement triggers — 2026-05-22 N gap finding (modelled -10 % vs observed -27 % of lab floor) flagged, refit gated on n ≥ 5 cohorts + petiole NO₃-N. All three additions are pointer-only — full analysis lives in research doc, not duplicated. No spec change, no REQ claim.
